node_modules/
dist/
.env
.env.local
proxy-server/node_modules/
proxy-server/service-account.json

# Local Netlify folder
.netlify

# Certificados locales mkcert (nunca commitear claves privadas)
certs/

# Planillas reales de personal (RUT/correo/nombre) usadas para alta masiva —
# nunca deben llegar al repo.
Trabajadores*

# Excel real de precios LPU / facturación a Entel (ver scripts/importar-lpu.mjs)
# — datos comerciales reales, nunca deben llegar al repo.
*.xlsm
