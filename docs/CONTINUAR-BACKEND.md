# Continuar — Migración a backend Supabase

> Documento de retomada — LEER ESTO PRIMERO si se retoma en otra máquina o
> sesión nueva. Última actualización: 26-08-2026.

## ⚡ Estado ahora mismo (26-08-2026)
- **`main` y `backend-supabase` están sincronizados** (merge del 25-08, push confirmado a `main` el 26-08) — ambos en v1.72. Seguir mergeando `backend-supabase` a `main` cuando el trabajo esté listo para producción, en vez de dejarlo acumularse.
- **Migraciones**: corridas y confirmadas hasta `0066`. Ninguna pendiente.
- **Para retomar en otra máquina**: `git clone` (o `git fetch` + `git checkout backend-supabase`), `npm install`, recrear el `.env` a mano (los nombres de variable están en `src/lib/supabaseClient.ts` y `src/lib/auth.ts`; los valores NO están en el repo, es público — Andrés los tiene), `npm run dev`.
- **Pendientes de Andrés** (no bloquean nada salvo lo marcado):
  1. ~~Desplegar la Edge Function de alta de usuarios~~ — **hecho y confirmado el 26-08** (probada sin sesión y con token inválido, responde el gate de auth de la función, no un 404 — está desplegada de verdad).
  2. Completar en Administración el texto de Corrección de los 2 hallazgos que llegaron sin definir ("Falta Planimetria" y "CTO en condición insegura o no autorizada") — ya no es tarea de código, es editar el campo ahí mismo.
  3. Corregir con el modo corrección los 2 "Asignado a técnico" que quedaron en la OTT de prueba.
  4. Decidir si se automatiza la reversión de resoluciones al anular (bloqueo conocido: `resolver_evento_parcial` no llena `movimientos.evento_resolucion_id`).
  5. Volver a guardar la contraseña en Firefox desde `about:logins` — las guardadas antes de v1.47 quedaron con campos anónimos y no se autocompletan.
  6. **Falta un historial global de eventos resueltos** (ver la entrada sobre "Ignorar" más abajo) — decidir si vale la pena una pantalla nueva para eso o basta con lo que ya hay por conteo.
  7. **Recuperar el levantamiento del colega** (ver entrada v1.59 más abajo) — el ZIP le creó un informe vacío en el servidor (0 puntos). Con el fix de esa versión, borrar ese levantamiento local (o el registro server-side si ya no está en el store local de nadie) y volver a importar el mismo ZIP.
  8. **Pendiente para la semana (sin fecha fija)**: reescribir el texto de advertencia de "Corregir errores de tipeo" en Resumen de Proyecto — hoy dice "para arreglar un error de tipeo" y eso confunde con el caso real de "anoté 6, eran 5, ya hubo un movimiento real de 6". Corrección NUNCA revierte el movimiento ni el stock, solo pisa el número que se ve en la tabla — si el movimiento real fue mal registrado, hay que anularlo y volver a registrar con la cantidad correcta, o el stock queda mintiendo (técnico con más de lo que en verdad tiene). Andrés pidió dejarlo pendiente, no implementarlo ahora.
- **Deploy**: el push del 26-08 a `main` falló al desplegar por una interrupción real de GitHub Actions/Pages (confirmada en githubstatus.com, no un problema del repo) — falta reintentar el workflow ("Re-run all jobs") una vez que GitHub se recupere. Fuera de eso, `.github/workflows/deploy.yml` publica bien en cada push a `main`.

## v1.80 — dos bugs reales: pantalla de carga colgada + texto de OTT cortado a medias (mejora)
Andrés reportó dos cosas nuevas, sin relación con la tabla física de las últimas versiones.

**1. "El sitio suele quedarse colgado en la pantalla en que sale un logo de antena, en especial la primera vez que se abre."** Esa pantalla (📡, `App.tsx`) se muestra mientras `useAuth().loading` es `true`, y `loading` SOLO pasaba a `false` dentro del `.then()` de `supabase.auth.getSession()` (`src/lib/auth.ts`) — sin `.catch()` ni límite de tiempo. Si esa llamada nunca resolvía (o algo dentro reventaba sin capturarse, ej. `fetchProfile` fallando por red en el primer intento con conexión inestable — justo el escenario de "la primera vez que se abre"), la pantalla de carga se quedaba así para siempre, sin caer nunca al login.

**Fix**: `Promise.race` entre `getSession()` y un timeout de 8s, más un `.catch()` — si la sesión no se resuelve a tiempo (o falla), igual se sigue: peor caso, aparece el login para entrar de nuevo, en vez de quedar colgado sin salida. Verificado en el navegador: login normal sigue funcionando igual que antes (sin regresión).

*De paso, algo que noté pero no se tocó esta vez*: el bundle principal pesa ~2 MB (739 KB comprimido) — bastante para una primera carga en una conexión mala, y puede ser OTRA causa de demora al abrir (no solo la sesión). Arreglarlo bien implicaría separar el código por módulo (ATT/Preventivos/Incidencias/Inventario) con carga diferida (`React.lazy`) — un cambio más grande, fuera de este pase. Aviso si vale la pena abordarlo aparte.

**2. "Pasa menos, pero aún ocurre que mientras se escribe la OTT el texto se corta a mitad del ingreso."** Este bug YA se había investigado y parchado antes (comentario de la propia `mergeFromServer` en `store.ts`, commit `8ba8f71`: "bug real reportado por Andrés — texto de OTT cortado a la mitad al cambiar de pestaña"). Ese fix cambió la comparación de `local.updatedAt > server.updatedAt` (dos relojes distintos) a comparar por IGUALDAD contra `syncedAt` — mucho más confiable, pero **`updatedAt` sigue siendo `Date.now()`**, y varios navegadores (sobre todo móviles, por endurecimiento contra fingerprinting) redondean su precisión — dos ediciones distintas muy seguidas pueden caer en el mismo milisegundo y `mergeFromServer` las trata como "la misma versión", dejando que el servidor pise la edición local en curso. Coincide con "pasa menos" (el fix de relojes ya cerró la mayoría de los casos) "pero aún ocurre" (el redondeo de precisión en celular sigue abierto).

**Fix**: nuevo `src/core/utils/monotonicNow.ts` — da la hora igual que `Date.now()`, pero garantiza que cada llamada devuelve un valor estrictamente mayor que la anterior, sin importar la resolución real del reloj. Se usa en `touch()` de los tres stores con el mismo patrón (`att/store.ts`, `preventivos/store.ts`, `incidencias/store.ts` — los tres tenían la función idéntica, ver v1.61-era). No se tocó `mergeFromServer`/`persistToServer`, que ya estaban bien diseñados — el problema era la precisión de la marca de tiempo que comparan, no la lógica de comparación en sí.

**Ojo**: es una condición de carrera, así que no hay forma de "probarla" de forma determinística ni confirmar en este entorno que ya no pasa — es la causa más probable dado lo que ya se sabía del bug (el propio comentario del código apuntaba a exactamente este síntoma), pero falta que Andrés lo note (o no) con uso normal.

## v1.79 — columna fija transparente en celular: se saca border-collapse (bug conocido con sticky)
Andrés en celular: "los datos se vuelven transparentes cuando hay uno en mitad del ingreso, y queda atrás del texto de nombre" — un número que se está tipeando en Solicitado/Entregado/etc. se veía transparente, "debajo" del texto fijo de Descripción al escrollear.

**Causa probable**: `position: sticky` en una celda (`<td>`/`<th>`) de una tabla con `border-collapse` es una combinación con bugs de pintado conocidos y documentados en varios navegadores — sobre todo en mobile. El fondo sólido de la celda fija no siempre se pinta de forma confiable por encima de lo que se escrollea debajo, dejando asomar contenido de otras celdas. Ya van dos bugs reales de esta columna fija que solo aparecían en el celular real y no en las pruebas de este entorno (v1.78 fue el otro) — encaja con que `border-collapse` + sticky es justo el tipo de cosa que los navegadores implementan de forma inconsistente.

**Fix**: la tabla pasa de `border-collapse` a `border-separate border-spacing-0` — separa la celda fija del modelo de bordes fusionados que causa el problema. Como contrapartida, un borde puesto en el `<tr>` (`border-t`) deja de pintarse en modo "separate" (no es válido ahí según el estándar) — se reemplaza por una sombra interna (`shadow-[inset_0_1px_0_0_#334155]`) que sí funciona en cualquier modo y se ve igual. Se agrega también `isolate` a las celdas fijas, para que tengan su propio contexto de apilamiento y no dependan de cómo el navegador resuelva el z-index alrededor.

Verificado en el navegador (v1.79): la tabla renderiza con `border-separate border-spacing-0`, la línea entre filas sigue viéndose (confirmé el box-shadow calculado, `rgb(51,65,85) 0px 1px 0px 0px inset` = slate-700), sin romper nada. **No pude reproducir el bug de transparencia en este entorno** (mismo límite que los dos anteriores) — es el mejor fix conocido para esta combinación específica, pero falta que Andrés confirme en su celular real que ya no aparece.

## v1.78 — fix real de truncado: v1.77 no cortaba el texto en el celular de Andrés
Andrés mandó un pantallazo real: la columna fija de Descripción se veía casi a todo ancho, con "CAJA C-MIC-12 6FO COMPLETA C/JUMP-PRENSA" completo, sin cortar — v1.77 no estaba funcionando en su celular pese a que en las pruebas de este entorno sí truncaba.

**Causa**: el `w-20` de v1.77 estaba en el `<td>`, no en el `<p>` que tiene `truncate`. En una tabla con layout automático (`<table>` sin `table-layout: fixed`, que es como está esta), un `width` en la CELDA es solo una sugerencia — el algoritmo de layout puede seguir agrandando la columna según el contenido, sobre todo con `white-space: nowrap` (parte de `truncate`), que le dice "mi ancho mínimo es todo el texto sin cortar". El `<p>` mismo no tenía ancho propio, así que terminaba ocupando lo que la columna decidiera darle — en este entorno de pruebas la columna sí quedaba angosta, pero en el navegador real de Andrés no.

**Fix real**: el `w-20` se pone directo en el `<p>` (y el `<span>` del encabezado) — el elemento que tiene `overflow:hidden` necesita su propio ancho explícito para que el corte sea confiable en cualquier navegador, sin depender de cómo la tabla decida repartir el espacio.

Verificado en el navegador con el MISMO material del pantallazo de Andrés ("CAJA C-MIC-12 6FO COMPLETA C/JUMP-PRENSA", 262px de ancho natural): la celda renderiza en 80px con corte real confirmado (`scrollWidth > renderedWidth`), no solo el CSS puesto — antes solo había verificado que la propiedad `text-overflow` estuviera activa, sin confirmar que de verdad cortara.

## v1.77 — columna fija de Descripción, angosta (~10 caracteres)
Andrés probó v1.76 en celular: la columna fija de Descripción quedaba tan ancha que, con nombres largos, no dejaba espacio útil visible hacia la derecha. Se angosta a `w-20` (~80px, unas 10-12 letras según el texto) en el encabezado y las dos filas de datos, con `truncate` + `title` (para poder ver el nombre completo pasando el mouse en escritorio, ya que ahora se corta más agresivo).

Verificado en el navegador (v1.77, 375px): la celda fija mide ~96px de los 307px visibles (antes ocupaba mucho más), dejando ~210px libres para el resto de la tabla; `text-overflow: ellipsis` confirmado activo.

## v1.76 — tabla física: Descripción fija (sticky) al escrollear, Técnico al final
Andrés en celular: "aún se puede escrollear a la derecha fuera de pantalla" — y aclaró el pedido de legibilidad de v1.75: en la tabla de Material físico, por defecto se ve técnico/SKU/descripción bien, pero al escrollear a la derecha para llegar a los campos Solicitado/Entregado/etc. (para tipear números), esas columnas se pierden de vista y no se sabe de qué material se trata la fila.

**Confirmado que NO es un bug de página**: revisé `document.body.scrollWidth` vs `window.innerWidth` en celular (375px) y son iguales — no hay overflow horizontal del documento, es el scroll INTERNO de la tabla (`overflow-x-auto`, 1281px de tabla en 307px de ancho visible), que es el comportamiento esperado de una tabla ancha. El problema real es de contexto: al escrollear esa tabla, se pierde de vista qué fila es cuál.

**Fix**: la columna **Descripción queda fija (`position: sticky; left: 0`)** — no importa cuánto se escrollee la tabla a la derecha, el nombre del material se sigue viendo. **Técnico se movió al final** ("al fondo a la derecha", tal como pidió Andrés) — no hace falta verlo mientras se tipean números, a diferencia del material. Nuevo orden: Descripción (fija) → SKU → Bodega → Lote → Solicitado → Entregado → Instalado → Devuelto → Merma → Asignado a técnico → Tránsito → Nota → Técnico. Aplicado en el encabezado, las filas ya guardadas y "+ Nuevo material".

Verificado en el navegador (v1.76, 375px): `document.body.scrollWidth === innerWidth` (375 = 375, sin overflow de página); tras escrollear la tabla 500px a la derecha, la celda de Descripción ("ODF 12 FIBRAS") seguía pegada al borde izquierdo visible; Técnico confirmado como última columna del encabezado.

## v1.75 — asignar técnico en un solo paso + buscadores anclados al viewport (no al botón) + info de material más legible
Andrés probó v1.74 en celular: "la vista ya no deja el espacio en blanco por defecto, pero se crea un espacio blanco igual" — el fix de `preventScroll` ayudó pero no alcanzaba. Junto con eso pidió dos cosas más.

**1. Asignar técnico ya no pide "elegir + apretar un botón"** — con solo elegir del desplegable queda agregado. Afecta "Técnicos asignados" (`EquipoSection`, `LogisticaTab.tsx`) y "Equipo por proyecto" en Administración (`TeamsSection`, `AdminScreen.tsx`) — los dos tenían el mismo patrón de `<select>` + botón "+ Agregar" aparte; ahora el `onChange` del select llama directo a `add`/`addMember` (que ya limpiaban la selección al terminar, no hizo falta tocar esa parte). Verificado en el navegador: elegir un trabajador lo agrega al tiro, sin botón.

**2. Causa real del espacio en blanco residual**: el panel de `MaterialSelect.tsx` (y `LpuCodigoSelect.tsx`/`ProyectoSelect.tsx`, mismo patrón) calculaba su posición UNA VEZ al abrir, con `getBoundingClientRect()` del botón — coordenadas en píxeles fijas. `preventScroll` evita que el `.focus()` mismo dispare un scroll, pero no evita que el navegador scrollee la página por su cuenta cuando cambia el visual viewport (el teclado apareciendo) — y ahí esas coordenadas quedan desalineadas del contenido real, dejando el panel "flotando" sobre una zona en blanco.

**Fix de raíz**: se saca el cálculo de posición basado en el botón — el panel ahora se ancla directo al viewport con CSS puro (`top-3 left-1/2 -translate-x-1/2`, `max-h-[85vh]`), sin depender de dónde esté el botón ni de un valor calculado una sola vez. Un scroll del navegador ya no lo puede desalinear, porque no está anclado a nada que se mueva. Aplicado a los 3 selectores con este patrón (`MaterialSelect`, `LpuCodigoSelect`, `ProyectoSelect`) — `ColumnHeader.tsx` (el filtro de columna de Bodega/Movimientos/KPI) se dejó con el fix de `preventScroll` nomás, por ahora, al ser una pantalla más de escritorio.

**3. De paso, "el cuadro de material desde móvil no muestra mucha info"**: cada resultado en `MaterialSelect` pasa de una sola línea truncada (`SKU — descripción`) a dos líneas completas — SKU en negrita arriba, descripción completa (sin truncar) abajo. Se ve la info completa sin adivinar qué dice el `…`.

Verificado en el navegador (v1.75, 375×812): el panel de Material quedó anclado arriba (top≈12px), centrado, dentro del viewport (altura 690px de 812 disponibles) — foco confirmado en el buscador. Los resultados muestran SKU + descripción completa en dos líneas. **Sigue pendiente que Andrés confirme en su celular real** que el espacio en blanco ya no aparece (este entorno de pruebas no dispara el mismo comportamiento de teclado nativo).

## v1.74 — bug en celular: los buscadores flotantes (Material, LPU, Proyecto, filtro de columna) escondían la página
Andrés reportó en celular: al usar "Registro de material (físico)" — tras crear una fila en la tabla — "el cuadro hace que se vaya la visión a una parte del sitio sin información".

**Causa**: `MaterialSelect.tsx` (y los otros 3 selectores con el mismo patrón: `LpuCodigoSelect.tsx`, `ProyectoSelect.tsx`, `ColumnHeader.tsx`) portan su panel de búsqueda a `document.body` con `position: fixed` — necesario para no quedar recortado dentro de una celda de tabla con `overflow-x-auto` (ver comentario original en `MaterialSelect.tsx`). El campo de búsqueda de ese panel tenía `autoFocus`. En celular, al enfocarse un input aparece el teclado, y el navegador intenta hacer scroll de la PÁGINA para "mostrar" el input enfocado — pero el panel ya está fijo en pantalla (no depende del scroll), así que ese scroll nativo solo lograba correr el contenido real (la tabla) a un área en blanco del documento, dejando la vista "perdida".

**Fix**: se saca `autoFocus` en los 4 componentes y se reemplaza por `ref` + `useEffect(() => { if (open) ref.current?.focus({ preventScroll: true }) }, [open])` — mismo resultado (el campo queda enfocado al abrir, listo para escribir) pero sin el scroll automático del navegador que lo rompía.

Verificado en el navegador: en celular (375px) no reproduje el síntoma original ni antes ni después del fix (probablemente porque este entorno no dispara el mismo comportamiento del teclado nativo que un celular real), pero confirmé que el foco sigue funcionando igual (`document.activeElement` es el input de búsqueda al abrir el panel) — **falta que Andrés confirme en su celular real** que ya no pasa.

## v1.73 — tabla física: Bodega antes que Lote + bodega por defecto solo si hay stock digital
Andrés pidió dos cambios en la tabla de Material físico de la OTT (`ResumenProyectoTable.tsx`):

**1. Orden de columnas**: SKU → Descripción → **Bodega → Lote** (antes era Lote → Bodega) — mismo orden en el encabezado, en las filas ya guardadas y en la fila de "+ Nuevo material".

**2. Bodega por defecto más cuidadosa** en `handleMaterialSeleccionado` (se dispara al elegir el material de una fila nueva): antes la fila SIEMPRE partía en la bodega del área (C088 ATT / C132 OyM), sin importar si el material tenía algo que ver con esa bodega — para un SKU que nunca se movió en digital, era una adivinanza. Ahora:
- Parte en la bodega del área **solo si el SKU ya tiene algo de `cantidad_digital` registrado** en alguna bodega (es un material real que ya pasó por SAP).
- Si no tiene stock digital, la bodega queda vacía — **salvo** que el material sí tenga stock FÍSICO en alguna bodega (ej. un insumo que solo se maneja físico, como "Huincha Aislante"), en cuyo caso salta ahí — esa parte del comportamiento viejo (buscar dónde el material realmente tiene stock físico, para no generar un negativo falso) se mantiene intacta.

Verificado en el navegador (v1.73), tres casos con "+ Nuevo material" en una OTT ATT:
- SKU 51024 (ODF, con stock digital conocido) → bodega quedó en **C088**.
- "Huincha Aislante" (sin stock digital, pero con físico en Insumos) → bodega saltó a **Insumos**.
- "Corrugado" (sin stock de ningún tipo) → bodega quedó **en blanco** ("Bodega…").

## v1.72 — Stock por trabajador ya no se limita a Terreno/Logística
Andrés pidió que "stock por trabajador" en Inventario y "trabajador por proyecto" en Administración muestren cualquier trabajador, no solo los de Terreno.

**Administración → "Equipo por proyecto" (`TeamsSection`, `AdminScreen.tsx`) ya estaba bien** — `adminRepo.listProfiles()` sin filtro de rol, con un comentario que ya documentaba el cambio ("Cualquier trabajador, sin importar su área/rol — antes solo Terreno y Logística"). Mismo criterio que "Técnicos asignados" en la pestaña Logística de la OTT (`EquipoSection`, `LogisticaTab.tsx`), también sin filtro. Nada que tocar ahí.

**Inventario → Stock → Técnico (`TecnicoTab`, `Home.tsx`) sí tenía el filtro viejo**: `p.rol === 'tecnico' || p.rol === 'log'` excluía a Oficina/Admin del selector — alguien de oficina con stock a su nombre (por una Entrega, Conteo, etc.) no podía verse en esta pantalla. Se saca el filtro de rol, queda solo `p.activo`.

Verificado en el navegador: el selector de Stock → Técnico ahora incluye a ANDRES BARAHONA (admin) y el resto de Oficina, además de Terreno/Logística.

## v1.71 — Entrada de Ferretería SÍ pide lote (fix SQL 0065 + nueva 0066)
Dos cosas en un mismo tiro, reportadas por Andrés después de probar v1.70:

**1. Bug real en `0065`**: al correrla en el SQL editor de Supabase tiró `ERROR 42883: function max(uuid) does not exist` — Postgres no tiene `max()`/`min()` como agregado nativo para `uuid`. Estaba en las dos consultas de `proyecto_materiales` que agregaban `origen_ubicacion_id` con `max(...)` (esa columna no es parte de la clave única, solo se necesitaba "un valor cualquiera" por grupo). Cambiado a `(array_agg(origen_ubicacion_id))[1]`, que no tiene ese problema. Como el error rompía toda la migración (Supabase corre el script pegado como una sola transacción implícita — sin `BEGIN`/`COMMIT` propio, un error en cualquier statement revierte TODO, incluida la función que sí se había definido bien más arriba), `0065` no había aplicado ningún cambio — corregirla en el mismo archivo era seguro, nunca llegó a "correr" de verdad.

**2. Cambio de diseño**: Andrés recordó que aunque Ferretería no distingue lote FÍSICO, el material sí está asociado a un lote real en SAP — "se debe poder ingresar un lote, aunque sea ferretería, para que calce el inventario interno con el de SAP. Que se grabe en stock físico como físico, que se grabe en stock digital bajo el lote indicado." v1.69/v1.70 habían hecho lo contrario: ocultar el selector de lote para Ferretería también en Entrada (heredado del gate que sí es correcto para Entrega/Devuelto/Instalado/Merma, que consumen del montón físico indistinguible) — de ahí el reclamo "no me deja editar los lotes".

**Nueva migración `0066_entrada_ferreteria_con_lote.sql`** — toca `registrar_movimiento`, `corregir_movimiento` y `anular_movimiento` (reconstruidas completas a partir de sus últimas versiones reales — 0055/0059/**0058**, diffeadas contra el archivo nuevo para confirmar que el ÚNICO cambio real es la rama `entrada`, nada más se tocó de paso). Regla nueva, solo en `entrada` y solo si el material es Ferretería:
- El físico SIEMPRE se acredita en `'Físico'` (eso no cambió).
- Si además se indicó un lote real (no vacío, no `'Físico'`), ESE MISMO movimiento también acredita `cantidad_digital` bajo ese lote. La fila de `movimientos` guarda el lote real (no `'Físico'`) — es lo que tiene sentido ver/editar en pantalla.
- `corregir_movimiento` (usado por "Editar líneas" en Entradas registradas) revierte el estado viejo con el mismo criterio antes de aplicar el nuevo — mover el lote de una entrada de Ferretería mueve el crédito digital de un lote a otro, sin duplicar ni perder nada. `anular_movimiento` revierte ambos lados igual.
- **Depende de que `0065` ya haya corrido**: para movimientos viejos, `corregir`/`anular` deciden si hubo "lote real" mirando si `movimientos.lote` guardado no es `'SinDefinir'` ni `'Físico'` — eso solo es confiable una vez que 0065 normalizó el histórico a `'Físico'`. Correr 0065 antes que 0066.

**Cliente** (`RegistrarMovimientoForm.tsx`, `ListaRegistros.tsx`): se sacó el gate de Ferretería SOLO para Entrada — el `LoteSelect` (desplegable de lotes digitales + "+ Lote nuevo…", de v1.69/v1.70) ahora se muestra siempre en Entrada, sea Ferretería o no. Nota nueva bajo "Material" en el formulario de Entrada explicando el porqué. `ListaRegistros.tsx` perdió la plomería que ya no servía (`listMateriales`/`esTipoFerreteria` — el gate para Entrada desapareció, no había otro uso).

Verificado en el navegador (v1.71): en el formulario de Entrada, ABRAZADERA ALTA TENSION 1/2INCH (Ferretería) mostró el desplegable con lotes reales (`932717 (2)`, `939776 (1)`, etc.) + "+ Lote nuevo…", antes bloqueado en "Físico" fijo. En "Editar líneas" de una tanda ya registrada, FIERRO ANGULO (Ferretería) mostró el mismo desplegable con sus lotes reales + el valor ya guardado (`Físico (nuevo)`, porque esa fila específica no tenía lote real todavía). **El guardado real (`Registrar movimiento`/`Guardar cambios`) no se probó en el navegador** porque depende de `0065`/`0066`, que están escritas pero pendientes de correr en Supabase — falta que Andrés las corra y vuelva a probar el guardado de punta a punta.

## v1.70 — el desplegable de lote de v1.69 también en "Entradas registradas" (editar)
Corrección sobre v1.69: Andrés aclaró que el desplegable pedido no era (solo) para la fila nueva del formulario de arriba — era para el editor de tandas YA REGISTRADAS ("✎ Editar líneas" en "Entradas registradas", `ListaRegistros.tsx`), que es donde de verdad se completa el lote cuando Entel actualiza su stock (pedido original del 11-08, ver v1.18-era). Ese editor tenía a propósito un campo de texto libre — comentario en el código explicaba que "el lote muchas veces todavía no existe en `stock`, así que un desplegable no serviría" — razón que dejó de aplicar con el "+ Lote nuevo…" de v1.69 (cubre exactamente ese caso).

**`ListaRegistros.tsx`**: en modo Entrada, la celda Lote de cada fila en edición ahora usa el mismo `LoteSelect` (naturaleza digital, todas las bodegas, `soloConDisponible`) que el formulario de arriba, con el mismo gate de Ferretería (estático "Físico", sin selector) — para eso el componente ahora carga `listMateriales()` una vez y lo pasa a través de `RegistroCard` → `LineaRegistro`. El modo Asignaciones no se tocó, sigue con el campo de texto libre de siempre (no era parte de lo pedido).

Verificado en el navegador: en una tanda ya registrada (C132, 2026-08-10), la fila de MUFA_BC_FOSC-A_2_24_4R-1O_TERMO (no Ferretería) mostró el desplegable con los lotes reales `968415 (3)`, `971327 (8)`, `972198 (1)` + "+ Lote nuevo…"; las filas de Ferretería (FIERRO ANGULO, CRUCETA…) en la misma tanda siguieron mostrando "Físico" fijo, sin selector.

## v1.69 — Entrada ahora permite elegir lote (desplegable de los ya conocidos en digital + "Lote nuevo…")
Andrés retomó lo hablado sobre lote en entradas: "la idea es que a las entradas se les pueda poner lote. Debe aparecer como desplegable de los ya existentes en digital, aunque en físico quede como 'físico'. Dentro de los desplegables debe estar la opción de lote nuevo para digitarlo."

**`LoteSelect.tsx`** (compartido por Entrada, Entrega, Rebajado, Devuelto, Instalado, Merma) gana dos capacidades genéricas, sin romper ningún uso existente porque son opt-in por prop:
- `buscarTodasBodegas?: boolean` — ignora `ubicacionId` y busca el material en TODAS las bodegas (`getStock({ materialId })`), juntando en una sola opción los lotes repetidos entre bodegas (suma cantidades) — el código real de un lote de SAP no depende de a cuál bodega física llega la mercadería.
- `soloConDisponible?: boolean` — filtra el desplegable a lotes que de verdad tienen algo en la naturaleza pedida, sacando los placeholders `'SinDefinir'`/`'Físico'` (no son lotes reales).
- **"+ Lote nuevo…"** al final del desplegable, siempre disponible (no solo con las dos props de arriba): al elegirlo cambia a un campo de texto igual al patrón ya usado en `UbicacionSelect` ("+ Nueva bodega…"), para digitar un lote que todavía no existe en el sistema.

**`RegistrarMovimientoForm.tsx`**, fila de Entrada: se le pasa `naturaleza="digital"` (pisando el `'fisico'` de `ctx.naturaleza`, que se deja intacto para que el gate de Ferretería — `ctx.naturaleza === 'fisico'` — siga funcionando igual que antes) más `buscarTodasBodegas` y `soloConDisponible`, ambos `true`. Para Ferretería el selector sigue sin mostrarse (queda fijo en "Físico", sin cambios) — así se cumple lo pedido: el desplegable ofrece los lotes ya conocidos en digital, pero en físico Ferretería sigue sin distinguir lote.

Verificado en el navegador (v1.69): con el material SKU 51024 (no Ferretería) el desplegable mostró `972478 (4)` (el lote digital real, sumado entre bodegas) y `+ Lote nuevo…`; escribir `PLXC999999` en "Lote nuevo…" lo dejó seleccionado como `PLXC999999 (nuevo)`. Con SKU 6 (Ferretería, ABRAZADERA ALTA TENSION 1/2INCH) la celda siguió mostrando el texto fijo "Físico", sin selector — sin regresión.

## v1.68 — bug real: el buscador de Movimientos no encontraba OTTs viejas + Anular movimiento desde la OTT
Andrés reportó que el buscador de la pestaña Movimientos no mostraba OTTs anteriores al 13-08. Confirmado con datos reales de la BD: no es un problema de guardado (los 344 movimientos existen, sin huecos reales — solo el 18-08, un martes, quedó sin ninguno, el resto de "huecos" eran sábados). El bug es de la pantalla:

**Causa**: `listMovimientos()` siempre traía como máximo 200 filas (`order(fecha desc).limit(200)`), y el buscador de texto filtraba SOLO sobre esas 200 ya cargadas — nunca volvía a pedir al servidor. Con ~250 movimientos entre el 13-08 y hoy, el corte de 200 caía justo a mitad del propio 13-08: todo lo anterior a esa fecha nunca llegaba a bajarse, así que buscar un OTT viejo no encontraba nada aunque sí existiera en la base.

**Fix** (`inventarioRepo.ts` + `Home.tsx`): `ListMovimientosFilters.limit` ahora acepta `null` = sin límite. Mientras el campo de búsqueda tiene texto, `MovimientosTab` pide sin límite (antes solo reaccionaba a los filtros de fecha, nunca al buscador); sin texto, sigue trayendo 200 para que la vista por defecto cargue rápido.

**De paso, pedido de Andrés**: "pon la opción de anular movimientos desde OTTs, para no tener que andar buscando en movimientos". Nueva sección **"▸ Movimientos de esta OTT"** en `LogisticaTab.tsx`, después de Material digital — colapsada por defecto (es una herramienta de auditoría, no algo para ver en cada entrada a la OTT), oculta para técnico igual que Técnicos asignados. Al abrirla, `listMovimientos({ projectId })` (sin problema de límite, un proyecto nunca tiene cientos de movimientos) y el mismo botón "🗑 Anular" con el mismo diálogo de confirmación y el mismo aviso de `requiereRevision` que ya existía en la pestaña Movimientos de Inventario (mismo `TIPO_LABELS_MOV`, ahora exportado desde `inventarioRepo.ts` en vez de vivir duplicado en `Home.tsx`).

## Migración 0065 — traspaso de Ferretería ahora es SOLO físico (pendiente de correr)
Andrés revisó lo hecho en 0064 (v1.62): "haz el traspaso de todos los lotes de items marcados como ferretería a físico, pero solo en stock físico, en digital se deben mantener los lotes que indica SAP." Dos ajustes, sin tocar 0064 (ya corrida) — se escribió `0065_ferreteria_traspaso_solo_fisico.sql` nueva:

1. **Alcance más amplio en lo físico**: 0064 solo traspasaba el lote `'SinDefinir'`. 0065 traspasa CUALQUIER lote físico (no solo `'SinDefinir'`) a `'Físico'` — el físico de Ferretería nunca se distingue por lote, sin importar en qué lote haya llegado originalmente.
2. **Alcance más angosto en lo digital (el cambio importante)**: el traspaso deja de tocar el lado digital. `stock.cantidad_digital`, `proyecto_materiales.cant_rebajada` y los `movimientos` con `naturaleza='digital'` quedan intactos, bajo el lote real que entrega SAP.

**Es posible** porque físico y digital nunca dependen de filas separadas para poder tratarse por separado — son COLUMNAS separadas en la misma fila: `stock.cantidad_fisico`/`cantidad_digital`, y en `proyecto_materiales` las 4 columnas físicas (`cant_entregada/instalada/devuelta/rezagada`) vs. la columna digital (`cant_rebajada`). En `movimientos` cada fila ya trae `naturaleza` ('fisico'/'digital'), así que ahí ni siquiera hace falta separar columnas. La función nueva (`traspasar_fisico_ferreteria_a_lote_unico`, reemplaza a `traspasar_lote_sindefinir_a_fisico` que queda `drop`eada) mueve solo las columnas físicas hacia la fila `'Físico'` (sumando si ya existía saldo ahí) y las deja en 0 en el lote de origen — si al lote de origen le queda algo digital (`cantidad_digital`/`cant_rebajada` ≠ 0), la fila NO se borra, sigue viva con su lote real y solo la parte física en 0.

Trigger y bloque retroactivo (los mismos de 0064) se actualizan para llamar a la función nueva — **falta correr esta migración en Supabase** (no la corrí yo, sigue el mismo flujo manual de las anteriores). Sin cambios de cliente: `esFerreteria.ts`/`LoteSelect` ya solo tocaban el lado físico desde v1.62, no hace falta tocar nada ahí.

## v1.67 — copiado como TSV puro + fecha en formato Excel real
Andrés aclaró lo pedido en v1.64/v1.66 sobre "formato Excel": no bastaba con que las columnas calzaran — pidió que el TIPO de cada celda calce (no se vean "fuera de lugar" al pegar), y notó que pegar con formato vs. "pegar sin formato" pegaba distinto.

**Causa de las dos cosas:**
- Copiar una `<table>` HTML deja DOS versiones en el portapapeles: una con el HTML (fuente/colores/bordes de esta app, que se ven fuera de lugar en una planilla Excel) y una de texto plano derivada de ella. "Pegar normal" en Excel prefiere la versión HTML; "pegar sin formato" fuerza la de texto plano — son dos rutas de parseo distintas dentro de Excel, así que un mismo dato puede terminar tipado distinto según cuál uses (una fecha reconocida como fecha real por un lado, como texto suelto por el otro).
- La fecha de instalación se pasaba en `yyyy-mm-dd` (formato de un `<input type="date">`). El Excel de Entel usa `dd.mm.yyyy` — Excel no reconoce `2026-01-20` como fecha bajo ese formato, así que quedaba como texto plano: sin alinear a la derecha, sin ordenar ni calcular como el resto de la columna. Ahí estaba el "fuera de lugar".

**Fix — `TablaDigital` y `RebajaPendienteSection`:**
- Botón nuevo "📋 Copiar tabla" en las dos, escribiendo el portapapeles con `navigator.clipboard.writeText()` — TSV puro, SIN HTML. Al no existir una versión con estilos, "pegar" y "pegar sin formato" quedan idénticos: no hay dos rutas de parseo entre las que Excel tenga que elegir.
- Sin fila de encabezado — se pega al final de las filas que ya existen en el control de Entel, no las repite.
- Nuevo `formatFechaExcel()`: `yyyy-mm-dd` → `dd.mm.yyyy`, aplicado tanto en lo que se ve en pantalla como en lo que se copia — mismo string en los dos lados, para que lo que ves sea justo lo que se pega.

No hay forma de forzar que Excel trate un valor pegado como texto plano EXACTAMENTE como un tipo de celda nativo de Excel (eso solo lo logra un copiado Excel→Excel real, vía su portapapeles OLE propio) — lo que sí se puede, y se hizo, es dejar el string en un formato que el parser de Excel reconoce de forma confiable como el tipo correcto.

## v1.66 — cable no se reparte entre lotes + "Rebaja pendiente" con formato Excel
Dos ajustes de Andrés sobre `sugerirRebaja()`/`RebajaPendienteSection` de v1.64-v1.65:

**1. Cable es un caso aparte en la sugerencia.** El resto de materiales sigue repartiéndose entre varios lotes digitales (menor a mayor cantidad) si hace falta — cable NUNCA se fracciona:
  - Si el físico ya tiene un lote real conocido (no `'SinDefinir'`) — el caso normal, cable siempre se instala de un lote específico — la rebaja va contra ESE MISMO lote, siempre, sin comparar disponible ni buscar otro. El digital ya permite quedar negativo (0055), así que no hace falta que "alcance".
  - Solo si el físico todavía no tiene lote (`'SinDefinir'`) se busca un lote digital — pero UNO SOLO que cubra toda la cantidad (filtro `cantidadDigital >= necesario`, no `> 0`), priorizando igual la bodega del área y, entre los que alcanzan, el más ajustado. Si ninguno alcanza solo, queda una línea sin lote con el total — nunca se fracciona en varias líneas más chicas.
  - Cable tampoco se agrupa por material entre lotes distintos como el resto: cada fila física (material+lote) es su propia línea, 1 a 1.
  - `esTipoCable` (`esCable.ts`, ya existía) decide qué material es cable — mismo criterio que EP/lista de ATT.

**2. "Rebaja pendiente" (`RebajaPendienteSection`) reformateada** con las mismas 9 columnas y orden que "Material digital" (OTT, Dirección de trabajos, Fecha de instalación, RUT empresa, Dirección empresa, SKU, Material, Lote, Cantidad) — antes solo tenía Material/Lote/Bodega/Cantidad/Origen. Bodega/Origen/Acción quedan como columnas extra al final (uso interno, no están en el Excel de Entel). Recibe `ott`/`direccion`/`fechaInstalacion` igual que `TablaDigital` — mismos props ya threaded desde v1.64, solo faltaba pasarlos a este componente también.

## v1.65 — guardar rebaja pendiente reventaba: "Rebajado requiere proyecto, técnico y bodega de origen"
Bug de la v1.64 recién subida: `RebajaPendienteSection` arma cada línea con material/lote/bodega/cantidad pero SIN técnico (con razón — Andrés: "técnico no es relevante para rebajas desde C088"), y el loop de guardado llamaba `registrarMovimiento({tipoUI:'rebajado', ...})` sin mandar `tecnicoUserId`.

**No es un problema nuevo del servidor ni faltó SQL** — `registrar_movimiento` exige técnico para `rebajado` desde `0005_registrar_movimiento.sql` (la primera versión de la función), sin cambios desde entonces. `tecnico_user_id` ahí no mueve stock de nadie — solo queda como `usuario_id` del movimiento, el campo de trazabilidad de "quién lo registró". El bug fue puramente del cliente nuevo: se armó `RebajaPendienteSection` sin ese campo, porque conceptualmente una rebaja SAP no es de un técnico particular.

**Fix**: se manda quien está guardando (`useAuth` → `session.user.id`, ya el admin/JP logueado) como `tecnicoUserId` — sin agregar un selector de técnico a `RebajaPendienteSection`, que habría sido pedirle un dato sin sentido para este flujo. `ResumenProyectoTable.tsx`, sin migración.

## v1.64 — orden de Logística + rebaja multi-bodega + formato copiable a Excel de Entel
Andrés compartió `CONTROL DE REBAJAS C088.xlsx` (pestaña 2026) — el control de rebajas real que usa Entel — y pidió 3 cosas sobre lo construido en v1.62:

1. **Orden de secciones en Logística confirmado**: Técnicos (`EquipoSection`) → Material físico → **Rebaja pendiente** → Material digital. Antes Material digital iba ANTES que Rebaja pendiente; se invirtió el orden de render en `ResumenProyectoTable.tsx` (ambos componentes seguían intactos, solo cambió el orden en que se llaman).
2. **`sugerirRebaja()` ahora busca en TODAS las bodegas**, no solo la elegida en la barra de abajo — antes exigía elegir una bodega primero. Por material, junta el stock digital de cualquier bodega vía `getStock({ materialId })` (sin `ubicacionId`), y ordena: primero los lotes de la bodega del área (`defaultBodegaId` — C088 en ATT, C132 en OyM), de menor a mayor cantidad; agotada esa bodega, sigue con el resto también de menor a mayor. Pedido explícito de Andrés: "ordena primero los de C088 y después los de otras bodegas".
3. **`TablaDigital` ("Material digital") reformateada** para calzar columna por columna con la pestaña 2026 del Excel — orden exacto: OTT, Dirección de trabajos, Fecha de instalación, RUT empresa, Dirección empresa, SKU, Material, Lote, Cantidad. Seleccionar filas y pegarlas directo en el Excel de Entel cae en su lugar sin reacomodar nada.
   - RUT empresa / Dirección empresa son constantes (`76.512.898-6` / `Primero de Mayo 3425`, iguales en las ~2200 filas del Excel de referencia — el RUT/dirección de Sinterk como contratista, no del proyecto).
   - OTT/Dirección/Fecha de instalación son del proyecto completo (mismo valor en cada fila) — nuevos props opcionales en `LogisticaTab`/`ResumenProyectoTable` (`ott`, `direccion`, `fechaInicio`), pasados desde `Editor.tsx` de ATT únicamente (`record.ott`, `record.direccion`, `fechaInicioDe(record)`). Preventivos/Incidencias no los mandan — esas 3 columnas quedan en blanco ahí, no rompe nada.
   - "Fecha de instalación" usa la Fecha de inicio de la OTT (confirmado con Andrés) — no hay una fecha de instalación real por fila (la rebaja suma instalaciones de varios días/puntos).
   - La tabla ahora filtra a `cantRebajada > 0` (antes mostraba toda fila del proyecto, incluso con 0 rebajado) — es un log de rebajas confirmadas, no el inventario completo.

De paso: nueva columna **Descripción** en la tabla de Material físico de la OTT (junto al SKU), pedido explícito de Andrés, sin relación con lo de arriba.

## v1.62-v1.63 — Ferretería sin lote físico + rebaja sugerida
Pedido de Andrés, con plan aprobado en la conversación (ver el mensaje "Plan:" del 11-08). Problema real: materiales como cruceta/tornillería no tienen lote físico distinguible — obligar a elegir "cualquier lote disponible" en cada Entrega/Instalado generaba descuadre.

**Parte 1 — sin lote en movimientos físicos.** Nuevo `src/lib/inventario/esFerreteria.ts` (`esTipoFerreteria`, mismo patrón que `esCable.ts`: por nombre del Tipo del Catálogo, no por id) y `LOTE_FISICO_FERRETERIA = 'Físico'`. En los 3 sitios reales donde se elige lote físico (`AsignacionesForm.tsx` Entrega/Conteo, `RegistrarMovimientoForm.tsx` Entrada, `ResumenProyectoTable.tsx` filas físicas nueva/existente), si el material es Ferretería no se muestra `LoteSelect` — se fija `'Físico'` directo. El valor real guardado en `lote` pasa a ser ese string, así que fluye solo a Bodega/Movimientos/Técnico sin tocar esas pantallas. La rama `naturaleza='digital'` de `RegistrarMovimientoForm` (rebajado) queda intacta a propósito — ahí el lote real sigue importando (hoy es código muerto de todos modos: el único render real de ese componente es `soloEntrada`, ver `Home.tsx:118`).

**Parte 2 — migración `0064_ferreteria_traspaso_lote.sql`.** Decisión de Andrés: traspasar lo histórico Y que el traspaso ocurra solo hacia adelante. Trigger `after update of tipo_id on materiales` — si el tipo nuevo resuelve a Ferretería y el anterior no, llama `traspasar_lote_sindefinir_a_fisico(material_id)`, que renombra `lote='SinDefinir'` → `'Físico'` en `stock`/`movimientos`/`proyecto_materiales`. En `stock` y `proyecto_materiales` (con unique constraint sobre lote) usa upsert-con-suma para no reventar si ya existiera una fila `'Físico'` con saldo propio — un cruce ahí "se cuadra a mano con Conteo después" (palabras de Andrés), no hay lógica de merge más fina. Retroactivo para lo ya clasificado hoy vía un `do` block (no por el trigger — `tipo_id` no cambia, así que "antes no lo era" sería falso).

**Parte 3 — rediseño de "Material digital (SAP)" en `ResumenProyectoTable.tsx`.** `TablaDigital` se simplifica a SOLO mostrar/corregir lo YA rebajado (se le sacó bodega/disponible/el "+" de edición libre — ya no hace falta, ver abajo). Nueva sección `RebajaPendienteSection`: botón "↻ Sugerir rebaja" (mismo patrón que "Regenerar avance" del EP, `EstadoPagoTab.tsx:62`) calcula por material `necesario = instalado total − ya rebajado`, y reparte esa cantidad entre los lotes digitales disponibles en la bodega elegida ordenados de MENOR a MAYOR — vacía primero el lote más chico, sigue con el siguiente, hasta cubrir lo necesario (algoritmo confirmado con Andrés: "vaciar el lote más vacío y luego empezar a consumir el 2do con menos unidades"). Si ni sumando todo alcanza, la línea final queda con el faltante y sin lote, marcada en ámbar. Las líneas quedan editables (lote/bodega/cantidad) y se puede agregar una suelta a mano ("+ Agregar línea de rebaja"); nada se guarda hasta el mismo botón "Guardar cambios" que ya existía para el resto de la tabla — no hizo falta un mecanismo de borrador nuevo, ya estaba.

## v1.61 — terreno (técnico) puede agregar y editar puntos de Preventivos
Pedido de Andrés: terreno no debe poder cambiar la info del proyecto (cuadrante/zona/responsable/etc.), pero SÍ debe poder agregar puntos y editarlos por completo — antes ni siquiera veía el botón "Agregar punto".

Causa: `Editor.tsx` pasaba `soloFotos={isTecnico}` tanto a `CuadranteSection` (correcto, la info de proyecto debe quedar bloqueada) como a cada `PuntoCard` (INCORRECTO — dejaba los puntos en modo solo-fotos para técnico: nombre/descripción/dirección/hallazgo de solo lectura, sin poder reordenar ni eliminar), y además ocultaba el botón "➕ Agregar punto" entero con `{!isTecnico && (...)}`.

Fix: se dejó de pasar `soloFotos` a `PuntoCard` (queda en su default `false` para todos los roles) y se sacó el gate `!isTecnico` del botón de agregar. `CuadranteSection` no se tocó — sigue bloqueada para técnico. Sin cambios de RLS: `puntos_all` (0002) ya permitía insert/update/delete a cualquier miembro del proyecto (`is_member`), esto era puramente una restricción de la interfaz, no del servidor.

## v1.60 — nombre de cuadrante/incidencia repetido entre períodos rompía la sincronización
Reportado por Andrés: subió el ZIP de un colega y "no se guarda bien"; además, el avance de ese colega trabajando en vivo tampoco le aparecía. Pista suya, clave para encontrarlo: "como esto se hace periódicamente, es común nombres iguales".

**Causa raíz.** `projects` es una tabla compartida entre ATT, Preventivos e Incidencias. El `unique(ott, version)` de `0001_init.sql` tenía sentido para ATT (un número de OTT no debería repetirse, salvo una "versión" explícita vía "copiar proyecto"). Pero en Preventivos/Incidencias, `ott` guarda el CÓDIGO DE CUADRANTE/de la incidencia — un nombre que se reutiliza cada período por diseño — y ninguna de las dos áreas usa jamás el mecanismo de versión (`version` queda en 1 para siempre). En la práctica, la constraint actuaba como `unique(ott)` puro para esas dos áreas, bloqueando el caso normal que describió Andrés.

**Por qué explica los dos síntomas a la vez.** Crear/importar un cuadrante con un nombre ya usado en un período anterior hace que el primer `insert` en `projects` (`insertProjectIdempotente`) choque contra esa constraint (23505). El código de recuperación de ese error asumía que un 23505 SIEMPRE es el reintento por `client_local_id` (0046) y volvía a buscar por esa columna — que no encuentra nada, porque el conflicto real fue por el OTT, no por el reintento — y revienta con un error de "no rows" que no dice en ningún lado "ya existe un cuadrante con ese nombre". Pasa igual si el colega lo hace en vivo: su autoguardado repite el mismo `insert`, choca igual, y su trabajo se queda local en su máquina para siempre — nunca llega al servidor, así que Andrés nunca lo ve.

**Fix — `0063_ott_unico_solo_att.sql`**: la unicidad de `(ott, version)` queda restringida a ATT vía índice único parcial (`where area = 'ATT'`). Preventivos e Incidencias quedan libres de repetir nombres entre períodos.

**Defensa en profundidad** (`attRepo.ts`, `incidenciaRepo.ts`, `preventivoRepo.ts`): el manejo de 23505 en `insertProjectIdempotente` ya no asume ciegamente que es el reintento por `client_local_id` — si la búsqueda por esa columna no encuentra nada, tira un error honesto ("ya existe un proyecto con el OTT/código …") en vez del error de "no rows" sin contexto. Relevante sobre todo para ATT, donde la constraint de nombre único SÍ sigue aplicando.

**Recuperación**: después de correr la 0063, NO hace falta re-importar el ZIP del colega — su levantamiento debería seguir en el store local (de quien lo haya intentado subir) marcado "sin sincronizar"; con reintentar desde el botón de guardado del Editor (o esperar el próximo autoguardado) alcanza, porque el `record.id` local nunca cambió y el mismo insert que antes chocaba ahora debería pasar limpio.

## v1.59 — ZIP de una versión vieja del sitio: "puntos.delete: invalid input syntax for type uuid"
Reportado por Andrés: subió un ZIP de un cuadrante hecho por un colega en la versión vieja del sitio (otro PC) y obtuvo ese error al guardar — pero el levantamiento "sí se subió" (a medias).

**Causa exacta.** `puntos.id` es `uuid` en la BD y no tiene rekey posterior (a diferencia del informe/proyecto, que sí lo tiene vía `client_local_id` — ver `insertProjectIdempotente`): el cliente manda su propio id tal cual en cada upsert. `ImportZip.tsx` confiaba ciegamente en `pt.id` del JSON del ZIP y, si faltaba, generaba el fallback con `nanoid()` — un formato de id que NUNCA es válido para esa columna (los puntos se crean normalmente con `crypto.randomUUID()`, ver `store.ts` `addPunto`). El ZIP del colega, exportado desde una versión vieja, trajo justamente eso: un `pt.id` no-UUID (o ausente).

**Por qué "sí se sube pero falla".** `preventivoRepo.save()` hace, en orden: (1) upsert de `projects`, (2) upsert de `informes_preventivo`, (3) `replacePuntos()`. Los pasos 1-2 ya habían confirmado en el servidor cuando `replacePuntos` revienta en su primera línea (el DELETE con el filtro `.not('id','in', (…))`, que intenta castear TODOS los ids de la lista a uuid y falla entero si uno solo no lo es) — antes de llegar al `upsert` de los puntos. Resultado: existe un `informes_preventivo` en el servidor con el cuadrante completo pero CERO puntos.

**Por qué reintentar (autoguardado) no lo arregla solo.** Como `save()` lanza la excepción, `persistToServer()` nunca llega a `rekey()` — el registro local se queda con el mismo id de siempre. Cada autoguardado repite exactamente los mismos tres pasos, encuentra el mismo `project` vía `client_local_id` (por eso no duplica), y vuelve a reventar en el mismo punto. Sin el fix, esto no se arregla solo ni reintentando.

**Fix — dos capas:**
1. `ImportZip.tsx`: solo reusa `pt.id` si `isUuid(pt.id)`; si no, genera `crypto.randomUUID()` — el mismo generador que usa la creación normal de puntos.
2. `replacePuntos()` en `preventivoRepo.ts`: valida los ids ANTES del delete y, si hay uno inválido, lanza un error legible con el nombre del punto — defensa en profundidad, para que si esto vuelve a pasar por otra vía el error diga qué punto es, no un casteo críptico de Postgres.

**Recuperar el levantamiento ya importado**: el fix solo previene el problema en NUEVOS imports — el registro que Andrés ya importó sigue con el id malo guardado localmente (persist de Zustand). Camino más simple: borrar ese levantamiento del store local (nadie más lo tiene, y en el servidor solo hay un informe vacío sin puntos que perder) y volver a importar el mismo ZIP una vez que este fix esté desplegado — `insertProjectIdempotente` va a reconocer el mismo `client_local_id` y reusar el `project`/`informe` ya creado, esta vez completando los puntos con UUIDs válidos.

## v1.58 — la corrección autocompletada parte en minúscula
Pedido de Andrés: en el informe, la corrección se concatena después de otro texto (`generarInformeEntel.ts`: `"Observación: " + [dirección, corrección].join(', ')`), así que un "Se regula…" con mayúscula quedaba leyéndose como el inicio de una frase nueva en medio de la oración. Fix en `PuntoCard.tsx` (`minusculaInicial`), aplicado en el momento del autocompletado (`handleHallazgoChange`) — no toca los 20 textos guardados en `correcciones_hallazgo` (esos se ven bien como oraciones sueltas en el editor de Administración) ni el texto ya autocompletado en puntos existentes, solo lo que se autocompleta de ahora en adelante. Cubre siglas: si la segunda letra también es mayúscula (CTO, DC) no la toca — evita "cTO".

## Nuevo — Correcciones por hallazgo editables desde Administración (11-08-2026, noche)
Pedido de Andrés: el texto de "Corrección" que se autocompleta en Preventivos al elegir un hallazgo (pendiente desde el 07-08, `CORRECCIONES_POR_HALLAZGO` vacío) — ahora editable desde Administración, por si llega feedback más adelante y hay que ajustarlo sin pedir un cambio de código. Andrés pasó el texto de 20 de los 22 hallazgos (archivo `Arreglos por hallazgo.txt`, mapeo posicional a la lista `HALLAZGOS` verificado por correlación de contenido antes de sembrarlo).

- **Tabla nueva `correcciones_hallazgo`** (`hallazgo` texto como PK, `correccion`, `updated_at`) — migración `0062_correcciones_hallazgo.sql`, sembrada con los 20 textos. Lectura para cualquier logueado (técnicos la necesitan para el autocompletado), escritura solo `is_admin()`.
- **`HALLAZGOS` se extrajo** de `PuntoCard.tsx` a `src/modules/preventivos/hallazgos.ts` — Administración necesitaba la misma lista, en el mismo orden, para no duplicarla y arriesgar que se desalineen.
- **`src/lib/correccionesRepo.ts`** — `listCorreccionesHallazgo` / `guardarCorreccionHallazgo` (upsert, porque un hallazgo puede no tener fila sembrada).
- **`Editor.tsx`** de Preventivos carga el mapa UNA VEZ (no por `PuntoCard`, son ≤22 filas) y lo pasa como prop; si falla (sin conexión) el mapa queda vacío y Corrección sigue editable a mano, como antes de este cambio.
- **`AdminScreen.tsx`** — nueva sección `CorreccionesHallazgoSection`, mismo patrón de edición en la celda que ya usaba la descripción del Catálogo (guarda al salir del campo).

## "Ignorar" en eventos de inventario + estado del historial de cerrados (11-08-2026, tarde)
Pedido de Andrés: una sexta forma de resolver un evento (además de Consumo/Devolución/Traspaso/Reasignación/Agregar) que **no mueve stock**, solo cierra el evento y deja constancia con una nota — para las diferencias que no vale la pena perseguir. A diferencia de las otras cinco, `nota` es obligatoria (es la única documentación que queda) y no tiene restricción por signo de diferencia ni por tipo de ubicación (aplica a cualquier evento).

Migración `0061_ignorar_evento.sql`: agrega `'ignorar'` al check de `eventos_inventario_resoluciones.tipo` y a `resolver_evento_parcial`, con una rama que no llama `adjust_stock` ni inserta `movimientos` — solo el `insert` genérico en `eventos_inventario_resoluciones` que ya hacían las otras cinco. Cliente: `ResolucionTipo` (types.ts), `TIPO_RESOLUCION_LABELS`, `tiposDisponibles` en `ResolverEventoForm` (Home.tsx) y `ResolucionRow`.

**Bug encontrado y corregido de paso**: el `tecnicoUserId` que arma `submit()` en `ResolverEventoForm` tenía un `else` genérico (`: modo === 'tecnico' ? tecnicoId : undefined`) que originalmente nunca se disparaba con `tecnicoId` vacío porque los únicos tipos que caían ahí (`devolucion`/`traspaso`) ya validaban que hubiera técnico elegido antes de llegar a `submit()`. "Ignorar" no tiene esa validación ni ese selector, así que sin el fix habría mandado `tecnicoUserId: ''` (string vacío, no `undefined`) al RPC — que intenta castearlo a `uuid` y revienta con un error de Postgres crudo, no el mensaje de validación esperado. Se agregó `tipo === 'ignorar'` a la rama que fuerza `undefined`.

**Pregunta de Andrés: ¿existe un registro de eventos cerrados?** Sí, parcialmente:
- Dentro de `ConteoDetail`, `EventosDelConteoSection` ya muestra TODOS los eventos de ese conteo — abiertos y resueltos juntos, con sus resoluciones (`ResolucionRow`) — así que el historial por conteo existe y no hace falta agregarlo.
- **Gap real**: los eventos de técnico (instalación forzada, sin conteo asociado) NO tienen ese mismo lugar. `TecnicoTab` solo llama `listEventosInventario({estado:'abierto'})` — en cuanto un evento de técnico queda resuelto, desaparece de la vista y no hay ninguna otra pantalla que lo muestre. `listEventosInventario` sí soporta filtrar por `estado:'resuelto'`, así que técnicamente se puede consultar — falta la pantalla. No se construyó sin que Andrés lo pidiera explícitamente: es una pantalla nueva (dónde va en la IA, si se filtra por técnico/fecha/tipo) y no una corrección de bug.

## Bug corregido (11-08-2026, tarde) — cerrar un conteo revienta con "Stock físico insuficiente… se intentó restar 0"
Reportado por Andrés al subir una planilla para actualizar el stock **digital** de C088. El mensaje no tenía sentido a primera vista — un conteo digital no debería tocar stock físico — pero es el mismo bug de fondo que 0055 corrigió en otras dos funciones: `adjust_stock` valida el SALDO RESULTANTE de las dos naturalezas, no si el delta de esa llamada es 0. `cerrar_conteo` (0010) nunca recibió el `p_permitir_negativo => true` que 0055 sí les dio a `registrar_movimiento`/`anular_movimiento` — quedó fuera a propósito ("tiene su propia semántica"), pero esa exclusión era un error para este caso.

Mecanismo exacto: si un material/lote ya tenía stock **físico** negativo (acá, por entregas de OTRO material —51024— registradas sin stock), cerrar un conteo **digital** de C088 llama `adjust_stock(…, 0, diferencia)` — delta físico 0 — pero la guarda evalúa igual `v_fisico + 0 < 0` y revienta, aunque ese cierre no tenga nada que ver con lo físico. "Se intentó restar 0" es la pista: no se restó nada, el saldo ya estaba negativo de antes.

Agravante: el `for` de `cerrar_conteo` no tiene manejo de excepción por línea — al reventar en una, se pierden TODAS las del conteo, no solo la mala. Con una planilla de decenas de líneas, una sola con negativo cruzado tumbaba el cierre completo.

**Fix — `0060_cerrar_conteo_permite_negativo.sql`**: mismo criterio de 0050/0055 (el negativo se permite, se ve en rojo en Bodega, se reconcilia por Conteo — que es justo lo que esta función hace). `p_permitir_negativo => true` en las dos llamadas a `adjust_stock`. Sin cambios de cliente.

## Estado anterior (10-08-2026)
- **v1.51 — la edición de una tanda es de todas sus líneas a la vez** (pedido de Andrés). "Editar líneas" abre todas las filas del registro y un solo "Guardar cambios" manda las que cambiaron. El caso real lo pide: cuando llega el lote de una entrega suele aplicar a varias líneas juntas.
  - El estado de edición vive en `RegistroCard` (un `Record<movimientoId, Draft>`), y `LineaRegistro` pasó a ser controlada — `draft` undefined = fila en solo lectura.
  - **Las llamadas van secuenciales, NO en `Promise.all`**: cada corrección rehace stock por dentro (revierte lo viejo, aplica lo nuevo), y dos filas del mismo material+lote en paralelo dejan el saldo intermedio a merced del orden de llegada. Además así el error queda atribuido a su fila.
  - Guardado parcial a propósito, no transaccional: las que fallan quedan en rojo con lo tipeado intacto para reintentar, las que pasaron se guardan igual. Revertir 5 correcciones buenas por 2 malas sería peor. Anular sigue siendo por fila — es destructivo y no corresponde en un guardado masivo.
- **v1.50 — los ítems de un registro se ven como `<table>`** (pedido de Andrés). Mismo patrón que Movimientos/Bodega: scroll horizontal en el contenedor, no en la página. Columnas SKU · Material · [Tipo] · Lote · Cantidad · Nota · [Acción]; Tipo solo en Asignaciones (en Entrada sería "Entrada" repetido) y Acción solo si el rol puede editar. **Efecto lateral**: al ser tabla real, copiar y pegar mantiene las columnas en Excel/Sheets — que es lo que Andrés había preguntado el 07-08 y quedó "para más adelante".
- **v1.49 + migración `0059` — lista de asignaciones/entradas registradas, con edición de lote** (pedido de Andrés). Bajo el formulario de Registro aparece lo ya guardado: fecha/técnico/código en Asignaciones, fecha/bodega/código en Entrada, con badge "N sin lote".
  - **El motivo**: no hay SAP, y el lote real de lo que entra muchas veces se sabe DESPUÉS, cuando Entel actualiza su stock. `registrar_movimiento` ya dejaba registrar sin lote (queda `'SinDefinir'`); faltaba poder completarlo sin perder la fecha real del movimiento.
  - **`0059_corregir_movimiento`**: corrige lote/cantidad/nota EN EL LUGAR, revirtiendo el efecto de stock viejo y aplicando el nuevo antes del update. **Nunca un UPDATE crudo del lote** — eso deja el stock sumado al lote anterior y descuadra la bodega en silencio. Mismo principio que `anular_movimiento` (0048) pero terminando en update, así se conservan id y fecha. Acepta solo los 4 tipos que se registran desde Registro (entrada/salida/traslado/ajuste); **`instalado` queda fuera a propósito** porque su cadena de fallback (0025) podría abrir eventos de revisión nuevos por una corrección de tipeo.
  - **Trampa encontrada**: los tres tipos de una Asignación (salida/traslado/ajuste) NO son exclusivos del formulario — resolver un evento de Conteo (0039/0042) y cerrar un conteo generan los mismos. El discriminador es `documento`: `AsignacionesForm` siempre manda uno calculado y ninguna función de conteo escribe esa columna. En Entrada no hace falta el filtro: ninguna resolución crea movimientos de tipo 'entrada', y ahí `documento` es campo libre que puede quedar vacío.
  - Las tandas no existen como tabla: se agrupan por (día, destinatario, documento) — que es justo el trío que Andrés pidió mostrar.
- **v1.48 — reordenado el árbol de pestañas de Inventario** (pedido de Andrés): `Registro` (→ Asignaciones · Entrada), `Stock` (→ Bodega · Proyecto · Técnico), `Conteo`, `Movimientos`, `Catálogo`. Movimientos subió a pestaña principal y las tres de stock bajaron a subpestañas. Registro es la pestaña por defecto. El contador que refresca Movimientos tras registrar subió a `Home`, porque Registro y Movimientos ya no comparten componente padre.
- **v1.47 — el login se autocompleta con el gestor de contraseñas** (Firefox, celular y PC). El formulario ya tenía `<form>` real, `type=email/password`, `autocomplete` y botón submit; **le faltaban `name` e `id`**. Los gestores guardan junto a la clave el identificador del campo (por `name`, con `id` de respaldo): sin ninguno de los dos el login queda guardado con campos anónimos y el autollenado al cargar falla.
  - **Las claves guardadas antes de este fix no se arreglan solas** — hay que borrarlas de `about:logins` y volver a guardarlas una vez.
  - De paso salió el diagnóstico de por qué pedía la clave en cada arranque del dev: **la IP local cambió** (192.168.1.83 → 192.168.0.10). El origen (`https://IP:puerto`) es la llave con que se guardan tanto la contraseña como la sesión de Supabase, así que una IP nueva = origen nuevo = nada guardado. En el PC conviene entrar por `https://localhost:5173`; para el celular, reservarle IP fija al PC en el router.
- **v1.46 — los totales de la lista de ATT se separan en cable y material** (pedido de Andrés). Cada tarjeta muestra `Cable <entregado>/<instalado>` y `Material <entregado>/<instalado>` (ambos ocultos en móvil, donde queda solo la fecha). Van separados porque son magnitudes distintas: el cable se mide en metros y el resto en unidades — un total único no diría nada.
  - **Criterio de "es cable"**: extraído a `src/lib/inventario/esCable.ts` (`esTipoCable`), que es la regla que YA usaba el Estado de Pago — el nombre del Tipo del Catálogo empieza con "Cable " (Cable ADSS, Cable ducto, Cable autosoportado). `epRepo.ts` pasó a usar el helper en vez de tener la comparación inline, para que no queden dos criterios que se puedan desincronizar. **Ojo**: NO se usa `tipo_tendido` como proxy — ese campo es la clave de búsqueda en `lpu_tendido_map`, se configura aparte y no todos los cables lo tienen.
  - `getTotalesMaterialPorProyecto` ahora trae el tipo por embed anidado (`materiales(material_tipos(nombre))`) y devuelve los 4 números.
- **v1.45 — la lista de ATT muestra fecha de comienzo y material** (pedido de Andrés). Cada tarjeta suma ahora: `📅 fecha de comienzo` (siempre) y `Entregado`/`Instalado` (solo desde tablet/escritorio, `hidden sm:inline` — en móvil la tarjeta queda ilegible con todo junto, y él pidió expresamente que ahí solo se vea la fecha).
  - Los totales salen de `getTotalesMaterialPorProyecto` (nuevo en `inventarioRepo.ts`): UNA consulta a `proyecto_materiales` para toda la lista visible, sumada en el cliente (PostgREST no agrupa sin vista/RPC y son pocas filas por proyecto). El efecto se dispara con la lista de ids **serializada y ordenada**, no con el array — si dependiera de `list`, que se recalcula en cada render, sería un ciclo infinito.
  - La fecha de comienzo usa el helper nuevo `src/modules/att/utils/fechaInicio.ts` (`fechaInicioDe`), compartido con la barra del Editor para que ambos lados apliquen la misma regla: la escrita a mano si existe, si no la de creación de la OTT.
- **v1.44 + migraciones `0057`/`0058` — cierre del tema "anular"**. Dos cosas distintas que Andrés destapó limpiando datos de prueba:
  - **`0057`**: anular una reasignación a preventivo no devolvía "Asignado a técnico" (borró todos los movimientos de una OTT y le quedaron 2, con −2 en Tránsito). La reasignación escribe tipo='traslado' — el MISMO tipo que un Devuelto — pero sin `project_id` y sin tocar stock: solo suma `cant_rezagada`. Se le dio rama propia, reconocida por el código `PREVENTIVO - <ott>` de 0051 o por la nota histórica. **Limitación**: los movimientos anteriores a 0051 no traen ese código y el movimiento no guarda proyecto, así que ahí no hay a quién descontarle — se arreglan a mano con el modo corrección (0052).
  - **`0058`**: se sacó el bloqueo "el evento ya tiene resoluciones" que había puesto en 0056. **El mensaje mandaba a deshacer las resoluciones "desde Conteo/Técnico", pero eso no existe en la app** — `ResolucionRow` solo las muestra. Era un callejón sin salida que dejó a Andrés trabado. Además el bloqueo no se justificaba: el efecto real de cada resolución quedó en el MOVIMIENTO que generó, que sigue en la tabla y se anula por separado con la reversión correcta; las filas de `eventos_inventario_resoluciones` son bookkeeping del evento y se van con él por el cascade de 0039.
  - Para no dejarlo como sorpresa silenciosa, el diálogo de confirmación de "Anular" ahora avisa, **solo cuando el movimiento tiene `requiere_revision`**, que se borrará también el evento y sus resoluciones y que lo que esas resoluciones movieron hay que anularlo por separado. `Movimiento` ganó `requiereRevision` (la columna ya venía en el `select('*')`, solo faltaba mapearla).
  - **Pendiente, a decisión de Andrés** ("veré bien cuando haga prueba de esos eventos"): automatizar la reversión de resoluciones dentro de anular. Bloqueo real para hacerlo bien: `resolver_evento_parcial` **nunca llena `movimientos.evento_resolucion_id`**, así que los movimientos que generan las resoluciones no quedan enlazados a ellas y no se pueden identificar de forma confiable. Si se decide hacerlo, empezar por llenar esa columna.
- **Migración `0056_anular_borra_su_evento.sql` — PENDIENTE de correr.** Anular un movimiento de "instalación forzada" quedaba bloqueado de más: en 0048 puse el candado "sin atado" contra cualquier `eventos_inventario` que referenciara al movimiento, pero ese evento no es una dependencia externa — lo generó ESE mismo movimiento (`registrar_movimiento` lo crea junto con el Instalado forzado, ver 0040), así que si el movimiento se anula el evento queda describiendo un descuadre que ya no existe. Ahora anular borra también ese evento (antes del delete del movimiento, porque `eventos_inventario.movimiento_id` es FK sin cascade). El bloqueo se conserva solo si el evento YA tiene resoluciones registradas (Consumo/Devolución/Traspaso/Reasignación): cada una movió stock por su cuenta y deshacerlas requiere el flujo normal de Conteo/Técnico.
- **Migraciones: TODAS corridas y confirmadas por Andrés hasta `0055_permitir_negativo_en_sumas.sql`** (0047 a 0055 incluidas). Anular movimientos ya funciona de punta a punta — probado por él borrando una fila real. No hay ninguna migración pendiente a esta fecha.
- **`main` está 12 commits atrás a propósito** (último: `4610183`, v1.33). Todo lo de v1.34 en adelante (anular movimiento, split físico/digital, "Asignado a técnico" editable, barra fija del Editor, orden de pestañas, y las migraciones 0048-0055) vive solo en `backend-supabase`, según la regla de no pushear a producción sin pedido explícito. **Cuando se haga el próximo cutover hay que correr primero las migraciones en Supabase** — ya están corridas, así que basta con el push.
- **Migración `0055_permitir_negativo_en_sumas.sql` — PENDIENTE de correr. Cierra por fin el bug de "no se puede anular"** (tres intentos: 0050, 0053 y este). Diagnóstico final, confirmado con `pg_proc` (`adjust_stock` tiene 6 parámetros y su guarda sí respeta el flag, o sea todo lo anterior estaba bien instalado): **`adjust_stock` no valida "no restes más de lo que hay", valida que el SALDO RESULTANTE no quede negativo**. Con una ubicación ya en −2, una llamada que SUMA 1 también salta (−2 + 1 = −1 < 0) — y el mensaje dice "se intentó restar 1" igual, porque usa `abs()` del delta. Por eso anular una Entrega seguía fallando después de 0053: la llamada que reventaba no era la resta al técnico (ya parchada) sino **la suma de vuelta a la bodega**, que estaba en −2. Lo mismo estaba latente en `registrar_movimiento` (una Entrada a una bodega en negativo habría fallado igual).
  - Fix: TODAS las llamadas a `adjust_stock` de `registrar_movimiento` (7) y `anular_movimiento` (6) pasan `p_permitir_negativo => true`, sumas incluidas. Desde 0050 el criterio es que el negativo se permite, se ve (rojo + "⚠ Descuadre — revisar" en Bodega) y se reconcilia por Conteo — la guarda ya no protegía nada, solo bloqueaba correcciones. No se tocaron `resolver_evento_parcial` ni `cerrar_conteo`.
  - **Lección para el futuro**: el texto del error de `adjust_stock` es engañoso ("se intentó restar N" aparece también al sumar). Si vuelve a aparecer, mirar el SALDO RESULTANTE, no el signo del delta.
- **v1.43 — barra fija de datos + Logística como primera pestaña en el Editor ATT** (pedido de Andrés).
  - **Migración `0054_fecha_inicio.sql`** — **PENDIENTE de correr**. Solo agrega `projects.fecha_inicio date`. `direccion` ya existía desde `0021_incidencias.sql` (la comparten todas las áreas), solo que ATT nunca la mapeaba; `fecha_cierre` existe desde 0001.
  - **`BarraDatosProyecto`** (al final de `Editor.tsx`): OTT · Dirección · Fecha de inicio · Fecha de término, siempre visible arriba de las pestañas y editable. Fecha de inicio vacía muestra la fecha de creación de la OTT (`createdAt`) sin escribir nada en la BD; al tipear una, esa manda y queda en `fecha_inicio`.
  - **Fecha de término**: NO va por el autoguardado — `fecha_cierre` sigue excluido de `recordToProjectRow` a propósito (un guardado normal nunca debe poder tocar el cierre de un proyecto). Se escribe con una acción propia, `setFechaCierre` (store) → `attRepo.setFechaCierre`. Y cerrar desde el badge de estado **ya no pisa** una fecha existente: `closeProject` lee primero y solo rellena si está vacía, y el mismo criterio se aplicó al estado local de los 3 stores (att/preventivos/incidencias), que también la sobrescribían.
  - **Orden de pestañas**: Logística → Info de proyecto → Estado de Pago. Default = Logística, salvo en un informe recién creado (todavía sin uuid), donde esa pestaña solo sabe decir "guarda el informe primero" — ahí abre en Info de proyecto.
  - La OTT queda editable en dos lugares (la barra y SeccionDatos); escriben el mismo campo del store, así que se mantienen en sincronía. Se dejó así a propósito para no sacar nada de la sección de Info sin pedirlo.
  - Compila limpio (`tsc --noEmit`); falta que Andrés lo pruebe.
- **v1.42 — fix de "anular" contra stock negativo + nota por fila en la tabla de la OTT**.
  - **Migración `0053_anular_permite_negativo.sql`** — **PENDIENTE de correr**. Andrés encontró, limpiando movimientos de una OTT de prueba: `movimientos.anular: Stock físico insuficiente (hay -2, se intentó restar 1)`. Causa: `anular_movimiento` (0048) se escribió ANTES de `0050_salidas_permiten_negativo.sql`, así que sus restas de stock iban sin `p_permitir_negativo` — y anular fallaba justo cuando la ubicación ya estaba en negativo, que es el caso en que uno quiere anular. Las 5 restas ahora pasan `true` (cuerpo idéntico a 0048 en todo lo demás). Anular es una corrección: tiene que poder correr siempre.
  - **Nota por fila** en la tabla de material de la OTT (`ResumenProyectoTable.tsx`): columna Nota al final, tanto en filas existentes (`rowNota`, por rowKey) como en filas nuevas (`NuevaFila.nota`). Se copia a cada movimiento que registre esa fila al guardar. Antes la nota solo se podía escribir desde "Registrar movimiento" en Inventario, aunque la columna Nota sí se mostraba en Movimientos. Se limpia sola al guardar con éxito (si quedara pegada, el próximo movimiento de esa fila saldría con una nota vieja); las filas que fallan conservan la suya para el reintento.
  - Compila limpio (`tsc --noEmit`); falta que Andrés lo pruebe.
- **v1.41 — "Asignado a técnico" pasa de botón a celda editable + "Sobrante (código)" en Movimientos** (pedido de Andrés).
  - **Migración `0052_corregir_rezagada.sql`** — **PENDIENTE de correr**. Agrega `cant_rezagada` a la lista blanca de `corregir_proyecto_material` (cuerpo idéntico al de `0022_merma.sql` salvo eso). Sin esto, un "Asignado a técnico" mal puesto solo se podía arreglar por SQL a mano — justo lo que le pasó a Andrés con el Tránsito en −1.
  - **Se eliminó el botón "→ preventivo"** de la columna Tránsito (y todo su formulario inline: `reassignKey`/`reassignTecnico`/`startReassign`/`confirmReassign`). Ahora "Asignado a técnico" es una celda más de la tabla física, con la misma mecánica aditiva del resto: el técnico sale del selector de la propia fila y al guardar se llama `reasignarTransitoAPreventivo` en vez de `registrar_movimiento` (`CAMPO_REASIGNACION` en `ResumenProyectoTable.tsx`). También quedó corregible en el modo "🔧 Corregir errores de tipeo".
  - **Guarda nueva**: no deja asignar al técnico más de lo que hay en tránsito — era exactamente cómo se generaba el −1 (el botón viejo no validaba nada y se apretaba por error creyendo que borraba la fila).
  - Una fila NUEVA no ofrece esa celda (no hay nada entregado que reasignar todavía).
  - **Movimientos**: la columna Proyecto ya no muestra "—" para estas reasignaciones. Como el movimiento va sin `project_id` a propósito pero lleva `documento = 'PREVENTIVO - <código>'` (ver 0051), ahora se muestra como **"Sobrante (código)"** — helper `etiquetaProyecto` en `Home.tsx`, usado también por el orden y el filtro de esa columna para que todo concuerde.
  - Compila limpio (`tsc --noEmit`); falta que Andrés lo pruebe.
- **v1.39/v1.40 — material del proyecto en dos tablas (física y digital) + trazabilidad de la reasignación a preventivo** (pedido de Andrés).
  - **Migración `0051_preventivo_documento.sql`** — **PENDIENTE de correr**. El movimiento que deja el botón "→ preventivo" sigue con `project_id = null` (a propósito), pero ahora lleva `documento = 'PREVENTIVO - <projects.ott>'`, así se puede rastrear de qué OTT salió. Antes aparecía como "Sin proyecto" y sin ninguna referencia.
  - **Columna "Asignado a técnico"** (`cant_rezagada`) ahora visible en la tabla del proyecto — era un término invisible de la fórmula de Tránsito y por eso Andrés vio un −1 sin ningún movimiento que lo explicara. Nombre elegido por él (más claro que "rezagado").
  - **Split físico/digital en `ResumenProyectoTable.tsx`**: la tabla física quedó con Solicitado → Entregado → Instalado → Devuelto → Merma → Asignado a técnico → Tránsito (se le sacó **Rebajado**), y la nueva `TablaDigital` (al final del mismo archivo) muestra SKU · Lote · Bodega · Cantidad · Disponible. "Cantidad" es `cant_rebajada` (la baja de SAP, lo que antes era la columna Rebajado) y "Disponible" es el `stock.cantidad_digital` de esa bodega+material+lote (solo lectura, se pide una vez por bodega distinta en uso, no una por fila).
  - Las dos tablas comparten filas (`proyecto_materiales`) y estado (`edits`/`corrections`): por eso una fila nueva cargada arriba aparece sola abajo con el mismo SKU/lote, y un solo "Guardar cambios" registra lo pendiente de ambas. La bodega SÍ es independiente por tabla (`rowBodegaDigitalOverride`) — la baja de SAP no tiene por qué salir de la misma bodega que el movimiento físico.
  - Compila limpio (`tsc --noEmit`); falta que Andrés lo pruebe.
- **v1.38 — "salidas sin stock" quedó a medias en v1.26; ahora sí funciona**. Andrés reportó que no podía asignar un CMIC a alguien de oficina: "Stock físico insuficiente". Causa (error mío en v1.26): al implementar "dejar registrar salidas de bodegas sin stock" afirmé que el backend ya lo permitía citando `0008_stock_alertas.sql` — pero **`0025_prioridad_instalado.sql` volvió a poner el bloqueo dentro de `adjust_stock`**, con `p_permitir_negativo boolean default false`. Solo se había sacado el candado del CLIENTE (`LoteSelect.tsx` dejaba elegir el lote), el servidor seguía rechazando.
  - **Migración nueva `supabase/migrations/0050_salidas_permiten_negativo.sql`** — **PENDIENTE: falta que Andrés la corra**. Es `0047` idéntica salvo 5 llamadas a `adjust_stock` que ahora pasan `p_permitir_negativo => true`: bodega de origen de **Entrega** y de **Traspaso entre bodegas**, ubicación del técnico en **Devuelto** y **Merma**, y baja digital de **Rebajado**. `instalado` NO se tocó — conserva su cadena propia (stock propio → otro miembro del proyecto → forzado en negativo con `requiere_revision` + `eventos_inventario`).
  - El negativo no queda escondido: la pestaña Bodega ya lo marca en rojo ("⚠ Descuadre — revisar") y se reconcilia por Conteo.
  - Se corrigió además el comentario equivocado en `LoteSelect.tsx` que citaba `0008` como prueba de que el backend permitía negativos, dejando anotado el historial completo (0008 abre → 0025 cierra → 0050 abre) para que no se repita la confusión.
  - Compila limpio (`tsc --noEmit`).
- **v1.37 — "Técnicos asignados" de una OTT ahora acepta trabajadores de cualquier área** (pedido explícito). El filtro no era por `profiles.area` sino por ROL: solo se ofrecían `tecnico` (Terreno) y `log` (Logística), dejando fuera `jp` (Oficina) y `admin`. Ahora se ofrece cualquier trabajador activo, en `src/ui/LogisticaTab.tsx` (EquipoSection, la de la OTT) y también en `src/ui/AdminScreen.tsx` (TeamsSection, "Equipo por proyecto" — misma tabla `project_members`, se cambió igual para que ambas pantallas ofrezcan la misma gente). El rol se sigue mostrando entre paréntesis junto a cada nombre. Textos actualizados ("Elegir trabajador…" en vez de "Elegir técnico o logística…").
  - **No se tocaron** los selectores de técnico de los formularios de movimiento (`RegistrarMovimientoForm`, `AsignacionesForm`, pestaña Técnico de Inventario), que siguen filtrando a Terreno/Logística — es otra decisión (a quién se le puede atribuir stock) y no se pidió. Ojo igual: como el selector de técnico de `ResumenProyectoTable` se arma desde los miembros del proyecto, alguien de Oficina asignado a la OTT sí va a aparecer ahí y se le puede entregar material (le crea su ubicación personal como a cualquiera).
  - Compila limpio (`tsc --noEmit`).
- **v1.36 — resuelto el misterio de "0 filas afectadas" (informes fantasma en caché)**. Andrés notó que el error pasaba con TODOS los datos de prueba, no con uno solo — esa fue la pista: el script `limpieza-datos-prueba.sql` ya los había borrado del SERVIDOR, pero seguían apareciendo en la app porque **`syncList()` solo agregaba/actualizaba lo que venía del servidor, nunca sacaba de la caché local lo que ya no está allá**. Resultado: informes fantasma para siempre en ese dispositivo, y al intentar borrarlos el DELETE no encontraba nada ("0 filas"), lo que el mensaje atribuía por error a permisos. No era ni rol ni sesión ni FK — era caché local desactualizada.
  - **Fix** (mismo cambio en los 3 stores, `att`/`preventivos`/`incidencias`): `syncList()` ahora poda de la caché los records que el servidor ya no devuelve como activos. Guardas para no perder trabajo: nunca borradores locales ni records con fotos sin subir (`hasPendingSync`), solo lo que localmente figura como `estado==='activo'` (un cerrado no tiene por qué venir en esa lista), y nada con ediciones locales sin confirmar. **Detalle importante**: la comparación con `syncedAt` trata `undefined` como "no sé" y NO como "tiene ediciones" — si no, los fantasmas viejos (caché anterior a que `syncedAt` se persistiera, justo los que hay que limpiar) nunca se habrían ido.
  - También se corrigió el mensaje de error de `removeProject` para nombrar primero la causa más probable ("el informe ya no existe en el servidor") en vez de asumir permisos.
  - Compila limpio (`tsc --noEmit`). **Para que a Andrés se le limpien los fantasmas basta con entrar a la lista de informes (dispara `syncList`) con esta versión** — no hace falta borrar nada a mano ni limpiar el navegador.
- **Segundo bug real, distinto al de v1.35 (sesión vencida)**: al investigar por qué Andrés (admin real, confirmado) seguía sin poder borrar informes, probó con uno REAL (no de prueba) y salió un error distinto y explícito: `update or delete on table "projects" violates foreign key constraint "movimientos_project_id_fkey" on table "movimientos"`. Causa real: a diferencia de casi todas las columnas que referencian `projects(id)` (informes/tramos/hitos/incidencias/ep_informes/observaciones/proyecto_materiales, todas `on delete cascade`) y de `movimientos.punto_id` (ya `on delete set null` desde 0003), **`movimientos.project_id` se agregó sin ninguna acción de borrado** — Postgres bloquea el DELETE por defecto (NO ACTION) si el proyecto tiene aunque sea un movimiento de inventario asociado, que es casi cualquier informe real con actividad de Logística. Revisando el resto del esquema encontré dos casos más con el mismo problema: `eventos_inventario_resoluciones.project_id` y `projects.copied_from_id` (autoreferencia de versionado por copia).
  - **Migración nueva `supabase/migrations/0049_projects_fk_on_delete.sql`** — **PENDIENTE: falta que Andrés la corra**. Corrige las 3 a `on delete set null` (no `cascade`: el movimiento/resolución es historial real que debe sobrevivir al proyecto borrado, queda "Sin proyecto" — mismo estado que ya soporta toda la UI para salida preventiva; `copied_from_id` en `cascade` habría sido catastrófico, borraría todas las copias/versiones nuevas al borrar la original).
  - Sin cambios de código cliente — el mismo botón "Eliminar" ya va a funcionar solo con la migración corrida.
  - **El caso original (OTT `TEST-1660705120`, error "0 filas afectadas" sin error de FK) sigue sin explicación — es un problema distinto y separado**, confirmado por Andrés mismo ("parece ser otro problema paralelo"). Sin FK de por medio, apunta más a RLS devolviendo 0 filas por alguna razón puntual de esa fila, o a que el id ya no coincide con una fila real (posible resto del bug de duplicados arreglado antes en esta sesión). Pendiente: conseguir el UUID de ese informe específico (desde la URL del Editor) para investigar más.
- **v1.35 — bug real: "no tengo rol admin" al borrar un informe, con rol admin**. Andrés reportó que al intentar borrar un informe le salía "requiere rol admin" aunque su cuenta sí tiene ese rol. Causa: `src/lib/projectLifecycle.ts` interpreta "0 filas afectadas" en el DELETE como "no tienes el rol" — pero 0 filas también puede pasar si la sesión quedó con un token vencido (mismo problema de JWT expirado ya visto antes en esta sesión: `autoRefreshToken` no llega a correr si la pestaña estuvo en segundo plano un rato largo) — la RLS rechaza la escritura en silencio y el mensaje culpaba al rol sin serlo. Fix: `removeProject`/`closeProject`/`reopenProject` ahora llaman `supabase.auth.getSession()` justo antes de la escritura (en supabase-js v2 esto refresca el token solo si ya venció, usando el refresh token guardado) para evitar la carrera; y el mensaje de error, si aun así pasa, ya no afirma "te falta el rol" como hecho — menciona ambas causas posibles y sugiere cerrar sesión y volver a entrar. Aplica a los 3 módulos (ATT/Preventivos/Incidencias — todos comparten este helper).
  - Compila limpio (`tsc --noEmit`); no verificado visualmente desde esta sesión (mismo límite de sandbox de siempre) — falta que Andrés confirme que ya puede borrar sin tener que cerrar sesión a mano primero.
- **v1.34 — bug real (OTT 72603674035, bodega quedó en negativo) + función nueva "Anular movimiento"**. Contexto de la pregunta de Andrés ("por qué corrección y no borrar"): cada movimiento ya aplicó su efecto en `stock` vía `adjust_stock` AL INSERTARSE — esto ya estaba documentado en el propio código, `ResumenProyectoTable.tsx` líneas 1-15: un DELETE crudo dejaría el stock desincronizado para siempre sin ningún registro de qué pasó, y la función de BD ni siquiera acepta cantidades negativas para "restar a mano" — por diseño, solo se puede corregir un exceso registrando el movimiento contrario (ej. un Devuelto deshace una Entrega). Ya existían dos mecanismos: el "+" normal (movimiento real, toca stock) y "🔧 Corregir errores de tipeo" (sobreescribe el número de `proyecto_materiales` sin tocar stock, solo para typos).
  - **Causa real del negativo**: en `ResumenProyectoTable.tsx` (tabla de material del proyecto, pestaña Logística), la columna "Bodega" de una fila YA EXISTENTE estaba hardcodeada a mostrar "—" — no era editable ni visible. Cada "+" tecleado en una fila existente usaba ciegamente la bodega del selector compartido de la barra de abajo (por defecto la del área, ej. C088 para ATT), sin importar si el material realmente vivía ahí. Si no, la fila registraba una salida de una bodega donde el material nunca estuvo → negativo falso. (El mismo problema para filas NUEVAS ya estaba resuelto desde antes — `handleMaterialSeleccionado` salta sola a la bodega correcta.)
  - **Fix**: la columna Bodega de una fila existente ahora es un `<select>` editable (mismo patrón que Lote/Técnico por fila, con override — `rowBodegaOverride`/`getRowBodega`/`setRowBodega`), en vez de usar siempre la bodega compartida. Afecta Entregado/Devuelto/Rebajado (los campos que necesitan bodega).
  - **Función nueva `anular_movimiento(p_movimiento_id)`** (`supabase/migrations/0048_anular_movimiento.sql`, **PENDIENTE: falta que Andrés la corra** en el SQL Editor): revierte el efecto exacto de un movimiento (mismo mapeo tipo→stock que `registrar_movimiento`, deltas invertidos, técnico re-derivado vía `ensure_ubicacion_tecnico(usuario_id)` para entrega/devuelto) y recién ahí borra la fila — nunca un DELETE a secas. Se bloquea ("sin atado", tal como lo pidió Andrés) si el movimiento generó un evento de inventario (instalación forzada) o si es la resolución de un evento de conteo — en esos casos hay que resolverlo por el flujo normal. Cubre los 9 tipos posibles de `movimientos.tipo`. Botón "🗑 Anular" nuevo en la tabla Movimientos (Entradas/Salidas → Movimientos), visible solo admin/jp/log, con confirmación antes de ejecutar.
  - Esto le da a Andrés la herramienta para arreglar el descuadre de la OTT 72603674035: anular el movimiento mal registrado (bodega equivocada) desde la tabla Movimientos, y volver a registrarlo con la bodega correcta ahora que la columna es editable.
  - Compila limpio (`tsc --noEmit`); no verificado visualmente desde esta sesión (mismo límite de sandbox de siempre) — falta correr la migración y que Andrés pruebe anular el movimiento problemático.
- **v1.33**: Andrés pidió confirmar que los Conteos también quedan en la tabla "Movimientos" (Entradas/Salidas → Movimientos) y corregir el formato del código automático. Revisado: `listMovimientos`/`MovimientosTab` (a diferencia de `getTecnicoLedger`, el bug de v1.32) **no filtra por tipo** — trae todo lo que haya, así que los Conteos ya aparecían ahí sin necesitar cambio, con la etiqueta "Ajuste (Conteo)" que ya se había agregado a `TIPO_LABELS_MOV`. Lo único que cambió: formato del código automático en `documentoAuto()` (`AsignacionesForm.tsx`) para Entrega — de `"Preventivo {fecha}"` a **`"preventivos ({fecha})"`** (minúscula, plural, fecha entre paréntesis), tal como lo pidió Andrés. Devolución/Conteo quedaron con el mismo estilo de paréntesis pero conservan su propia palabra ("Devolución (fecha)", "Conteo (fecha)") — no se pidió cambiarlas, solo se ajustó el formato de fecha para que sea consistente.
  - Compila limpio (`tsc --noEmit`); no verificado visualmente desde esta sesión (mismo límite de sandbox de siempre).
- **v1.32 — bug real, confirmado por Andrés (probó Conteo de mufas — el material quedó "sin asignar" a ojos del usuario)**: la migración `0047` sí quedó corrida (si no, el RPC habría fallado con "Tipo de movimiento no reconocido" y el formulario lo habría mostrado en rojo) y `adjust_stock` sí créditó bien el stock físico del técnico — el bug estaba en la LECTURA, no en la escritura. `getTecnicoLedger()` (`src/lib/inventario/inventarioRepo.ts`), usada tanto por la pestaña "Técnico" de Inventario como por "Mi inventario" del propio técnico (`InventarioTecnicoScreen.tsx`), filtraba `movimientos` con `.in('tipo', ['salida','instalado','traslado','rebaja','merma'])` — sin `'ajuste'`, que es el tipo que escribe un Conteo. El movimiento existía y el stock estaba bien, pero desaparecía de ambas pantallas. Arreglado: se agregó `'ajuste'` al filtro y se acumula junto con `'salida'` hacia `cantEntregada` (un Conteo es, para el técnico, exactamente eso: material que quedó a su nombre). Único lugar en el código con este filtro — no hay otro sitio que necesitara el mismo arreglo.
  - Compila limpio (`tsc --noEmit`); no verificado visualmente desde esta sesión (mismo límite de sandbox) — falta que Andrés confirme que el conteo de mufas ya aparece en "Técnico"/"Mi inventario" sin tener que registrar nada de nuevo (el movimiento ya existe, esto solo corrige cómo se lee).
- **v1.31**: Andrés probó "Asignaciones" (v1.30) y reportó que Lote no salía como desplegable, a diferencia de las demás ventanas. Causa: en `AsignacionesForm.tsx` el orden de campos por línea era Material→Cantidad→Lote→Bodega (Bodega al final) — `LoteSelect` solo se muestra como `<select>` cuando ya conoce `materialId` Y `ubicacionId` (bodega); si la bodega todavía no se eligió, cae a un campo de texto libre (comportamiento normal del componente, ver `LoteSelect.tsx`). Se reordenó a **Material → Bodega → Lote → Cantidad** (el orden que ya pedía el mensaje original, que se me había pasado) — ahora Bodega se elige antes de llegar a Lote, así que el desplegable con los lotes disponibles aparece de entrada, igual que en Logística/Registrar Movimiento.
- **v1.30 — Rediseño de Entradas/Salidas → Registro → "Asignaciones"** (antes "Salida"), pedido explícito de Andrés. Migración nueva **`supabase/migrations/0047_conteo_asignacion.sql`** — **PENDIENTE: falta que Andrés la corra** en el SQL Editor de Supabase (mismo procedimiento de siempre) antes de que el tipo "Conteo"/la Devolución con sobrante funcionen contra la BD real; sin correrla, `registrar_movimiento` sigue sin reconocer `tipoUI='conteo'` y esas llamadas fallarían con "Tipo de movimiento no reconocido: conteo".
  - **Tipo nuevo `'conteo'`** en `registrar_movimiento` (rama agregada, resto de la función sin cambios de comportamiento): crédito unilateral al técnico (mismo `adjust_stock(+cant, 0)` que la rama `'agregar'` de `resolver_evento_parcial` en `0042_agregar_stock_tecnico.sql`, pero llamable directo sin necesitar un `eventos_inventario` previo), sin restar de ninguna bodega. Escribe `movimientos.tipo='ajuste'` (ya permitido por el CHECK desde `0037`). `MovimientoTipoUI` ganó `'conteo'` en `src/lib/inventario/types.ts`.
  - **Nuevo componente** `src/modules/inventario/components/AsignacionesForm.tsx` — reemplaza el lado "Salida" del formulario compartido `RegistrarMovimientoForm` SOLO en Inventario → Entradas/Salidas → Registro (las pestañas Logística de ATT/Preventivos, que siguen necesitando Solicitud/Instalado/Rebajado/Merma atados a un proyecto, no se tocaron — `RegistrarMovimientoForm.tsx` ganó un prop `soloEntrada` nuevo, no-invasivo, para que el lado "Entrada" del Registro lo siga usando sin su propio toggle interno).
  - Tres tipos, **sin selector de Proyecto/OTT** (pedido explícito: "ya no se deben asignar materiales a proyectos específicos desde este lado" — `project_id` siempre queda `null` en estos movimientos, algo que `entrega`/`devuelto` ya soportaban desde `0007_salida_preventiva.sql`):
    - **Entrega**: Técnico + líneas de Material/Bodega de origen/Lote/Cantidad. Resta bodega, suma técnico.
    - **Conteo**: mismos campos que Entrega (confirmado por Andrés — el selector de Bodega queda igual aunque no se use para restar stock, solo trazabilidad), pero NO resta de la bodega.
    - **Devolución**: Técnico + Bodega destino, y una tabla que se carga sola con el **stock físico real del técnico** (`getStock({ubicacionId: ubicación del técnico})` — no el ledger por proyecto de la pestaña Técnico, que excluye ajustes; decisión explícita de Andrés porque encaja con "ya no hay proyecto") — columnas SKU/Descripción/Lote/Asignado/A devolver. Si "a devolver" > lo asignado, se registra primero un `Conteo` por la diferencia y después la `Devolución` completa, para que el técnico quede en 0 (no en negativo) — orquestado en el cliente con dos llamadas RPC secuenciales, sin función de BD nueva para esto.
  - **Etiqueta automática** (reemplaza al selector de Proyecto/OTT que ya no está — Andrés: "donde anteriormente se podía indicar número de OTT... ahora se debe ingresar ese código automáticamente"): el campo `documento` de cada movimiento se autocompleta con `"Preventivo {fecha}"` (Entrega), `"Devolución {fecha}"` o `"Conteo {fecha}"`, fecha en formato dd-mm-aaaa. No es editable por el usuario — si prefieren otro texto/formato es un cambio chico de ajustar en `documentoAuto()`.
  - `TIPO_LABELS_MOV` (tabla de historial "Movimientos") ganó `ajuste: 'Ajuste (Conteo)'` para que estos nuevos registros se vean bien ahí.
  - Compila limpio (`tsc --noEmit`); no verificado visualmente desde esta sesión (mismo límite de sandbox de siempre) — falta que Andrés lo pruebe en su 5173, ideal probando el caso de devolución con cantidad mayor a la asignada para confirmar que deja al técnico en 0.
- **v1.29 — la OTT es el título de la pestaña del navegador en el Editor ATT** (pedido explícito: "es el principal identificador"). Nuevo hook genérico `src/ui/useDocumentTitle.ts` (pone `document.title`, vuelve al default `SinterkProyectos` al desmontar) usado en `src/modules/att/components/Editor.tsx` con `record?.ott` — se actualiza en vivo mientras se tipea, útil para distinguir varias pestañas del navegador abiertas a la vez. Alcance acotado a ATT/OTT, tal como se pidió — no se tocó Preventivos/Incidencias (podrían querer lo mismo con su propio identificador después, pero no se pidió).
  - Compila limpio (`tsc --noEmit`); no verificado visualmente desde esta sesión (mismo límite de sandbox de siempre).
- **v1.28 — Catálogo de materiales: se pueden agregar materiales nuevos desde el sitio** (pedido explícito de Andrés). Antes los materiales solo llegaban por import SAP; ahora hay un botón "➕ Agregar material" arriba de la tabla en la pestaña Catálogo (`src/modules/inventario/components/Home.tsx`, componente `NuevoMaterialForm`) que pide la misma información que ya se muestra/edita en esa tabla: SKU y Descripción (obligatorios), Nombre alternativo, Tipo (con "+ Nuevo tipo…" inline, mismo patrón que la fila existente) y Proveedores (mismo `ProveedoresSelect` reutilizado, seleccionando en memoria hasta guardar). Mínimo y `controla_lote_fisico`/`unidad`/`categoria` (columnas de `materiales` que existen en la BD pero no se muestran en esta pantalla) se dejaron fuera a propósito — no se piden porque no se muestran/editan en ningún lado hoy.
  - Nueva función `crearMaterial()` en `src/lib/inventario/inventarioRepo.ts` — inserta en `materiales` y devuelve la fila mapeada (mismo embed que `listMateriales`); SKU duplicado da un mensaje claro en vez del error crudo de Postgres (`23505` → "Ya existe un material con el SKU...").
  - RLS: igual que el resto de ediciones del catálogo, `can_move_inventory()` (admin/jp/log) — no se agregó gating extra en el cliente, la RLS ya lo resuelve.
  - Compila limpio (`tsc --noEmit`); no verificado visualmente desde esta sesión (mismo límite de sandbox de siempre).
- **v1.27 — bug real de fondo corregido: "se corta el texto al cambiar de pestaña"**. Andrés reportó que al escribir la OTT de un ATT (ej. `72603650402`) y cambiar de pestaña, el campo quedaba truncado (ej. `726036`). Diagnóstico (confirmado por código, no solo hipótesis): el autoguardado (debounce 800ms, `useAttAutosave.ts`) puede disparar un guardado parcial si hay una pausa natural a mitad de tipeo; ese guardado parcial queda en Supabase con un `updated_at` puesto por el RELOJ DEL SERVIDOR (Postgres), mientras que la edición local completa que sigue tipeando queda con `updatedAt` puesto por el RELOJ DEL DISPOSITIVO (`touch()`, `Date.now()`). `mergeFromServer` decidía cuál versión ganaba comparando `local.updatedAt > server.updatedAt` — **comparar dos relojes distintos es frágil**: si el reloj del celular está aunque sea un poco atrasado respecto al del servidor (común en terreno), la edición completa y más reciente pierde contra el guardado parcial viejo en el próximo `syncOne`/`syncList` (se dispara, por ejemplo, al reabrir el informe tras que el navegador/PWA suspenda o recargue la pestaña en segundo plano).
  - **Fix aplicado** (pedido explícito de Andrés — opción "arreglar de fondo", no el parche rápido de flush-on-blur): se dejó de comparar timestamps de relojes distintos. Ahora cada store (`att`/`preventivos`/`incidencias`, mismo patrón duplicado en los 3) usa `syncedAt[id]` — el `updatedAt` que se sabe CON CERTEZA que ya quedó confirmado en el servidor — y `mergeFromServer(local, server, synced)` conserva lo local si `local.updatedAt !== synced` (hay una edición sin confirmar), sin importar de qué reloj viene cada timestamp. `persistToServer` ahora actualiza `syncedAt[id]` tras un guardado exitoso (solo si nada cambió localmente mientras viajaba a Supabase — si cambió, se deja "sin confirmar" a propósito para que el siguiente ciclo de autoguardado lo reintente). `syncedAt` ahora se persiste (`partialize`) en los 3 stores — antes no sobrevivía un reload, lo que habría hecho que toda edición ya sincronizada pareciera "sin confirmar" para siempre tras recargar.
  - Archivos: `src/modules/att/store.ts`, `src/modules/preventivos/store.ts`, `src/modules/incidencias/store.ts` (misma función `mergeFromServer` y mismo cambio en `persistToServer`/`partialize`, duplicado en los 3 — convención ya existente en este proyecto de no compartir repo/store entre módulos).
  - **Pendiente, mencionado pero no implementado** (Andrés eligió arreglar la causa raíz en vez de esto): un flush inmediato del autoguardado al perder foco de la pestaña (`visibilitychange`) reduciría además la ventana en la que puede quedar algo a medio guardar. No pedido, no hecho.
  - Compila limpio (`tsc --noEmit`); no verificado visualmente desde esta sesión (mismo límite de sandbox de siempre) — falta que Andrés lo prueba en su 5173, idealmente forzando la condición (escribir, pausar >800ms, seguir escribiendo, cambiar de pestaña) para confirmar que ya no se trunca.
- **`scripts/limpieza-datos-prueba.sql` — bug real encontrado al correrlo**: Andrés lo corrió y salió `ERROR 23503: update or delete on table "movimientos" violates foreign key constraint "eventos_inventario_movimiento_id_fkey"`. Causa: la migración `0040_reasignacion_eventos_tecnico.sql` (posterior a cuando se escribió este script) agregó `eventos_inventario.movimiento_id` (FK hacia `movimientos.id`, sin `on delete`), y el script no la contemplaba — solo rompía el ciclo `movimientos.evento_resolucion_id` ↔ `eventos_inventario_resoluciones`, no esta otra. **Corregido**: nueva tabla temporal `_t_movimientos` (mismo criterio que el delete del paso 6) calculada antes, y un paso "2-bis" que suelta `eventos_inventario.movimiento_id` para cualquier evento que apunte a uno de esos movimientos — sin importar si el evento en sí calificaba como "de prueba". El paso 6 ahora borra directo por `id in (select id from _t_movimientos)` en vez de repetir el filtro. **Todavía no vuelto a correr/confirmado por Andrés** — falta que lo intente de nuevo con la versión corregida.
- **v1.26** (04-08-2026, en `backend-supabase`, no pusheada a `main` sin pedido explícito):
  1. **Backlog documentado — admin gestione contraseñas y cree usuarios desde el sitio** (pedido explícito de Andrés: "deja registro para a futuro"). Hoy esto se hace a mano fuera de la app: crear usuarios vía `scripts/importar-usuarios.mjs` (usa `supabase.auth.admin.createUser`, requiere `SUPABASE_SERVICE_ROLE_KEY` pasada como variable de entorno, corrido a mano por Andrés — nunca en `.env`/bundle del cliente); resetear contraseña de otro usuario, a mano en el dashboard de Supabase (Authentication → Users → Send password recovery / Reset password). No implementado en la app porque requiere la `service_role key`, que no puede vivir en el bundle del cliente (mismo motivo por el que "Equipo por proyecto" en Admin no crea logins, ver más abajo). **Camino técnico correcto quedaría**: una Supabase Edge Function (server-side, con la `service_role key` como secreto de la función, nunca del cliente) que exponga acciones tipo `crearUsuario`/`enviarResetPassword`/`actualizarUsuario`, invocada desde `AdminScreen.tsx` vía `supabase.functions.invoke(...)` con la sesión del admin (la función valida el rol server-side antes de actuar). No estimado ni empezado — queda como backlog.
  2. **Inventario: salidas de bodega ya no se bloquean por falta de stock en el sistema** (pedido explícito, "período transitivo" mientras se termina de cargar el stock real al sitio). El backend ya permitía stock negativo desde `0008_stock_alertas.sql` (sin CHECK, `adjust_stock` no lanza excepción) — el único bloqueo real estaba en el cliente: `src/ui/LoteSelect.tsx` deshabilitaba la opción de un lote en el `<select>` si su cantidad disponible no era `> 0` (usado para Entrega/Instalado/Rebajado vía `checkAvailability`). Se sacó el `disabled`; ahora esos lotes se pueden elegir igual, solo se marcan con "⚠ sin stock" en la etiqueta de la opción. No se tocó nada en el backend (ya soportaba esto) ni la vista de Bodega (ya marcaba cantidades negativas en rojo, sin cambios).
  - Compila limpio (`tsc --noEmit`); no verificado visualmente desde esta sesión (mismo límite de sandbox) — falta que Andrés lo pruebe.
  - **Pendiente, explícitamente pedido por Andrés, NO ejecutado por esta sesión**: correr `scripts/limpieza-datos-prueba.sql` contra Supabase para borrar los datos de prueba en producción. **No lo corrió el agente** — es un borrado permanente de filas en la base de datos real, y en este proyecto (como todas las migraciones) ese paso siempre lo corre Andrés a mano en el SQL Editor de Supabase, nunca un script/agente automatizado. **Aviso de Andrés a mitad de esta tarea**: hoy (04-08-2026) se cargaron datos reales en ATT que NO se deben borrar — el filtro de "proyectos de prueba" es por patrón de OTT (`_verify_*`/`test-*`), no por fecha, así que en teoría no los toca, pero por seguridad se cambió el bloque "0. Verificación previa" para que liste las filas completas que coincidirían con el borrado (ott/nombre/área/fecha) en vez de solo un conteo — así se puede confirmar a ojo antes de seguir. Instrucciones: abrir el SQL Editor, correr primero solo el bloque "0. Verificación previa" y revisar que CADA fila listada sea realmente de prueba (si aparece algo real de hoy, no seguir, avisar para ajustar el filtro), y recién después pegar el archivo completo (corre dentro de un `begin`/`commit`, si algo falla se deshace solo). El bloque final (comentado, líneas ~176-195) para desvincular a `iperez@sinterk.cl` del historial va aparte — revisar antes de descomentar.
- **Regla nueva, explícita de Andrés**: de ahora en adelante, **solo pushear a `main` (producción) cuando él lo pida textualmente en ese mismo turno** — no asumir por continuidad de sesiones anteriores. Commits/push a `backend-supabase` siguen siendo automáticos (para no perder trabajo entre PCs), pero `main` no se toca salvo pedido explícito. **A partir de v1.25, `main` quedó desactualizado a propósito** — antes de un próximo cutover hay que revisar qué le falta y pushear con `git push origin backend-supabase:main` recién cuando Andrés lo pida.
- **v1.25** (04-08-2026): reordenamiento del formulario de "Puntos" en Preventivos (`src/modules/preventivos/components/PuntoCard.tsx`), pedido explícito de Andrés. Orden nuevo dentro de la tarjeta expandida: Fotografías → Descripción → Dirección → Tipo de hallazgo → Corrección → checkbox "Punto cerrado" (solo visible si hay hallazgo) → Material → Comentarios.
  - **Corrección se autocompleta** al elegir el hallazgo, vía una tabla `CORRECCIONES_POR_HALLAZGO` en `PuntoCard.tsx` (mismo patrón que `RESPONSABLES_POR_ZONA` en `CuadranteSection.tsx`) — sigue editable después. **PENDIENTE: la tabla está vacía** (autocompleta `''`), Andrés dijo que iba a pasar el texto de corrección real para cada uno de los ~22 hallazgos — falta llenarla cuando la mande.
  - **Criterio abierto/cerrado, ahora definido por Andrés** (no 100% manual como se había dejado en v1.24 — se afinó): un punto está **cerrado** si no tiene hallazgo (nada que corregir) O si se marca manualmente el checkbox "Punto cerrado"; está **abierto** si tiene un hallazgo real y el checkbox no está marcado. Implementado en `src/modules/preventivos/utils/puntoEstado.ts` (`isPuntoCerrado`), usado por el badge de `PuntoCard.tsx` y el resumen de `Editor.tsx`. El checkbox ("Ticket de punto" en el pedido original de Andrés, aclarado después como el mismo checkbox "Punto cerrado") solo aparece si `hallazgo !== ''`. Elegir un hallazgo nuevo resetea `resuelto` a `false` (fuerza a cerrar de nuevo manualmente en vez de heredar el estado del hallazgo anterior).
  - Compila limpio (`tsc --noEmit`); no verificado visualmente desde esta sesión (mismo límite de sandbox de siempre) — falta que Andrés lo pruebe en su 5173.
- **v1.23/v1.24** (04-08-2026), en `backend-supabase` (y también en `main`, pusheadas antes de la regla nueva de arriba):
  1. **Aviso global de "sin sincronizar"**: `src/lib/useGlobalPendingSync.ts` (nuevo) combina `hasPendingSync` de los 3 módulos con estado local (att/preventivos/incidencias — cada uno ya lo exporta desde su `store.ts`) y expone `{ total, porArea }`. Se muestra en el header (`src/ui/Layout.tsx`, junto al `UserMenu`) como botón ⚠️ con el conteo total, tooltip con el desglose por área, y al hacer clic navega a la primera área con pendientes. Antes cada módulo solo avisaba dentro de su propio `Home.tsx` — esto se ve en cualquier pantalla. **Nota**: el EP (`ep_informes`) no tiene concepto de "pendiente de sync" (escribe directo a Supabase sin cola offline), así que no está incluido — solo att/preventivos/incidencias, que sí usan el patrón nanoid-antes-de-sync.
  2. **Preventivos: estado abierto/cerrado visible por punto**: `Punto` ya tenía `resuelto: boolean` pero no había ninguna señal visual fuera del checkbox (enterrado dentro de la tarjeta expandida). Se agregó una etiqueta 🔓 Abierto (ámbar) / 🔒 Cerrado (verde) en el header de cada `PuntoCard` (`src/modules/preventivos/components/PuntoCard.tsx`), visible aunque la tarjeta esté colapsada, más un resumen "N abierto(s) · M cerrado(s)" junto al total en `Editor.tsx`. No se agregó campo nuevo — reutiliza `resuelto`. Objetivo (pedido explícito): que una segunda brigada que llega a reparar sepa de un vistazo qué puntos ya están cerrados sin tener que abrir cada tarjeta.
  - **v1.24**: Andrés probó y reportó que no quedaba claro qué abría/cerraba un punto (el checkbox "Resuelto" se auto-marcaba al elegir un hallazgo y quedaba deshabilitado sin uno). Se sacó ese acoplamiento: elegir hallazgo ya NO toca `resuelto`; el checkbox (ahora "Punto cerrado") queda siempre habilitado y es 100% manual. `handleHallazgoChange` se eliminó de `PuntoCard.tsx`, el `<select>` de hallazgo llama `updatePunto` directo.
  - Compila limpio (`tsc --noEmit` sin errores) en ambas iteraciones; no se pudo verificar visualmente desde esta sesión (el sandbox de este entorno no alcanza `localhost:5173`, limitación de tooling ya conocida, no de la app) — Andrés probó la primera versión directo en su 5173.
- **Ya en producción.** `main` fue reemplazado por completo (`git push origin backend-supabase:main --force-with-lease`) — ya NO es la app vieja cliente-only, es esta reescritura con Supabase. Deploy automático vía `.github/workflows/deploy.yml` a GitHub Pages en cada push a `main`, confirmado exitoso (run #75).
- **Ramas**: `main` = `backend-supabase` (mismo commit, `4904358` al momento de escribir esto) — limpias, SIN el invitado técnico. `prueba` (commit `f01abed`) es una rama aparte, creada solo para tener el botón "Continuar como invitado (técnico)" disponible al probar — **nunca mergear `prueba` a `main`/`backend-supabase` sin volver a sacar ese revert**, o el invitado técnico volvería a producción.
- **Migraciones**: todas las de `supabase/migrations/` hasta `0046_projects_client_local_id.sql` están CORRIDAS y verificadas (Andrés confirmó cada una). No hay ninguna pendiente de correr a esta fecha.
- **Pendiente, explícitamente pospuesto por Andrés** ("ignoremos por ahora datos de prueba"): correr `scripts/limpieza-datos-prueba.sql` contra Supabase — sigue con datos de prueba en producción (proyectos `_verify_*`/`TEST-*`, materiales `999901-905`, etc.). No bloquea nada, es cuando él decida.
- **Pendiente, no bloqueante**: borrar la cuenta `iperez@sinterk.cl` en el dashboard de Supabase (Authentication → Users) — se dejó a propósito viva porque la rama `prueba` la sigue usando para pruebas.
- **`.env` local NO se versiona (gitignored)** — en esta máquina tiene las 4 vars (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_GUEST_EMAIL`/`PASSWORD`, `VITE_GUEST_TECNICO_EMAIL`/`PASSWORD`). **En otra máquina hay que recrear este archivo a mano** — los nombres de variable están en `src/lib/auth.ts`; los valores (contraseñas de las cuentas invitado) NO están en ningún archivo de este repo a propósito (el repo es público) — hay que traerlos aparte (Andrés los tiene). Sin `.env`, la app corre igual pero sin botones de invitado (`guestEnabled`/`guestTecnicoEnabled` quedan en `false`).
- **Para clonar en otra PC**: `git clone` (o `git fetch` + `git checkout main` si ya estaba clonado — ojo, si el `main` local es viejo hay que `git branch -f main origin/main` para que apunte a lo nuevo, exactamente el problema que se encontró y corrigió hoy en esta máquina), `npm install`, crear `.env` como arriba, `npm run dev`.

## Contexto del proyecto
- **SinterkProyectos** (nombre oficial desde v1.11 — antes "TelecomCatalog", placeholder): PWA React 18 + Vite + TS + Tailwind + Zustand + idb.
- Repo `github.com/Sinterk/SinterkProyectos` (renombrado el 30-07-2026, ver log más abajo). Deploy: push a `main` → `.github/workflows/deploy.yml` → GitHub Pages. Versión visible `vX.YY` en el header (`APP_VERSION` en `vite.config.ts`).
- Módulos (registry pattern): `att` (informes OTT/ATT), `preventivos`, `incidencias`, `inventario`. Generadores de informe: `src/modules/att/utils/generarInformeAtt.ts` (DOCX) y `generarPdfAtt.ts` (PDF con jsPDF lazy-loaded).

## Objetivo: pasar de cliente-only a backend Supabase
Tres metas: (1) data online (técnicos suben directo), (2) usuarios con roles, (3) inventario por proyecto (stock físico/digital + movimientos).

- **Proyecto Supabase**: `https://xwawplezarrfonuyaaxu.supabase.co`
- **Publishable (anon) key**: en `.env` / `.env.production` (`VITE_SUPABASE_ANON_KEY`). Es pública por diseño; la RLS es el filtro real.
- **Esquema**: `supabase/migrations/0001_init.sql` (ATT + base) y `0002_preventivos.sql` (Preventivos) — AMBOS ya ejecutados en Supabase, más los `GRANT` a anon/authenticated y `alter table projects add column jefe_proyecto`. RLS verificada funcionando.

### Roles y acceso (RLS)
`admin` (todo) · `jp` (ve/edita TODOS los proyectos, asigna técnicos) · `tecnico` (solo proyectos donde está en `project_members`) · `log` (mueve inventario). Solo el admin crea usuarios (no hay registro público). Crear proyectos = solo jp/admin.

### Modelo de datos (resumen)
- `projects` (maestro genérico por área; 1 informe por proyecto; cierre + versionado por copia) — compartido por ATT y Preventivos, cada uno con su propio hijo 1:1:
  - **ATT** (`area='ATT'`): `informes` + `tramos`/`hitos`/`fotos`.
  - **Preventivos** (`area='OyM'`): `informes_preventivo` + `puntos`. `projects.ott`/`comuna` se reutilizan como identificador visible/comuna del cuadrante (nombres de columna heredados de cuando solo existía ATT).
- Inventario: `ubicaciones` (bodega/camioneta), `materiales` (SKU único, apodo, controla_lote_fisico), `stock` (dual: cantidad_fisico + cantidad_digital, por ubicacion+material+lote), `movimientos` (naturaleza fisico/digital), `conteos`+`conteo_lineas`, `eventos_inventario`, `proyecto_materiales` (entregado/instalado/devuelto/rezagado/rebajado; tránsito calculado).

## Estado actual (rama `backend-supabase`, NO en producción)
Rama en **v0.75**, pusheada a `origin/backend-supabase` (al día). **No se hizo push a `main`** para no publicar el gate de login sin datos conectados ni usuarios (producción sigue en v0.52, sin login). Nota de versiones: `backend-supabase` (v0.75) va **muy por delante** de `main` (v0.52); se reconcilia en el cutover.

Ya hecho:
- `src/lib/supabaseClient.ts` — cliente supabase-js leyendo `VITE_SUPABASE_*`.
- `src/lib/auth.ts` — store Zustand de auth (sesión + perfil/rol, `signIn`/`signOut`).
- `src/ui/LoginScreen.tsx` — login real email/contraseña.
- `src/App.tsx` — gatea por sesión; bloquea cuentas desactivadas.
- `.env.production` versionado; build de producción pasa.
- **PASO 1 — Capa de datos** `src/modules/att/data/attRepo.ts`: CRUD `list`/`load`/`save`/`remove` que mapea `AttRecord` ↔ `projects` + `informes` + `tramos`/`hitos`/`fotos`. `save` = upsert (uuid→update; id de cliente→insert y devuelve el uuid canónico); lectura se arma sobre `emptyAttRecord`; jsonb directo para `coords`/`ingresoRed`/`infraestructura`; hijos por delete+insert con `orden`. Se añadió `storagePath?` a `FotoEntry` (`types.ts`) como seam para el paso 2 (`save` solo persiste fotos que ya tengan `storagePath`).
- **Modo invitado** (cuenta compartida, rol jp): `signInAsGuest`/`isGuest`/`guestEnabled` en `auth.ts`; botón "Continuar como invitado" en `LoginScreen`; menú de usuario en el header (`src/ui/UserMenu.tsx`) con correo, rol, mostrar contraseña (solo invitado), cambiar contraseña (usuario real: verifica la actual + `supabase.auth.updateUser`) y cerrar sesión. Requiere `VITE_GUEST_EMAIL`/`VITE_GUEST_PASSWORD` en `.env`. **OJO**: al ser `VITE_*` la contraseña queda embebida en el bundle → NO desplegar a producción con la cuenta invitado activa.
- **Segundo invitado, rol técnico** (14-07-2026, **TEMPORAL** — solo para poder probar flujos con rol técnico mientras no existen cuentas reales de técnico): mismo patrón, generalizado en `auth.ts` con `GuestKind` ('jp'|'tecnico') en vez de un solo booleano `isGuest` ciego a cuál cuenta es. Botón extra "Continuar como invitado (técnico)" en `LoginScreen` (solo visible si `guestTecnicoEnabled`); `UserMenu` muestra "Invitado técnico" y su propia contraseña (`guestTecnicoPassword`). Requiere `VITE_GUEST_TECNICO_EMAIL`/`VITE_GUEST_TECNICO_PASSWORD` en `.env`. Cuenta ya existe en Supabase (`iperez@sinterk.cl`, rol `tecnico`) — probado en el navegador de punta a punta (login, label, contraseña mostrada correctas). **Eliminar código + credenciales del `.env` + la cuenta en Supabase junto con el resto de datos de prueba antes del cutover** (ver "Próximos pasos" #15).
- **Cuentas en Supabase**: admin `abarahona@sinterk.cl` + invitado jp `abarahona.sinterk@gmail.com` + invitado técnico `iperez@sinterk.cl` (temporal, ver arriba). Login + invitados probados de punta a punta contra la RLS (list/insert/lectura anidada OK). No recrearlas.
- **PASO 2 — Fotos → Storage** `src/modules/att/data/photoStorage.ts`: `uploadPhotoObject`/`getSignedUrl(s)`/`removePhotoObjects` (bajo nivel) + `uploadRecordPhotos(record)` (sube blobs pendientes de IndexedDB al bucket `fotos`, ruta `att/{blobId}.jpg`, rellena `storagePath`). Lectura: `src/modules/att/hooks/useResolveAttPhotoUrls.ts` resuelve signed URLs en lote para fotos con `storagePath` sin `previewUrl` (espejo online de `useRestoreAttPhotos`, conviven sin pisarse). Probado extremo a extremo (upload, signed URL, fetch, remove) contra el bucket real.
- **PASO 3 — Store online-first** `src/modules/att/store.ts`: `createNew`/edición de campos siguen 100% locales e instantáneas (sin red en cada tecla); `useAttAutosave` (nuevo hook) debounce 800ms → `uploadRecordPhotos` + `attRepo.save`. Nuevas acciones del store: `syncList()`/`syncOne(id)` (traen de Supabase y fusionan en cache — solo si el servidor es más nuevo que lo local, para no pisar tipeo en curso) y `remove(id)` ahora async y **role-aware**: admin → `attRepo.remove` (DELETE real); jp/invitado → `attRepo.close` (nuevo método: `estado='cerrado'`+`fecha_cierre`, no borra). `attRepo.list()` ahora filtra `estado='activo'`. `attRepo.remove`/`close` verifican filas afectadas (`.select()` tras la mutación) y lanzan error explícito si la RLS bloqueó la operación (0 filas), en vez de fallar en silencio.
  - **Promoción de id** (borrador local nanoid → uuid del servidor tras el primer guardado): el store expone `rekey(oldId, saved)`. Cuidado detectado y corregido: justo después del rekey, el id viejo desaparece del store un instante antes de que la navegación a la URL nueva se confirme; el efecto de Editor que redirige a Home "si no hay record para este id" puede dispararse en esa ventana y ganarle la carrera al `navigate` correcto. Se resolvió con un ref `hadRecord` en `Editor.tsx`: solo redirige a Home si el record **nunca** existió para ese id (deep-link roto), no cuando ya existió y desapareció por la promoción.
  - **`syncedAt`** (nuevo, en el store): registra el último `updatedAt` que vino del servidor (por `syncOne`/`syncList`/`rekey`), para que `useAttAutosave` no confunda "el store se refrescó desde Supabase" con "el usuario editó algo". Sin esto, cada vez que se abre o recarga un informe se disparaba un guardado espurio (delete+insert de tramos/fotos) sin que nadie tocara nada.
  - Verificado en el navegador: crear→autoguardar→promoción de id→URL correcta; edición en caliente de un registro ya-uuid; subida de `fotoAerea` y de foto de galería (con inserción real en `fotos`); resolución de signed URL tras reload; `remove()` como invitado (rol jp) confirmado en la BD como **cierre** (`estado='cerrado'`, `fecha_cierre` hoy), no borrado.
- **PASO 4 — Migración de borradores locales**: se extrajo el guardado real a una acción del store, `persistToServer(id)` (sube fotos pendientes + `attRepo.save` + `rekey` si corresponde), que ahora usan tanto `useAttAutosave` como la migración — un solo camino para "persistir esto en Supabase", sin duplicar lógica. `hasPendingSync(record)` (exportado de `store.ts`) marca un informe como pendiente si su id sigue siendo nanoid (nunca llegó al servidor) o si le quedó una foto con `blobId` sin `storagePath` (subida que no llegó a completarse). En `Home.tsx`, si hay pendientes aparece un aviso ("⚠️ N informe(s) sin sincronizar" + botón "Sincronizar ahora") que los sube uno por uno vía `persistToServer`; los que fallan se listan con su error y un botón "Reintentar" individual, sin bloquear a los demás. Probado en el navegador: se creó un borrador sin tocar ningún campo (para simular uno que quedó huérfano de antes del cutover), el aviso lo detectó, y "Sincronizar ahora" lo persistió con uuid real confirmado en la BD.
- **PASO 5 — Pantalla de admin** `src/ui/AdminScreen.tsx` (ruta `/admin`, enlace "⚙️ Administración" en `UserMenu` solo si `rol==='admin'`; la propia pantalla también verifica el rol y muestra un candado si no — doble cerrojo, además de la RLS). Capa de datos en `src/lib/adminRepo.ts`. Dos secciones:
  - **Usuarios**: lista todos los `profiles` (RLS `profiles_admin`: solo admin lee/edita todos); por fila se edita nombre/rol/área/activo y se guarda con `adminRepo.updateProfile`. **La creación del login (`auth.users`) sigue siendo manual en el dashboard de Supabase** — decisión deliberada: crearlo desde la app requeriría la `service_role key`, que no puede exponerse en el bundle del cliente sin backend nuevo. El trigger `handle_new_user` ya crea el `profiles` automáticamente (rol `tecnico` por defecto) al dar de alta el login; la pantalla es donde se ajusta ese rol/área después.
  - **Equipo por proyecto**: selector de proyecto ATT activo + checklist de técnicos/logística para asignar/quitar de `project_members` (`adminRepo.addMember`/`removeMember`), que es lo que la RLS usa (`is_member`) para decidir qué ve un `tecnico`.
  - Probado por el usuario con la cuenta admin real: encontró que el admin podía quitarse su propio rol/desactivarse. **Fix**: en `UserRow`, si la fila es la del usuario logueado (`session.user.id`), los controles de rol y activo quedan deshabilitados con una nota explicativa. Es un candado de UI, no de RLS — un admin decidido podría bypasear vía API directa; si se quiere blindar del todo, falta un trigger en Postgres. **Confirmado arreglado por el usuario.**
  - Al probar "Equipo por proyecto" no había ningún perfil con rol técnico/log todavía (mensaje "No hay técnicos ni logística registrados todavía") — es el comportamiento esperado en un sistema nuevo, no un bug; queda por probar en cuanto exista al menos un usuario técnico real. Ahora el selector de proyecto incluye tanto ATT como Preventivos (`[ATT]`/`[Preventivo]` + código + comuna/nombre).
- **Filtro de estado + buscador en Home** (ATT y Preventivos): pedido tras probar admin — antes solo se veían los activos, sin forma de ver cerrados ni buscar. `attRepo.list(opts?: {estado})`/`preventivoRepo.list(opts?: {estado})` aceptan `'activo'|'cerrado'|'todos'` (default `'activo'`, sin romper a `syncList`/demás llamadores existentes). `AttRecord`/`Preventivo` ganaron `estado`/`fechaCierre` (solo lectura desde el Editor — `recordToProjectRow` los excluye explícitamente del payload de `save()`, para que un guardado normal nunca pueda reabrir/cerrar un proyecto por accidente). En cada `Home.tsx`: selector Activos/Cerrados/Todos + buscador; cerrados/todos se piden aparte del store (que solo cachea activos editables) y se re-consultan al cambiar filtro o borrar/cerrar algo. Tarjeta muestra badge "🔒 Cerrado *fecha*" cuando corresponde. **Confirmado por el usuario en el navegador** (ATT); Preventivos verificado en esta sesión (ver abajo).
- **PASO 6 — Migración de Preventivos a Supabase**: mismo patrón completo que ATT (pasos 1-4), aplicado a este módulo.
  - **Esquema nuevo** `supabase/migrations/0002_preventivos.sql`: reutiliza `projects` (`area='OyM'`) para heredar roles/RLS/estado-cierre/`project_members` ya construidos — no duplica esa infraestructura. Tablas nuevas: `informes_preventivo` (1:1 con `projects`; grupo/fecha/semana/semestre/zona/responsable/foto del plano) y `puntos` (hijos; nombre/descripción/dirección/corrección/hallazgo/resuelto + 3 fotos: levantamiento/antes/después). RLS calcada de ATT (`is_jp_or_admin()`/`is_member()`, con un `can_edit_informe_prev()` nuevo espejo de `can_edit_informe()`). Ejecutado y verificado por el usuario en el SQL Editor.
  - **Refactor previo, compartido con ATT** (para no duplicar código entre módulos): `src/lib/photoStorage.ts` (subir/firmar/borrar en el bucket `fotos`, genérico) y `src/lib/projectLifecycle.ts` (`removeProject`/`closeProject`, ambos operan sobre `projects` sin importar el área). `att/data/photoStorage.ts` y `att/data/attRepo.ts` se actualizaron para consumir estos compartidos en vez de tener su propia copia.
  - **Capa de datos** `src/modules/preventivos/data/preventivoRepo.ts` (list/load/save + remove/close reexportados del lifecycle compartido) y `data/photoStorage.ts` (`uploadRecordPhotos` recorre `cuadrante.fotoPlano` + las 3 fotos de cada punto; ruta `preventivos/{blobId}.jpg`).
  - **Store online-first** `src/modules/preventivos/store.ts`: mismo diseño que ATT (`persistToServer`, `syncList`/`syncOne`, `rekey`, `syncedAt`, `hasPendingSync`, `remove` role-aware). Diferencia clave: como las fotos no son un array plano sino `fotoPlano` + 3 slots nombrados por punto, el merge servidor↔local (`mergeFromServer`/`rekey`) indexa **todas** las fotos del record en un `Map` por `storagePath` (`collectFotosByPath`) en vez de buscar por posición, para no depender de que los puntos conserven el mismo orden/índice.
  - **Bug encontrado y corregido antes de que llegara a producción** (no en el navegador, por inspección de código): `useRestorePhotoPreviews.ts` (restaura previews desde IndexedDB al montar) usaba `updateCuadrante`/`setFoto`, que sí marcan `touch()` (bump de `updatedAt`). Con autoguardado real eso habría disparado un guardado espurio solo por abrir el Editor — el mismo bug que ya se había encontrado y arreglado en ATT (`setFotoPreview`/`setFotoAereaPreview`). Se agregaron setters gemelos sin `touch()`: `setFotoPlanoPreview`/`setPuntoFotoPreview`, y se migró el hook a usarlos.
  - Hooks nuevos: `usePreventivoAutosave.ts` (idéntico a `useAttAutosave.ts`, sin partes específicas de ATT) y `useResolvePhotoUrls.ts` (resuelve signed URLs en lote, recorriendo plano + puntos).
  - `Editor.tsx`: mismo fix de condición de carrera que ATT (`hadRecord` ref) para la promoción de id nanoid→uuid, `syncOne` al montar, indicador de guardado con estado de error + reintentar.
  - **Verificado en el navegador de punta a punta**: crear levantamiento → autoguardado → promoción de id (sin la carrera, ya con el fix desde el diseño) → URL correcta; agregar punto + foto → subida a Storage (`preventivos/{blobId}.jpg`) confirmada + fila en `puntos` con el path correcto; reload/deep-link → `syncOne` + signed URL resueltas → **sin guardado espurio** (mismo chequeo que en ATT); filtro Activos/Cerrados/Todos con datos reales; `remove()` como invitado (jp) confirmado en la BD como cierre, no borrado.
- **PASO 7 — Inventario**: diseño acordado con el usuario a fondo (ver historial de conversación) antes de tocar código, dado el riesgo de descuadrar cantidades. Decisión de arquitectura clave: la lógica de negocio de "registrar un movimiento" vive **en la base de datos** (función Postgres `security definer`), no en JS del cliente — necesaria por atomicidad (stock + movimientos + proyecto_materiales deben cuadrar siempre) y porque un técnico necesita escribir tablas que la RLS le bloquea directamente.
  - **Esquema** `supabase/migrations/0003..0007`: `0003` amplía `movimientos.tipo` (+`solicitud`,`instalado`), agrega `movimientos.punto_id`/`proveedor`/`documento`, default de lote `'SinDefinir'`, `proyecto_materiales.punto_id` (nullable, con dos índices únicos parciales por el caso `punto_id is null`). `0004` renombra `ubicaciones.tipo` `'camioneta'`→`'tecnico'`. `0005` agrega las funciones `registrar_movimiento`/`adjust_stock`/`ensure_ubicacion_tecnico`/`adjust_proyecto_material`/`reasignar_transito_a_preventivo` (`EXECUTE` revocado de `PUBLIC`, solo internas). `0006` corrige `reasignar_transito_a_preventivo` (duplicaba stock — el material ya estaba físicamente donde debía, la función es puro bookkeeping). `0007` corrige `registrar_movimiento`: Entrega/Devuelto ahora aceptan `project_id` nulo (**"Salida preventiva"**, técnico surtiendo su stock personal sin OTT asociado); Solicitud/Instalado/Rebajado lo siguen exigiendo. Todas verificadas contra la BD real con scripts Node temporales (nunca solo por lectura de código) antes de darlas por buenas.
  - **Tabla tipoUI → efecto**: `entrada` (bodega +fisico, sin proyecto) · `solicitud` (solo registro, requiere proyecto+técnico) · `entrega` (bodega→técnico, proyecto opcional) · `instalado` (resta stock técnico, requiere proyecto+técnico) · `devuelto` (técnico→bodega, proyecto opcional) · `rebajado` (resta digital, SAP, requiere proyecto+técnico+bodega).
  - **Capa de datos** `src/lib/inventario/{types,inventarioRepo}.ts`: lecturas (`listMateriales`/`listUbicaciones`/`getStock`/`listMovimientos`) + wrappers RPC (`registrarMovimiento`/`reasignarTransitoAPreventivo`) + agregaciones en cliente (`getResumenProyecto` fusiona `proyecto_materiales`+`movimientos` tipo=`solicitud`; `getTecnicoLedger` agrupa `movimientos` por usuario). **Bug real encontrado y corregido de paso**: `src/lib/adminRepo.ts` (`listMembers`) y el `inventarioRepo.ts` nuevo asumían que todo embed de PostgREST vuelve como array; un embed "hacia adelante" (la tabla consultada tiene la columna `*_id`, ej. `stock.ubicaciones(nombre)`) vuelve como **objeto simple**, confirmado empíricamente — solo es array cuando el embed es "hacia atrás" (`projects.informes(...)`). El bug en `adminRepo` ya estaba en producción (silencioso: "Equipo por proyecto" nunca marcaba los checkboxes existentes) y quedó arreglado de paso.
  - **UI compartida** (`src/ui/`): `RegistrarMovimientoForm.tsx` (Datos: toggle Entrada/Salida + tipo/proyecto/técnico/punto/fecha/nota — según props `fixedProject`/`puntos`/`lockTipoUI` oculta lo que no aplica; Material: líneas repetibles con bodega por línea solo si el tipo la necesita); `ResumenProyectoTable.tsx` (solicitado/entregado/instalado/devuelto/rebajado/tránsito por material+lote+punto, con botón "Reasignar tránsito a preventivo" inline cuando tránsito > 0); `LogisticaTab.tsx` (equipo asignado + resumen + formulario, fijados a un proyecto).
  - **ATT y Preventivos**: `Editor.tsx` de ambos ganó tabs "Info de proyecto"/"Logística" (oculto hasta guardar el registro — necesita un `project_id` real). Preventivos pasa además `puntos` del cuadrante a `LogisticaTab` (habilita desglose "· nombre del punto" en el resumen y selector de punto al registrar, incluida la opción "Ningún punto en particular").
  - **Ventana Inventario** (`src/modules/inventario/`, antes "Gestión de inventario" en `ProjectHome.tsx`, ahora activa y renombrada a "Inventario"): 5 sub-tabs — Entradas/Salidas (Registro con `RegistrarMovimientoForm` sin proyecto fijo, incluye la opción de Salida preventiva; Movimientos con filtros de tipo/fecha/texto), Bodega (`getStock` + selector de bodega + buscador), Proyecto (selector de proyecto activo, ambas áreas, + `ResumenProyectoTable`), Técnico (selector de técnico/log + `getTecnicoLedger`), Conteo (placeholder — deferred, ver abajo).
  - **Bug encontrado y corregido en el navegador**: el toggle Entrada/Salida y el modo Entrada de `RegistrarMovimientoForm` solo se ocultaban con `lockTipoUI`, no con `fixedProject` — dentro de la pestaña Logística de un proyecto se podía cambiar a "Entrada", lo cual no tiene sentido ahí (una entrada de bodega no está ligada a ningún proyecto). Corregido: ambas props ocultan el toggle.
  - **Bug encontrado y corregido en el navegador**: la fecha de un movimiento se mostraba un día antes en "Movimientos" (`new Date(fecha).toLocaleDateString('es-CL')` usa timezone local del navegador; el campo `fecha` es un día calendario, se guarda como medianoche UTC). Corregido formateando con `{ timeZone: 'UTC' }`.
  - **Verificado**: migración 0007 con script Node contra la BD real (Entrega/Devuelto sin proyecto ahora funcionan; Solicitud/Instalado/Rebajado siguen exigiéndolo; regresión de Entrega-con-proyecto sigue actualizando `proyecto_materiales`). Flujo completo en el navegador (guest/jp): Registro→Entrada de un material de prueba → aparece en Bodega y en Movimientos con cantidad y fecha correctas; pestañas Logística de ATT y Preventivos cargan sin error con el proyecto fijado y sin selector de proyecto/Entrada. **Sin usuarios `tecnico`/`log` reales todavía** (solo admin+jp existen) — el selector de técnico queda vacío en todos lados hasta que se cree el primero; es el comportamiento esperado, no un bug.
  - **Pendiente** (fuera de esta sesión): sub-opción "Consumibles" en Inventario (alcance sin definir); tabla maestra de proveedores (alcance sin definir) — ambas explícitamente pospuestas por el usuario.
- **PASO 7b — Stock negativo, umbral de renovación y Conteo** (pedidos tras probar la app desde otro PC en red local):
  - **Migración `0008_stock_alertas.sql`**: `materiales.stock_minimo` (numeric, nullable — umbral de "hay que renovar" por SKU); el stock ahora **puede quedar negativo** (descuadre a revisar, no imposibilidad): `adjust_stock` ya no lanza excepción por insuficiencia, y se eliminaron los `CHECK >= 0` de `stock` que venían de `0001` (descubiertos al repasar el esquema — sin dropearlos, la función podía permitirlo pero Postgres igual rechazaba el UPDATE; constraints buscados dinámicamente por columna, no por nombre).
  - **Migración `0009_conteo.sql`**: flujo de conteo completo en funciones de BD — `abrir_conteo` (crea el `conteos` y toma la foto del stock ≠ 0 de esa ubicación+naturaleza en `conteo_lineas`, con `cantidad_contada` = `cantidad_sistema` de partida), `actualizar_linea_conteo`, `agregar_linea_conteo` (material hallado que el sistema tenía en 0; rechaza duplicados), `cerrar_conteo` (por cada línea con diferencia: ajusta `stock` a lo contado + abre un `eventos_inventario`; el conteo cerrado queda inmutable), `resolver_evento_inventario` (marca resuelto con causa `devolucion_pendiente`/`reubicacion`/`perdida`). Todo requiere `can_move_inventory()`.
  - **UI**: Bodega marca en **rojo** las cantidades negativas ("⚠ Descuadre — revisar") y en **ámbar** las que están en o bajo el umbral ("Renovar (mín. N)"), con editor inline del umbral por fila ("Umbral: N", clic para editar — no existe pantalla de gestión de materiales aún). Sub-tab **Conteo**: lista de conteos + banner de eventos abiertos con resolución inline; detalle con líneas editables (guardado on-blur), alta de línea manual y cierre.
  - **Fragilidad real detectada al verificar en el navegador**: el guardado on-blur de una línea es async; si el usuario tocaba "Cerrar conteo" justo tras tipear, el cierre podía ganarle la carrera al guardado (cerrando con la cantidad vieja). Fix: cada fila reporta su estado dirty/guardando al padre y el botón "Cerrar conteo" queda deshabilitado con la pista "Hay cantidades sin guardar…" mientras exista alguna. También: la diferencia mostrada en un conteo cerrado ahora sale de lo persistido, no del draft local.
  - **Verificado**: script Node contra la BD real (stock_minimo editable; stock queda en -3 sin excepción; ciclo abrir→contar→línea manual→duplicado rechazado→cerrar (stock ajustado + eventos con diferencia correcta)→inmutabilidad→resolver (+re-resolver e inválida rechazadas)); y en el navegador con datos de prueba: alertas ámbar/roja en Bodega, editor de umbral, candado de cierre con edición pendiente, cierre real (-4 → 3, evento +7) y resolución del evento con desaparición del banner. Datos de prueba limpiados.
- **PASO 7c — Crear bodega inline + importar Excel SAP para conteo digital**: pedidos al probar desde otro PC en red local (primera vez con la app "real", sin datos de prueba propios).
  - **Crear bodega sin salir del formulario**: antes, todo selector de ubicación solo listaba lo ya existente — sin materiales/bodegas cargados (catálogo real vacío), esto bloqueaba cualquier flujo desde cero. Nuevo componente compartido `src/ui/UbicacionSelect.tsx` (opción "+ Nueva bodega…" → input de nombre inline → `inventarioRepo.crearUbicacion`), reemplaza los `<select>` de bodega en `RegistrarMovimientoForm.tsx` (Entrada, y por línea en Entrega/Devuelto/Rebajado) y en `NuevoConteoForm` (Inventario → Conteo).
  - **Importar conteo digital desde Excel SAP** (`src/lib/inventario/importarSap.ts` + `inventarioRepo.importarFilasSapAConteo`): formato confirmado con un archivo real de la empresa — columnas `SAP` (código), `MATERIAL` (descripción), `CENTRO`/`ALM` (constantes, sin uso), `PEP`, `LOTE`, `STOCK`, `Fisico` (vacía). El parser ubica columnas **por texto de encabezado**, no por posición — columnas de más o en otro orden no rompen nada; si falta un encabezado reconocible, el error dice exactamente cuál falta y qué encabezados sí vio, para que quien sube el archivo lo corrija solo. Acepta `.xlsx` (ExcelJS, mismo bundle ya usado para generar informes) o texto pegado tab-separado (copiar celdas de Excel y pegar).
  - **PEP resuelto con el usuario tras un hallazgo real**: el archivo de prueba tenía 6 casos donde el mismo Lote se repite con PEP distinto y cantidades distintas (ej. lote 945460: 130 en `...GPHBOD01` + 2 en `E-24RE38.EQU`) — sumar ciegamente por SKU+Lote sin más habría sido la decisión correcta, pero se confirmó explícitamente con el usuario antes de asumirlo (es "un mismo lote repartido en diferentes pedidos" de SAP, sin lógica de negocio nuestra detrás). Se agrupa sumando cantidad por (sku, lote); PEP se ignora.
  - Import crea los materiales que falten (`ensureMaterialBySku`, por sku) y llena `cantidad_contada` de cada línea del conteo (agregándola si no estaba en la foto inicial) — secuencial, una llamada RPC por línea, con barra de progreso; el botón "Cerrar conteo" queda bloqueado mientras la importación está en curso (mismo candado que las ediciones de línea sin guardar).
  - **Verificado con el archivo real de la empresa** (`Sinterk.xlsx`, 71 filas → 65 agrupadas): parseo exacto (SKU 220/lote 945460 = 132, la suma de los dos PEP) confirmado por script y en el navegador subiendo el archivo real vía `<input type=file>`; pipeline completo contra la BD real (crear material → agregar línea → actualizar cantidad → cerrar conteo → `stock.cantidad_digital` queda igual al Stock importado). Datos de prueba limpiados (incluida la bodega y los 40 materiales creados durante la prueba en el navegador).
- **PASO 7d — El primer conteo de un material no genera evento de diferencia**: toda línea agregada a mano (`agregar_linea_conteo`, botón "Material encontrado que no estaba en la lista") o por el import SAP parte de `cantidad_sistema=0` porque nunca antes hubo stock ahí — al cerrar, cualquier cantidad contada generaba un `eventos_inventario` de "diferencia a revisar" que en realidad no es un descuadre, es solo la primera vez que se registra ese material en esa ubicación.
  - **Migración `0010_conteo_primera_vez.sql`**: nueva columna `conteo_lineas.primera_vez` (default `false`); `agregar_linea_conteo` la marca `true`; `cerrar_conteo` sigue ajustando `stock` igual en ambos casos, pero solo abre el evento cuando `primera_vez = false` (líneas que vinieron de la foto inicial de `abrir_conteo`, donde sí hay un valor de sistema real contra el cual comparar).
  - **UI**: la línea muestra "primer conteo aquí" en vez de "sistema: N", y no se le pinta el badge de diferencia (`+N`/`-N`) aunque `cantidad_contada` sea distinto de 0.
  - **Verificado**: script Node contra la BD real (línea de la foto inicial con corrección real 5→8 genera evento con diferencia +3; línea agregada a mano con +10 ajusta el stock igual pero no genera evento) y en el navegador (mismo caso, con `<input type=file>` no necesario esta vez — solo alta manual): sin badge de diferencia en ningún momento, texto "primer conteo aquí", cierre real deja stock=25 y cero eventos. **Ojo**: durante esta verificación se confirmó que el catálogo de materiales real ya no está vacío — el usuario lo cargó con el import SAP en su propia sesión (`npm run dev` en el puerto 5173) mientras se hacían estas pruebas en el puerto 5199 para no interferir; los datos de prueba de esta sesión (sku `UI-PV-*`, ubicación `Bodega UI PV *`) se limpiaron sin tocar el catálogo real.
- **PASO 7e — Drag-and-drop en toda subida de archivos**: nuevo hook `src/ui/useFileDrop.ts` (contador de dragenter/dragleave, no comparación target===currentTarget, para no parpadear al pasar sobre hijos internos del dropzone) aplicado en los 7 puntos donde se sube un archivo: `PhotoSlot.tsx` y `PhotoCapture.tsx` (fotos ATT/Preventivos, un archivo por slot), `SeccionFotos.tsx` (galería ATT, acepta varios archivos arrastrados a la vez, se procesan en secuencia), `SeccionDescripcion.tsx` (foto aérea), `CuadranteSection.tsx` (foto del plano), `ImportZip.tsx` (zip de levantamiento), y `ImportarSapSection` en `modules/inventario/components/Home.tsx` (Excel SAP). Cada componente ya tenía (o se le extrajo) un `processFile(s)` compartido entre el `<input type=file>` y el nuevo drop, para no duplicar la lógica de subida. Resaltado visual (borde/fondo `brand-500`) mientras se arrastra un archivo sobre la zona.
  - **Verificado en el navegador**: simulando un `DragEvent` con `DataTransfer` real sobre el dropzone del import SAP — el resaltado aparece en `dragenter` y el archivo soltado se parsea igual que por el input nativo.
  - **Incidente durante la verificación, corregido de inmediato**: al limpiar el conteo de prueba abierto para probar el drop, un script de limpieza asumió que el conteo más reciente (`order by created_at desc limit 1`) era el propio, pero el usuario abrió concurrentemente un conteo real en otra bodega (C132) en su propia sesión (puerto 5173) casi al mismo tiempo — por suerte el `.eq('id', conteo.id)` no coincidió con ninguna fila (la causa exacta no se determinó) y no se borró nada; se detectó la ambigüedad, se confirmó con el usuario cuál conteo era cuál antes de borrar, y se borró específicamente por id solo el propio (C103, 65 líneas sin editar), dejando el de C132 intacto. **Lección**: con el usuario trabajando en paralelo sobre la misma BD compartida, nunca asumir "más reciente" al limpiar datos de prueba — filtrar siempre por un identificador propio y verificar antes de borrar.
- **PASO 7f — Alias de columnas SAP estándar** (`src/lib/inventario/importarSap.ts`): el primer archivo real usado para probar (`Sinterk.xlsx`) tenía encabezados `SAP`/`MATERIAL` no estándar, donde `MATERIAL` traía la *descripción*. El usuario confirmó que el formato real de SAP MM usa `Material` para el código/SKU y `Texto breve de material` para la descripción — se corrigieron los alias (`sku` ahora incluye `material`; se quitó de `descripcion`, que ahora usa `texto breve de material`) y se agregó `Libre utilización` (con y sin tilde) como alias de `cantidad`. Como consecuencia, el formato viejo de `Sinterk.xlsx` ya no se reconoce automáticamente (choque irresoluble: "material" no puede significar código y descripción a la vez) — si vuelve a aparecer ese formato, el error ahora dice exactamente qué columna falta y qué encabezados sí reconoció, para renombrar la columna manualmente. Texto de la UI (`Home.tsx`) actualizado para reflejar los nombres nuevos. Verificado con script: formato nuevo (con y sin columnas extra intercaladas) parsea bien; formato viejo falla con el mensaje esperado.
- **PASO 8 — Sesión del 13-07-2026: pulido de Bodega, Excel más rápido, y Logística/ATT** (v0.68→v0.75). Todo verificado en el navegador contra la BD real; commits ya en `origin/backend-supabase`.
  - **Tabla Bodega**: SKU y nombre en columnas separadas (antes "SKU — nombre" junto) con líneas divisorias entre todas las columnas. Orden por defecto Bodega→SKU→Lote; SKU se ordena **numérico** (`src/lib/inventario/sku.ts`, `compareSku` — un SKU con letras siempre va al final). Cada columna (las 7) tiene un botón ▾ con menú: ordenar asc/desc + **filtro tipo Google Sheets** (checklist de valores presentes, con buscador si hay más de 8) — reemplaza el filtro de texto libre que tenía antes solo Bodega.
  - **Excel SAP también en conteo físico** (antes solo digital — era un candado de UI sin motivo, el backend ya soportaba ambos).
  - **Descartar conteo**: nueva función `descartar_conteo` (migración `0011`) para cancelar un conteo **abierto** sin tocar stock (uno cerrado no se puede deshacer así). Botón en `ConteoDetail`.
  - **Import de Excel SAP hecho por lotes** (antes 2-4 llamadas de red POR FILA, secuenciales — con un archivo de un par de cientos de filas tardaba minutos): ahora los materiales faltantes se crean en un solo `insert` y las líneas se vuelcan con una única RPC (`importar_lineas_conteo`, migración `0012`, con fix de ambigüedad de columnas en `0013` — usar `#variable_conflict use_column` cuando el nombre de una columna de salida choca con el de una columna real referenciada en el cuerpo de la función) que hace el loop en Postgres. 3 llamadas totales sin importar el tamaño del archivo.
  - **Fix de bug**: el menú ▾ de filtro/orden de una columna se recortaba (quedaba "escondido") cuando la tabla tenía pocas filas — el contenedor tiene `overflow-x-auto`, que por regla de CSS fuerza `overflow-y` a `auto` también, recortando cualquier hijo absoluto que se saliera por abajo. Fix: el popover se porta a `document.body` con `position:fixed` (posición calculada desde el botón al abrir) en vez de vivir dentro del `<th>`.
  - **Logística (ATT/Preventivos), `RegistrarMovimientoForm.tsx`**:
    - Selector de material con buscador y orden numérico (`src/ui/MaterialSelect.tsx`, reemplaza el `<select>` nativo).
    - Selector de lote con disponibilidad real: "LOTE (CANTIDAD)", deshabilitado/gris si no hay cantidad, admite dejarlo en blanco (`src/ui/LoteSelect.tsx`) — mira la ubicación correcta según el tipo de movimiento (bodega elegida para Entrega/Rebajado, ubicación personal del técnico para Instalado/Devuelto; sin referencia para Solicitud, cae a texto libre).
    - Bodega de origen/destino por defecto según el área del proyecto: **C088 en ATT, C132 en Preventivos** (`src/lib/inventario/defaults.ts`, `BODEGA_DEFECTO_POR_AREA`).
    - Técnico por defecto: el primero de los asignados al proyecto (`adminRepo.listMembers`) — antes listaba TODOS los técnicos del sistema sin ningún default.
  - **`ResumenProyectoTable.tsx` (resumen de material del proyecto)**: pasó de tarjetas a **tabla** con divisores (SKU/Lote/Solicitado/Entregado/Instalado/Devuelto/Rebajado/Tránsito). Dos formas de editar las 5 cantidades:
    - **Modo normal ("+", suma)**: cada valor tecleado se registra como un movimiento real (mismo camino atómico que "Registrar movimiento", con técnico/bodega elegidos en una barra que aparece al haber cambios pendientes) — nunca sobreescribe directo, así nunca se desincroniza de `stock`. Solo admite sumar (la función de BD no acepta negativos); para bajar un valor hay que registrar el movimiento contrario.
    - **Modo corrección ("=", botón "🔧 Corregir errores de tipeo", solo admin/jp/log)**: sobreescribe el número directo vía `corregir_proyecto_material` (migración `0014`) **sin generar movimiento ni tocar stock** — exclusivamente para arreglar un error de tipeo real. Incluye mensaje de instrucciones fijo mientras el modo está activo. Solicitado queda afuera de este modo porque no es una columna propia (se calcula sumando movimientos tipo='solicitud').
  - **Migraciones nuevas de esta sesión, todas ya corridas en Supabase**: `0011_descartar_conteo.sql`, `0012_importar_lineas_conteo.sql`, `0013_fix_importar_lineas_conteo.sql`, `0014_corregir_proyecto_material.sql`.
- **PASO 9 — Menú simplificado para el rol técnico (14-07-2026, v0.77)**: hasta ahora todos los roles veían el mismo nav (Inicio + Preventivos + ATT + Inventario) y el técnico llegaba a sus proyectos a través de las Home completas de cada módulo (con crear/importar/borrar, pensadas para jp/admin). Pedido explícito: el técnico debe ver solo dos ventanas — Asignaciones e Inventario propio.
  - **`src/ui/AsignacionesScreen.tsx`** (nuevo): home del técnico. Combina `attRepo.list({estado:'activo'})` + `preventivoRepo.list({estado:'activo'})` — no hace falta filtrar por usuario en la query, la RLS de `projects` ya solo devuelve las filas donde el usuario es `is_member()`. Tarjeta simple (badge de área + código + nombre + comuna) que navega a `/att/:id` o `/preventivos/:id`, sin crear/importar/borrar.
  - **`src/ui/InventarioTecnicoScreen.tsx`** (nuevo): "lo que tengo en mi poder" = entregado − instalado − devuelto − rebajado. Reutiliza `getTecnicoLedger(userId)` (la misma función que ya alimentaba la pestaña "Técnico" de Inventario para admin/jp/log) acotada a la sesión propia, y el componente `Stat` de `ResumenProyectoTable.tsx` — sin lógica ni tabla nuevas.
  - **`App.tsx`**: si `profile.rol === 'tecnico'`, la ruta `/` renderiza `AsignacionesScreen` en vez de `ProjectHome`, y se agrega la ruta `/mi-inventario`. Las rutas "Inicio" de los módulos (`/att`, `/preventivos`, `/inventario`) se excluyen del router para este rol filtrando por `route.label !== 'Inicio'` — los editores (`/att/:id`, `/preventivos/:id`) se mantienen registrados, son a donde navega Asignaciones.
  - **`Layout.tsx`**: nav inferior condicional por rol — técnico ve "📋 Asignaciones"/"📦 Inventario"; el resto ve el nav de siempre (Inicio + módulos). El invitado técnico temporal (`iperez@sinterk.cl`, ver "Estado actual" más arriba) tiene `profile.rol='tecnico'`, así que activa esta misma vista — sirvió para probar todo esto sin necesitar una cuenta de técnico real.
  - **Verificado en el navegador con la cuenta invitado técnico** (`iperez@sinterk.cl`): nav muestra solo Asignaciones/Inventario; Asignaciones lista los proyectos ATT asignados (RLS confirmada — solo los suyos); clic abre el editor real (`/att/:id`) con pestañas Info/Logística intactas; Inventario muestra el ledger correcto (material con tránsito positivo y uno con tránsito negativo por datos de prueba previos, cálculo consistente con `getTecnicoLedger`).
- **PASO 10 — Comentario de material, Observaciones, y candados de técnico dentro del editor (14-07-2026, v0.78)**: pedido explícito, tres partes.
  - **Comentario libre por material**: columna `materiales.comentario` (migración `0015_material_comentario.sql`, **ya corrida**). Editor inline en la pestaña Bodega (`ComentarioEditor` en `modules/inventario/components/Home.tsx`), mismo patrón que `UmbralEditor` — columna nueva `StockColKey='comentario'` en la tabla, mismo `mat_write` (admin/jp/log) que el resto de `materiales`, sin política RLS nueva.
  - **Observaciones** (`src/ui/ObservacionesSection.tsx`, nuevo): entradas libres de texto por proyecto — sin edición, solo agregar/borrar. Pensada, entre otras cosas, para que un técnico avise de material instalado que no quedó registrado como entregado y oficina lo corrija por su cuenta. Tabla `observaciones` nueva (migración `0016_observaciones.sql`, **ya corrida**): `usuario_id uuid not null default auth.uid()` (nunca lo manda el cliente); RLS lectura/alta = `can_move_inventory()` (admin/jp/log) o `is_member(project_id)` (técnico asignado); borrado = `is_jp_or_admin()` puede cualquiera, el resto (técnico, log) solo la propia. Se agregó a `LogisticaTab.tsx` para **todos** los roles (no solo técnico).
  - **`LogisticaTab.tsx` reducida para técnico**: ya no ve "Técnicos asignados" (`EquipoSection`) ni el formulario suelto de `RegistrarMovimientoForm` — solo la tabla Material (`ResumenProyectoTable`) y Observaciones.
  - **`ResumenProyectoTable.tsx` — edición acotada para técnico**: nuevo `editableCampos` (antes todas las columnas eran editables para quien pudiera ver el "+"); para `rol==='tecnico'` solo `cantInstalada`/`cantDevuelta` muestran el input "+", el resto (Solicitado/Entregado/Rebajado) se ve como valor fijo. **Candado de UI, no de RLS**: `registrar_movimiento` (migración `0005`) ya autoriza al técnico a registrar cualquier `tipoUI` sobre sus propios proyectos asignados (por diseño, para que pueda operar sin pasar por admin/jp/log) — este candado solo lo oculta de la interfaz, mismo criterio que el candado de auto-democión del admin en `UserRow` (`AdminScreen`/`adminRepo`).
  - **"Info de proyecto" para técnico — solo fotos**: pedido explícito tras aclarar que "solo logística" no debía tapar la captura de fotos de campo. Nuevo prop `soloFotos` en `CuadranteSection.tsx` (Preventivos: oculta Comuna/N°Cuadrante/Grupo + el bloque JP fecha/semana/nombre/dirección/zona/responsable, deja "Foto del plano") y `PuntoCard.tsx` (Preventivos: nuevo `camposEditable = editable && !soloFotos` gobierna nombre/descripción/dirección/corrección/hallazgo/resuelto/reordenar/eliminar-punto; las 3 fotos del punto siguen atadas a `editable`, no a `soloFotos`, así que técnico las sigue pudiendo agregar/quitar). En Preventivos `Editor.tsx`, el botón "➕ Agregar punto" queda oculto para técnico (decisión conservadora: crear un punto nuevo no es "agregar una foto"; si el usuario necesita que el técnico también pueda crear puntos nuevos, falta ese ajuste). (`SeccionDescripcion.tsx` de ATT llegó a tener este mismo prop en esta sesión, pero se revirtió — ver PASO 11.)
  - **Verificado en el navegador con el invitado técnico**: Logística muestra solo Material (Instalado/Devuelto con "+", el resto fijo) + Observaciones (el POST real falló hasta correr la migración 0016, con el error esperado de PostgREST "tabla no encontrada" — confirma que el código llega bien a Supabase). No se pudo probar en vivo el lado Preventivos (la cuenta de prueba no tiene un Preventivo asignado) — la implementación es simétrica a ATT y pasa `tsc --noEmit` limpio, pero queda pendiente de un vistazo cuando haya un Preventivo asignado a un técnico real.
- **PASO 11 — Fix de nav + ajuste de fotos en ATT para técnico (14-07-2026, v0.79)**:
  - **Bug encontrado por el usuario**: al volver de una asignación (botón "← Volver" del editor, que navega a `/att` o `/preventivos`), el nav inferior de técnico desaparecía por completo (ni Asignaciones ni Inventario). Causa: `Layout.tsx` decide `showNav` comparando el pathname contra una lista fija de "pantallas de inicio"; para técnico esa lista solo tenía `/`, `/admin` y `/mi-inventario` — no incluía `/att`/`/preventivos`, que es justo a donde vuelve el botón. Fix: la lista de técnico ahora agrega `...modules.map(m => m.indexPath)` igual que la de los demás roles.
  - **Ajuste**: la Foto general de ATT (la busca el JP desde mapa, no es tarea del técnico) se saca por completo de lo que ve el técnico en Info de proyecto — queda solo el Registro fotográfico (galería). Se revirtió el prop `soloFotos` de `SeccionDescripcion.tsx` (quedó sin usuarios) y en `Editor.tsx` de ATT el rol técnico ahora renderiza únicamente `<SeccionFotos />`.
  - **Nota pendiente de confirmar con el usuario**: no se tocó el lado Preventivos — ahí "Foto del plano" (`CuadranteSection`) sigue siendo editable por el técnico, porque el tipo original la marca como "Llena el TÉCNICO" (a diferencia de la Foto general de ATT). Si en Preventivos también corresponde que esa foto la ponga el JP y no el técnico, falta ese mismo ajuste ahí.
  - **Verificado en el navegador con el invitado técnico**: Info de proyecto en ATT muestra únicamente "Registro fotográfico"; volver de un proyecto con "← Volver" ya deja el nav (Asignaciones/Inventario) visible.
- **PASO 12 — "+ Nuevo material" en la tabla, se quita el formulario aparte, y estado Abierto/Cerrado en el Editor (14-07-2026, v0.80)**: pedido explícito, tres partes.
  - **`ResumenProyectoTable.tsx` gana "+ Nuevo material"**: botón (admin/jp/log, junto a "🔧 Corregir…") que agrega una fila en blanco arriba de la tabla — ahí mismo se elige `MaterialSelect` (SKU/nombre) y lote (`LoteSelect`) y se llenan las cantidades con el mismo mecanismo "+" que las filas existentes. Estado nuevo `nuevasFilas` (array, no una sola — se pueden abrir varias a la vez); se guardan con el mismo botón "Guardar cambios" ya existente (mismo técnico/bodega elegidos una vez para todo el lote de cambios), y cada fila cuyo material+cantidad se registró con éxito desaparece del borrador (ya quedó como fila real tras el `reload()`); las que fallan o quedan sin material elegido se mantienen con su error.
    - **Ajuste del mismo día**: el usuario pidió que el Lote de esa fila nueva fuera "desplegable editable, como en el formulario" (no el texto libre con el que quedó al principio). Se cambió `ubicacionId={null}` → `ubicacionId={bodegaEdicion || null}` (con `checkAvailability={false}`, modo "comodidad" — lista los lotes existentes en esa bodega sin bloquear ninguno, igual que hace `LoteSelect` para Entrada): como `bodegaEdicion` ya se auto-completa con la bodega por defecto del área (`BODEGA_DEFECTO_POR_AREA`), en la práctica el desplegable ya viene poblado apenas se abre la fila nueva.
    - **Pendiente que quedó del 17-07 — resuelto distinto el 21-07-2026 (ver PASO 18)**: en su momento se planteó "corregir el lote de una fila existente" como un problema de re-escribir historia (requeriría una función de BD que devuelva del lote viejo y registre de nuevo bajo el correcto). Terminó no haciendo falta esa función: bastó con dejar el Lote (y el Técnico) de las filas existentes editables de la misma forma que ya eran las de "+ Nuevo material" — ver PASO 18.
  - **Se elimina `<RegistrarMovimientoForm>` de `LogisticaTab.tsx`**: pedido explícito ("eliminemos el cuestionario") — quedaba redundante con "+ Nuevo material", que cubre el mismo caso (dar de alta un material que el proyecto no tenía) sin el paso adicional de elegir "Tipo" de movimiento aparte. El componente `RegistrarMovimientoForm.tsx` no se borró — sigue en uso en `modules/inventario/components/Home.tsx` → Entradas/Salidas (movimientos sin proyecto fijo). De paso se quitó el prop `projectOtt` de `LogisticaTab` (quedó sin uso al irse el formulario).
  - **`EstadoProyectoBadge.tsx`** (nuevo, en `src/ui/`): selector Abierto/Cerrado sobre las pestañas Info de proyecto/Logística, en el header de ambos `Editor.tsx`. Editable solo para admin/jp (mismo candado de UI que el resto de estos toggles); el resto ve un badge fijo sin `<select>`. Requería una función de reapertura que no existía: `reopenProject()` nueva en `projectLifecycle.ts` (gemela de `closeProject()`, estado=activo + fecha_cierre=null), expuesta como `attRepo.reopen`/`preventivoRepo.reopen`. Acción nueva `setEstado(id, estado)` en ambos stores (`att/store.ts`, `preventivos/store.ts`) que llama `close`/`reopen` y actualiza el cache local al vuelo (sin esperar un refetch) — separada de `remove()` porque esa ya tenía su propia lógica de admin-borra/jp-cierra y no hacía falta tocarla.
  - **Verificado en el navegador con la cuenta invitado (jp) sobre un proyecto de prueba real**: "+ Nuevo material" con el material de prueba `999901` + cantidad en Entregado → apareció el bloque "Guardar cambios (1)" → al guardar, la fila pasó a ser una fila real de la tabla (ya no editable como borrador); el formulario de "Registrar movimiento" ya no aparece en Logística; el selector de estado cerró el proyecto (confirmado en Home con el filtro "Cerrados"), y lo reabrió de vuelta (confirmado con el filtro "Activos").
- **PASO 13 — Lote como desplegable + Técnico por fila en "+ Nuevo material" (17-07-2026, v0.81→v0.82)**:
  - **Lote de la fila nueva verificado en el navegador**: el ajuste `ubicacionId={bodegaEdicion || null}` (dejado sin probar al final de la sesión anterior por caída del dev server) quedó confirmado — el campo pasa de texto libre a un `<select>` real ("Sin lote específico" + lotes existentes) apenas hay una bodega elegida (que ya viene con el default del área).
  - **Selector de técnico por fila**: nueva columna "Técnico" **a la izquierda de SKU** en la tabla — pedido explícito. Solo tiene contenido en las filas de "+ Nuevo material" (`NuevaFila.tecnicoUserId`, por defecto `members[0]?.id` = primer técnico asignado al proyecto); las filas existentes muestran "—" ahí (no aplica, no son un concepto por-fila). `guardarCambios()` ahora usa `fila.tecnicoUserId` en vez del selector compartido de la barra "Guardar cambios" para las filas nuevas — ese selector compartido (`tecnicoEdicion`) sigue existiendo y sigue siendo obligatorio, pero solo cuando hay ediciones pendientes sobre filas **existentes**; una fila nueva sin su propio técnico elegido muestra su propio error ("Elige un técnico antes de guardar") en vez de bloquear todo el guardado.
  - **Verificado en el navegador con la cuenta invitado (jp)**: la fila nueva trae "iperez@sinterk.cl" preseleccionado (único técnico asignado a este proyecto de prueba); alta completa del material `999902` con Entregado=2 usando ese selector por fila, guardado exitoso confirmado como fila real en la tabla.
- **PASO 14 — Bug: recién asignado un técnico, no aparecía en "+ Nuevo material" hasta salir y volver a entrar a la OTT (17-07-2026, v0.83)**:
  - **Causa**: `EquipoSection` (checklist de técnicos asignados) y `ResumenProyectoTable` leen `project_members` cada uno con su propio `useEffect`/estado local, sin nada que los conecte. Al tildar un técnico en `EquipoSection`, ese componente recargaba **su propia** lista; `ResumenProyectoTable` nunca se enteraba (su `useEffect` de members solo depende de `projectId`, que no cambió) — el selector de "Nuevo material" seguía con la lista vieja hasta desmontar/remontar el Editor (salir y volver a entrar).
  - **Fix**: mismo patrón que ya existía para el stock (`refreshKey`). `LogisticaTab.tsx` ahora guarda un contador `membersVersion`; se lo pasa a `ResumenProyectoTable` (nuevo prop opcional, se suma a las deps del `useEffect` de `listMembers`) y un callback `onMembersChanged` a `EquipoSection`, que lo dispara justo después de `addMember`/`removeMember`.
  - **Verificado en el navegador con la cuenta invitado (jp), en una OTT de prueba sin técnico asignado**: tildar el checkbox de "Técnicos asignados" y, **sin salir de la OTT**, abrir "+ Nuevo material" → el selector de técnico de esa fila ya trae la opción nueva (una fila ya abierta antes de asignar no cambia su selección sola, pero si se abre una fila **después** de asignar, el técnico recién asignado queda preseleccionado por defecto, como se esperaba). Se deshizo la asignación de prueba al terminar.
- **PASO 15 — Bodega por fila en "+ Nuevo material" (17-07-2026, v0.84)**: pedido explícito, mismo patrón que el técnico por fila.
  - **`NuevaFila.ubicacionBodegaId`** (nuevo): columna "Bodega" agregada después de Lote. Por defecto trae `defaultBodegaId` (la bodega de `BODEGA_DEFECTO_POR_AREA[area]` ya resuelta a un id real — **C088 para ATT, C132 para OyM/Preventivos**; Incidencias, cuando se construya, ya heredaría el default de OyM porque comparte esa área). Filas existentes muestran "—" (igual que Técnico: no es un concepto por-fila ahí, siguen usando el selector compartido `bodegaEdicion` de la barra "Guardar cambios").
  - **Conectado también al selector de Lote de la fila**: `LoteSelect` ahora usa `fila.ubicacionBodegaId` (antes usaba el `bodegaEdicion` compartido) — cambiar la bodega de la fila actualiza en vivo qué lotes/cantidades disponibles se listan, y limpia el lote elegido (`lote: ''`) para no dejar seleccionado un lote que no aplica a la bodega nueva.
  - **`guardarCambios()`**: el `ubicacionBodegaId` que se manda a `registrar_movimiento` para una fila nueva ahora sale de `fila.ubicacionBodegaId`, no del `bodegaEdicion` compartido; mismo error por celda ("Falta elegir bodega") si falta.
  - **Verificado en el navegador con la cuenta invitado (jp) contra la BD real**: cambié la bodega de una fila de C088→C132→C103 y el desplegable de Lote se actualizó cada vez (mostrando `SinDefinir (-3)` en C088, vacío en C132 sin stock); guardé un Entregado=1 de `999901` con bodega **C103** explícita (distinta del default) y confirmé en Inventario → Bodega que el stock quedó correctamente en **C103: -1** (no en C088) y el técnico sumó el físico correspondiente — confirma que no es solo un dato visual, sí llega a `registrar_movimiento` con la bodega correcta.
- **Hecho — 3 usuarios técnico de prueba**: `tecnico1/2/3@sinterk.cl` creados. No se podían crear desde la app (falta `service_role key`, deliberadamente fuera de este entorno) — se armó `scripts/create-test-tecnicos.mjs` (usa `@supabase/supabase-js` + `auth.admin.createUser`, la key se pasa por variable de entorno al correrlo, nunca se commitea ni se pega en el chat) para que el usuario los creara de una pasada en vez de 3 veces por el dashboard. El trigger `handle_new_user` les puso `rol='tecnico'` solo; el `UPDATE` del script para ponerles nombre falló ("permission denied for table profiles" — `service_role` sin GRANT explícito en esa tabla en este proyecto, aparte de RLS) pero es cosmético nomás, la UI ya cae al correo cuando `nombre` es null. Verificados en el navegador: aparecen los 4 técnicos (iperez + los 3 nuevos) en "Técnicos asignados" de un proyecto ATT real.
- **PASO 16 — Bug: el desplegable de `MaterialSelect` tapaba la tabla de Material (17-07-2026, v0.85)**: reportado por el usuario ("la tabla de materiales queda oculta... al seleccionar material aparece el menú desplegable, queda tapada"). Mismo bug de fondo que ya se había arreglado en el filtro de columna de la pestaña Bodega (PASO 8): `MaterialSelect.tsx` posicionaba su panel con `position:absolute` dentro de la celda de la tabla — al vivir esa tabla en un contenedor `overflow-x-auto`, el panel (alto, con buscador + lista) terminaba tapando/recortado sobre las filas de abajo (y aparentaba "ocultar" también el resto de la pantalla mientras estaba abierto). Fix: mismo patrón que `StockColumnHeader` — el panel se porta a `document.body` con `position:fixed`, posición calculada desde `getBoundingClientRect()` del botón al abrir. Se arregla solo cerrando el desplegable (eligiendo un material o clic afuera) — no hacía falta F5, y no era un cuelgue de datos ni un crash. Verificado en el navegador: el panel ahora es hijo directo de `document.body` con `position:fixed` y coordenadas correctas.
- **PASO 17 — Movimientos como tabla con filtros, igual que Bodega (17-07-2026, v0.86)**: pedido explícito.
  - **`StockColumnHeader` generalizado a `ColumnHeader<K>`**: era específico de `StockColKey`; se parametrizó por tipo genérico (y `StockChecklistFilter` → `ChecklistFilter`, ya era agnóstico) para reusarlo tal cual en Movimientos sin duplicar las ~90 líneas del popover de orden+filtro. `BodegaTab` sigue igual, solo cambió el nombre del componente que usa.
  - **`MovimientosTab` reescrita como tabla**: mismo patrón que `BodegaTab` — 10 columnas (Fecha/Tipo/SKU/Material/Lote/Cantidad/Bodega/Proyecto/Técnico/Nota), cada una con su `ColumnHeader` (orden asc/desc + filtro tipo Google Sheets). SKU ordena numérico (`compareSku`, igual que Bodega); Fecha usa `m.fecha.slice(0,10)` (YYYY-MM-DD) como valor de orden/filtro en vez de la fecha formateada es-CL, para que ordene bien como string sin parsear formatos ambiguos.
  - **Qué se mantuvo vs. qué se quitó de los filtros de arriba**: el buscador de texto libre y el rango de fechas (`desde`/`hasta`) se mantienen **arriba de la tabla** porque son filtros que golpean la API (`desde`/`hasta` cambian qué se trae del servidor, no solo qué se muestra — importante porque `listMovimientos` trae máx. 200 filas). El `<select>` de "Tipo" que existía arriba se **quitó**: quedó cubierto por la columna Tipo con su propio filtro de checklist, evitando un control duplicado.
  - **Verificado en el navegador con la cuenta invitado (jp)**: tabla completa con datos reales; filtro de columna Tipo (destildar "Salida" dejó solo las 2 filas "Instalado"); orden de columna Cantidad descendente (500, 500, 3, 2, 2, 1×6) correcto.
- **PASO 18 — Lote y Técnico editables también en filas EXISTENTES de Material (21-07-2026, v0.87)**: dos pedidos relacionados ("que se pueda seleccionar el lote al modificar datos ya ingresados a una OTT" + "el técnico no parece guardarse en esa misma tabla").
  - **No era el problema de "corregir lote" que se había dejado pendiente del 17-07** (ese era sobre re-escribir un lote ya guardado, con todo el riesgo de desincronizar stock). Esto es más simple y sigue siendo 100% aditivo: las filas existentes de la tabla Material (que ya tenían Lote fijo en texto y "—" en Técnico) ahora tienen el mismo `LoteSelect`/`<select>` de técnico que ya tenían las filas de "+ Nuevo material" — con las cantidades "+" que se tecleen, el movimiento se registra con el **lote y técnico elegidos en esa fila**, no necesariamente los que la fila mostraba originalmente ni el técnico compartido de la barra de abajo.
  - **Nuevo estado** `rowLoteOverride`/`rowTecnicoOverride` (`Record<rowKey, string>`), con `getRowLote`/`getRowTecnico` cayendo al valor original de la fila / al `tecnicoEdicion` compartido si no se tocó nada — así el comportamiento por defecto (sin tocar los selects) queda idéntico a antes.
  - **Por qué sigue siendo seguro**: `guardarCambios()` solo cambió qué `lote`/`tecnicoUserId` le pasa a `registrarMovimiento()` para esa fila — sigue siendo un `INSERT` puro vía la función de BD (nunca un `UPDATE` de lo ya guardado). Si el lote elegido es distinto al de la fila, el movimiento nuevo simplemente aparece como **su propia fila separada** tras el `reload()` (mismo criterio que "+ Nuevo material": nunca se reescribe la fila vieja).
  - **Verificado en el navegador con la cuenta invitado (jp) contra la BD real**: en la fila existente de `83656` (lote `SinDefinir`), se cambió el selector de esa fila a lote `MNPXA29600` + técnico `tecnico1@sinterk.cl`, se tecleó Instalado=1 y se guardó — tras recargar apareció como fila nueva separada (Tránsito -1, lote `MNPXA29600`), y en Inventario → Movimientos el registro quedó con `lote=MNPXA29600` y `usuario=tecnico1@sinterk.cl` exactos, confirmando que no es solo visual.
- **PASO 19 — Pestaña Técnico (Inventario) como tabla con filtros, igual que Bodega/Movimientos (21-07-2026, v0.88)**: pedido explícito ("que aparezcan los materiales en formato tabla, como en los demás stock"). Mismo patrón otra vez, tercera vez que se reusa `ColumnHeader<K>` sin duplicar código: `TecnicoTab` pasó de tarjetas por fila a tabla con 9 columnas (SKU/Material/Lote/Proyecto/Entregado/Instalado/Devuelto/Rebajado/Tránsito) sobre `TecnicoLedgerRow`, cada una ordenable y con filtro tipo Google Sheets. El selector de técnico (arriba) se mantiene igual — solo cambió cómo se muestran las filas de abajo. `Stat` (de `ResumenProyectoTable.tsx`) quedó sin uso en este archivo tras el cambio, se sacó del import. Verificado en el navegador con la cuenta invitado (jp): cambiar de técnico recarga bien la tabla (probado iperez↔tecnico1, cada uno con sus propias filas reales); orden descendente de Tránsito correcto (500, 500, 4, 2, 2, 2, 1, 1, -1).
- **PASO 20 — Bug: cerrar un proyecto desde el Editor no lo sacaba de "Activos" en Home (21-07-2026, v0.89)**: reportado por el usuario ("qué se considera activo... al cerrarlos no parecen ser filtrados").
  - **Causa**: `Home.tsx` (ATT y Preventivos) arma la lista de "Activos" con `Object.values(records)` **sin filtrar por `estado`** — confía en que la caché local (Zustand) solo contiene proyectos activos. Eso era cierto mientras la única forma de cerrar era el botón "×" de Home (`remove()`, que sí saca el id de `records` con `dropLocal()` tras cerrar). Pero `EstadoProyectoBadge`/`setEstado()` (agregado en el PASO 12) cierra actualizando el campo `estado` del registro **en el mismo lugar de la caché**, a propósito sin sacarlo de `records` — si lo sacara, la página del Editor se quedaría en blanco o redirigiría a Home mientras el usuario sigue ahí mismo justo después de cerrar. Consecuencia: el campo `estado` en la BD y en la caché quedaba bien (`'cerrado'`), pero Home nunca lo miraba para la vista "Activos".
  - **Fix**: un filtro explícito, en los dos `Home.tsx` (ATT y Preventivos): `Object.values(records).filter((r) => r.estado === 'activo')` en vez de confiar ciegamente en el contenido de `records`. No se tocó `setEstado()` ni `remove()` — el fix es enteramente del lado de qué se muestra, no de qué se guarda.
  - **Verificado en el navegador con la cuenta invitado (jp) contra la BD real**: cerrar `_verify_repo2_ott` desde el Editor bajó el conteo de Home de 8→7 informes activos, y apareció correctamente bajo el filtro "Cerrados". Se reabrió al terminar para dejarlo como estaba.

- **PASO 21 — Descargar/borrar proyectos cerrados + visor de ZIPs archivados (21-07-2026, v0.90)**: pedido explícito para ahorrar espacio de Storage en Supabase ("quiero agregar un botón de 'descargar proyectos cerrados', entre tal y tal fecha... es razonable pensar que no sería necesario esto para los movimientos de materiales, al ser estos pequeños?").
  - **Por qué "solo fotos" y nunca borrar la fila del proyecto**: se revisó `supabase/migrations/0001_init.sql` y `movimientos.project_id` tiene `on delete cascade` — borrar la fila de `projects` arrastraría en cascada el historial de movimientos de material del proyecto (justo lo que el usuario ya determinó que no hace falta limpiar, por ser liviano). Además las fotos son ~99.8% del peso real de un proyecto (fotos comprimidas ~300-400KB c/u vs. filas de texto/jsonb del orden de KB) — borrar la fila encima de las fotos ahorra menos del 0.2% extra de espacio, a cambio de perder el historial navegable. Con esto se decidió: **"Descargar y borrar" borra únicamente los objetos del bucket Storage**, nunca la fila de `projects`/`informes`/`puntos`.
  - **Dos acciones separadas, no una sola**: "📥 Descargar" (exporta un ZIP combinado, repetible, sin tocar Supabase) y "🗑️ Descargar y borrar fotos" (solo se habilita tras una descarga exitosa de exactamente la misma selección — se guarda una "firma" de ids descargados y se compara).
  - **Refactor "core + wrapper" en los generadores de informes**: `generarPdfAtt.ts` (ATT, jsPDF), `generarLevantamiento.ts` y `generarInformeEntel.ts` (Preventivos, xlsx-js-style/ExcelJS) disparaban la descarga directamente (`doc.save()`/`XLSX.writeFile()`/click en un `<a>`) en vez de devolver un Blob. Se extrajo el "core" que arma el documento (`buildPdfAtt`, `buildLevantamientoWorkbook`, `buildInformeEntelBlob`) y se agregó una variante que devuelve `{blob, fileName}` (`generarPdfAttBlob`, `generarLevantamientoBlob`, `buildInformeEntelBlob`) sin cambiar el comportamiento de los botones individuales existentes (siguen descargando igual que siempre). `generarInformeAtt.ts` (docx) ya devolvía `Promise<Blob>`, no necesitó cambios.
  - **Bug real encontrado y arreglado, no solo de esta feature**: `compressImg` en `generarPdfAtt.ts` cargaba las fotos con `new Image()` **sin `crossOrigin='anonymous'`**. Con una foto local (`blob:` URL, mismo origen) no pasaba nada, pero con una signed URL de Supabase Storage (cross-origin) el canvas queda "manchado" y `canvas.toDataURL(...)` lanza `SecurityError` — como eso pasaba dentro de un `img.onload` sin `try/catch`, la excepción nunca resolvía la Promise y el `Promise.all` que arma el PDF quedaba **colgado para siempre, sin ningún error en consola**. Esto ya afectaba en producción a cualquier caso donde alguien generara el PDF de un proyecto cuyas fotos no tiene cacheadas localmente (p. ej. un JP generando el informe de un proyecto que fotografió un técnico) — simplemente nunca se había notado porque quien genera el informe suele ser quien mismo sacó las fotos. Fix: `img.crossOrigin = 'anonymous'` en los 4 lugares que cargan imágenes remotas (`generarPdfAtt.ts`, `generarInformeEntel.ts` ×2, `generarInformeAtt.ts`), más un `try/catch` alrededor del `toDataURL` en `compressImg` para que un futuro fallo similar resuelva `null` en vez de colgarse.
  - **Exportador masivo nuevo por módulo** (`src/modules/att/utils/exportClosedAtt.ts`, `src/modules/preventivos/utils/exportClosedPreventivos.ts`): arma un único ZIP con una carpeta por proyecto (`fotos/` + informes generados + `datos.json`), trayendo las fotos por `fetch(signedUrl)` (vía `getSignedUrls`, ya genérico en `@/lib/photoStorage`) en vez de depender del caché local (IndexedDB) como hace `ExportZipButton.tsx` — necesario porque un proyecto cerrado pudo haberse editado/fotografiado desde otro dispositivo. ATT incluye `informe_att.docx` + `informe_att.pdf`; Preventivos incluye `informe_entel.xlsx` + `levantamiento.xlsx`. Pendiente a futuro (no bloqueante): sumar el excel de "estado de pago" de ATT cuando ese módulo exista.
  - **`DescargarCerradosPanel.tsx`** (uno por módulo, mismo patrón): visible en Home solo con el filtro "Cerrados"; rango de fechas `desde`/`hasta` filtrando por `fechaCierre` en el cliente (sin tocar los repos); confirmación de borrado exige escribir `BORRAR` en un campo de texto (más fuerte que los `confirm()` del resto de la app, por ser irreversible). El borrado en sí: `removePhotoObjects(paths)` seguido de `attRepo.save()`/`preventivoRepo.save()` con las fotos vaciadas — reutiliza `replaceFotos`/`replacePuntos` (delete + insert vacío) que ya existían, **sin ninguna migración SQL nueva**.
  - **`ZipArchiveViewer.tsx`** (nuevo, `src/ui/`, compartido entre ambos módulos): visor 100% local de un ZIP ya descargado (individual o el combinado) — `<input type="file">` → `JSZip.loadAsync` → miniaturas de fotos + enlaces de descarga de los documentos. No sube ni consulta nada a Supabase; es la forma de seguir consultando fotos de un proyecto al que ya se le borraron de Storage. Botón "📂 Abrir descargado" en ambos `Home.tsx`.
  - **Verificado en el navegador con la cuenta invitado (jp) y contra la BD/Storage real**, usando los proyectos de prueba `_verify_inv_proj` y `TEST-VERIFY-PASO3` (ATT) y `C-TEST`/`C-TEST-99` (Preventivos): antes del fix de `crossOrigin` el botón "Descargar" se quedaba colgado en "Generando… 1/2" al llegar al proyecto con foto real (reproducido y diagnosticado en vivo inyectando el mismo `new Image()`/`toDataURL` en consola — confirmó `SecurityError: Tainted canvases may not be exported`); tras el fix, el ZIP se generó completo en ~2.5s con `fotos/`, `.docx`, `.pdf`, `datos.json` por carpeta (confirmado inspeccionando el Blob con JSZip directo en consola, sin pasar por la descarga real del navegador). Se ejecutó también "Descargar y borrar fotos" de punta a punta: tras confirmar con `BORRAR`, la signed URL de la foto pasó a devolver `"Object not found"` (borrada de Storage), `fotos`/`fotoAerea` quedaron en null en la BD, y el proyecto + su tramo siguieron existiendo intactos (confirmado con `attRepo.list` y una consulta directa a `movimientos` para ese `project_id`).

- **PASO 22 — Bug: en Conteo, un cambio de cantidad no se guardaba (21-07-2026, v0.91)**: reportado por el usuario tras un conteo de prueba real (SKU 51024, lote 972478, cambió 13→12 y no pudo guardarlo).
  - **Causa**: `ConteoLineaRow` (`src/modules/inventario/components/Home.tsx`) solo guardaba el cambio vía `onBlur` del `<input>` (o Enter, que llama `.blur()`); no existía ningún botón explícito de guardar. El único aviso en pantalla era el texto "toca fuera del campo para guardarlas" — en varios celulares el teclado numérico no deja "tocar fuera del campo" (no todos tienen una tecla Enter/Done que dispare `blur`, y si el teclado tapa toda la pantalla no queda nada tocable "afuera"). El cambio quedaba solo en el estado local (`draft`) del componente y nunca llegaba a `actualizar_linea_conteo` — sin ningún error en consola ni alerta, porque el problema era que la función `save()` simplemente nunca se llamaba.
  - **Fix**: se agregó un botón "✓" explícito junto al input (deshabilitado si no hay cambios pendientes), que llama a `save()` directamente al tocarlo — ya no depende de que el blur se dispare. Se mantuvo el `onBlur`/Enter existente (sigue funcionando cuando sí se puede tocar afuera). El input también se resalta con borde ámbar mientras hay un cambio sin guardar (antes no había ninguna señal visual por fila, solo un aviso genérico arriba de toda la lista). Se actualizó ese aviso para mencionar el botón ✓.
  - **Verificado en el navegador con la cuenta invitada (jp) contra la BD real**, reproduciendo el caso exacto del usuario: en el conteo abierto de C088, SKU 51024/lote 972478 (sistema: 13), se cambió el valor a 12 y se tocó ✓ — el botón quedó deshabilitado (cambio guardado) y, tras salir y volver a entrar al conteo (recarga desde el servidor), el valor persistido seguía en 12.

- **PASO 23 — Script de alta masiva de usuarios reales desde Excel (21-07-2026)**: el usuario tiene una planilla real de personal con columnas APELLIDO/NOMBRE/RUT/CORREOEMPRESA y preguntó la forma más fácil de cargarlos todos.
  - **`supabase/migrations/0017_profiles_rut.sql`** (nueva, **pendiente de correr en Supabase** — no se ejecuta sola): agrega `profiles.rut` (nullable) y arregla de raíz el permiso que ya se había visto fallar al crear los 3 técnicos de prueba (`grant select, insert, update, delete on public.profiles to service_role`) — sin este grant, un script con la service_role key puede crear la cuenta de Auth pero el `.update()` sobre `profiles` falla en silencio con "permission denied".
  - **`scripts/importar-usuarios.mjs`** (nuevo, generaliza `create-test-tecnicos.mjs`): lee un `.xlsx` con esas 4 columnas (`xlsx-js-style`, ya era dependencia — sin instalar nada nuevo) y crea una cuenta de Auth por fila vía `auth.admin.createUser()`. Decisiones confirmadas con el usuario: nombre se guarda como "Nombre Apellido"; **la contraseña inicial de cada cuenta es su propio RUT** (con guion, sin puntos — ej. `12345678-9`), así no hay que comunicar nada por otro canal — cada persona ya se lo sabe, y ya existe "Cambiar contraseña" en el menú de usuario para que la reemplacen cuando quieran. El `nombre`/`rol` se pasan como `user_metadata` al crear (los toma el trigger `handle_new_user` ya existente, sin depender del `.update()`); solo el `rut` se guarda aparte con un `.update()` (por eso el fix de permiso importa acá). Filas sin correo o sin RUT se omiten con aviso; correos que ya existen se saltan sin frenar el resto del lote.
  - Verificado localmente el parseo (RUT con/sin puntos, con/sin guion, distintas capitalizaciones de encabezado, filas sin correo) contra un `.xlsx` de prueba generado y leído con la misma librería — sin tocar Supabase real (no hay `service_role` key en este entorno; el alta de cuentas la debe correr el usuario en su propia terminal, como con `create-test-tecnicos.mjs`).
  - **Pendiente**: el usuario debe (1) correr la migración 0017 en el SQL editor de Supabase, (2) correr el script con su Excel real y su propia `SUPABASE_SERVICE_ROLE_KEY`.
  - **Actualización (21-07-2026)**: formato de columnas real ajustado a `NOMBRES, APELLIDOPATERNO, APELLIDOMATERNO, RUT, CORREO, TIPO` (separa los dos apellidos en vez de una sola columna APELLIDO; CORREOEMPRESA→CORREO, ROL→TIPO). `nombre` en `profiles` ahora se arma como `"Nombres ApellidoPaterno ApellidoMaterno"`. También acepta el encabezado con el typo "APELLIDOPARTERNO" además de "APELLIDOPATERNO", por si la planilla real lo trae así.

- **PASO 24 — Renombrar roles a Oficina/Terreno/Admin + LOG con los mismos privilegios que JP (21-07-2026, v0.92)**: pedido explícito del usuario, con la pregunta de fondo "¿es posible a estas alturas?" (sí, porque los valores internos `admin`/`jp`/`tecnico`/`log` en BD/RLS **no cambiaron** — solo el nombre que ve el usuario).
  - **Solo se tocó `ROL_LABELS`** (`src/lib/auth.ts`, ya existía centralizado y se usa en todos lados donde se muestra un rol: `UserMenu`, `LogisticaTab`, `AdminScreen`): `jp`→"Oficina", `tecnico`→"Terreno", `admin`→"Admin" (antes "Administrador"), `log` se dejó como "Logística" (el usuario no pidió renombrarlo, solo igualar privilegios). Los valores reales en `profiles.rol`, la RLS, `Rol`/`GuestKind` en TS, etc. siguen siendo `admin`/`jp`/`tecnico`/`log` — renombrar esos identificadores internos habría significado tocar todas las funciones RLS, el `check` de la tabla y cada comparación `rol === '...'` del código, sin ningún beneficio funcional (nadie los ve).
  - **LOG con los mismos privilegios que JP**: `can_move_inventory()` ya incluía a `log` (inventario/conteo), pero `is_jp_or_admin()` no — dejaba a `log` sin poder ver/editar proyectos ajenos, crear proyectos, gestionar `project_members` ni ver todos los perfiles. Fix en `supabase/migrations/0018_log_como_jp.sql` (**pendiente de correr en Supabase**): `is_jp_or_admin()` ahora incluye `log`. Del lado del cliente había dos candados de UI con la misma laguna (permitían admin/jp pero no log) que también se corrigieron: `EstadoProyectoBadge.tsx` (abrir/cerrar proyecto) y `ObservacionesSection.tsx` (borrar observación de cualquiera).
  - **Script de alta masiva actualizado** (`scripts/importar-usuarios.mjs`): ahora acepta una columna `ROL` opcional en el Excel, reconociendo tanto el valor interno como el nombre visible (nuevo y antiguo: Admin/Administrador, Oficina/Jefe de proyecto, Terreno/Técnico, Logística), sin distinguir mayúsculas/tildes. Si viene vacía usa el default (`tecnico`); si viene con algo no reconocido, esa fila se omite con aviso en vez de asignar un rol a ciegas. Probado localmente con un Excel de prueba (Oficina/Terreno/logística/vacío/rol inválido) — todos los casos resolvieron correctamente.
  - **Verificado en el navegador con la cuenta invitada (jp)**: el menú de usuario ahora muestra "Oficina" en vez de "Jefe de proyecto" ("Tipo de usuario: Oficina · cuenta compartida").
  - **Pendiente**: correr `0017_profiles_rut.sql` y `0018_log_como_jp.sql` en el SQL editor de Supabase (ninguna de las dos se ejecuta sola).

- **PASO 25 — Bug: ZIP importado en Preventivos quedaba "sin sincronizar" + limpieza de botones del Editor (21-07-2026, v0.93)**: reportado por el usuario tras importar un levantamiento real desde ZIP.
  - **Causa**: `ImportZip.tsx` solo hacía `upsert(p)` (guarda en el store local, Zustand) y navegaba al Editor — nunca llamaba a `persistToServer`. El autoguardado del Editor (`usePreventivoAutosave`) está diseñado para no disparar en el primer montaje (evita re-guardar de más al simplemente abrir un registro ya sincronizado) — pero eso significa que un levantamiento recién importado, que nunca se sincronizó, **solo se guarda si el usuario edita algo después**. Si no tocaba nada más, quedaba indefinidamente solo local hasta que el banner de "cambios sin sincronizar" de Home (con su botón "Sincronizar ahora") lo detectaba.
  - **Fix de raíz**: `ImportZip.tsx` ahora llama a `persistToServer(p.id)` inmediatamente después del `upsert`, antes de navegar al Editor — el levantamiento importado ya queda guardado en Supabase (fotos subidas + fila persistida) antes de que el usuario vea el Editor.
  - **Además, botón explícito de guardado** (lo que el usuario preguntó: "¿no hay un botón para sincronizar desde dentro?"): la barra inferior del Editor ahora tiene "💾 Guardar cambios" en vez del texto pasivo de estado — el mismo botón muestra guardando/error y permite forzar el guardado en cualquier momento (llama a `retryNow()`, ya existía en el hook mismo, solo faltaba exponerlo como control).
  - **Botones de la barra inferior renombrados/limpiados a pedido del usuario**: `← Volver | 💾 Guardar cambios | 📊 Levantamiento | 📋 Informe | 📦 Guardar ZIP`. `ExportLevButton`/`ExportInformeButton` ya decían "Levantamiento"/"Informe" pero el texto estaba oculto en pantallas chicas (`hidden sm:inline`) — se sacó esa clase para que el texto siempre se vea. `ExportZipButton.tsx` se simplificó fuerte: se eliminó el botón "Compartir ZIP" (Web Share API, nunca funcionó de forma confiable en Chrome — irrelevante ahora que todo es online) y el panel de diagnóstico con los botones "🧪 texto"/"🧪 archivo txt" (eran solo para depurar por qué compartir fallaba — sin uso real para el usuario). Solo queda un botón "Guardar ZIP" que arma y descarga el ZIP.
  - **Verificado en el navegador contra la BD real** (cuenta invitada jp, levantamiento real "Independencia — 1", 19 puntos/34 fotos): la barra inferior renderiza los 4 botones nuevos; "Guardar cambios" ejecuta sin error y vuelve a estado ✅. En mobile (375px) la barra desborda (619px de contenido) pero se dejó `overflow-x-auto` en el contenedor — se puede deslizar en vez de romperse; si se quiere que quepan los 4 botones sin scroll habría que angostar más el texto o los íconos, no se hizo porque no fue parte del pedido.

- **PASO 26 — Cargo/TIPO desde la planilla real de personal + admin ve toda la info + confirmación de aislamiento por rol (21-07-2026, v0.94)**: el usuario pasó su planilla real de personal (`Trabajadores.txt`, **nunca debe llegar al repo** — agregado a `.gitignore` como `Trabajadores*` apenas se detectó sin trackear) con columnas NOMBRES/APELLIDOPATERNO/APELLIDOMATERNO/RUT/CORREO/CARGO/TIPO (TIPO vacía).
  - **`supabase/migrations/0019_profiles_cargo.sql`** (nueva, **pendiente de correr en Supabase**): agrega `profiles.cargo` (nullable, texto libre — el puesto real, ej. "Técnico Ayudante").
  - **Clasificación CARGO → TIPO por palabras clave**, agregada como función reusable `derivarTipoDesdeCargo()` en `scripts/importar-usuarios.mjs` (no un mapeo manual de una sola vez): `logístico→Logística`, `técnico/liniero/empalmador/capataz→Terreno`, `ingeniero/jefe/director/gerente/supervisor/asesor/admin/coordinador→Oficina`. Deliberadamente conservadora: un cargo no reconocido devuelve `null` y cae al default `IMPORTAR_ROL` ('tecnico', el menos privilegiado) — nunca asigna Oficina a ciegas. Verificada contra las 36 filas reales de la planilla: las 36 se clasificaron sin caer al default (`TECNICO LOGISTICO→log`, `ASESOR DE PPRR` y `SUPERVISOR DE PROYECTOS→jp` son los dos casos menos obvios, marcados como juicio propio al usuario).
  - Se generó `Trabajadores_con_tipo.txt` (mismo gitignore, junto al original) con la columna TIPO ya rellenada para que el usuario revise/corrija antes de correr el script — aunque ya no es estrictamente necesario, porque el script ahora auto-deriva el TIPO desde CARGO si viene vacío.
  - **Admin ve toda la info cargada**: `AdminScreen.tsx` (`UserRow`) ahora tiene campos editables de RUT y Cargo, además de nombre/rol/área/activo que ya tenía. `adminRepo.listProfiles()`/`updateProfile()` y `Profile` (`src/lib/auth.ts`) se extendieron con `rut`/`cargo`. El script de importación ahora guarda `cargo` junto con `rut` en el mismo `.update()` posterior a `createUser()`.
  - **Confirmado (sin cambios de código, ya era así por diseño)**: un técnico solo ve su propia cuenta. La RLS `profiles_read` (`0001_init.sql`) es `id = auth.uid() or is_jp_or_admin()` — un técnico nunca cumple `is_jp_or_admin()`, así que a nivel de base de datos directamente no puede leer la fila de nadie más (ni con rut/cargo nuevos ni sin ellos). `AdminScreen` además está gateado a `rol==='admin'` y `LogisticaTab` ya ocultaba `EquipoSection` (la lista de roles del equipo) para técnico desde el PASO 11.
  - **tsc limpio**; verificado el parseo completo contra la planilla real (36/36 filas, sin PII en este documento). **No se pudo verificar `AdminScreen` visualmente en el navegador** — no hay cuenta admin de prueba configurada en este entorno (solo invitados jp/técnico); el cambio replica exactamente el patrón ya probado de los inputs de nombre/área en el mismo archivo.
  - **Pendiente**: correr `0019_profiles_cargo.sql` en Supabase (además de 0017/0018 si aún no se corrieron); revisar `Trabajadores_con_tipo.txt` y correr `importar-usuarios.mjs` con la planilla real.

- **PASO 27 — Bug: `importar-usuarios.mjs` creaba las 36 cuentas reales sin nombre (21-07-2026)**: el usuario corrió el script contra su planilla real y reportó que "se subió todo excepto los nombres".
  - **Causa**: los encabezados reales de la planilla (`NOMBRES TRABAJADOR`, `APELLIDO PATERNO`, `APELLIDO MATERNO` — con espacios) no coinciden con los que se habían asumido al diseñar el script (`NOMBRES`, `APELLIDOPATERNO`/`APELLIDOMATERNO`, pegados, tal como los escribió el usuario en el chat). El resto de las columnas (`RUT`, `CORREO`, `CARGO`, `TIPO`) sí coincidían exactamente, por eso solo el nombre completo quedó vacío en las 36 cuentas ya creadas.
  - **Fix**: `leerFilas()` ahora acepta ambas variantes de cada encabezado (con y sin espacio). Verificado releyendo la planilla real: las 36 filas resuelven `nombreCompleto` correctamente (con Ñ intacta).
  - **Además, el script ahora repara cuentas que ya existen** en vez de solo saltarlas: si `createUser()` falla por "already registered", busca el perfil por correo y le actualiza `nombre`/`rol`/`rut`/`cargo` contra la fila actual. Esto es lo que permite corregir las 36 cuentas ya creadas simplemente **volviendo a correr el mismo comando** — no hace falta borrar nada a mano.
  - **Segundo bug encontrado al reintentar**: el mensaje real de Supabase para correo duplicado es *"A user with this email address has **already been** registered"* — el regex `/already registered|already exists/i` no matcheaba por la palabra "been" en el medio, así que las 36 filas caían al error genérico en vez de entrar a la rama de reparación. Corregido a `/already.*registered|already exists/i`.
  - **Pendiente**: el usuario debe volver a correr `node scripts/importar-usuarios.mjs Trabajadores_con_tipo.txt` con su `SUPABASE_SERVICE_ROLE_KEY` para reparar los 36 perfiles.

- **PASO 28 — "Técnicos asignados" como desplegable + agregar, en vez de checklist completo (22-07-2026, v0.95)**: con los 30+ técnicos reales ya cargados, el checklist de `EquipoSection` (`src/ui/LogisticaTab.tsx`, usado desde la pestaña Logística de cualquier OTT/levantamiento) mostraba a todos con un tick — impráctico a esa escala.
  - **Cambio**: los ya asignados se listan aparte (con botón "×" para quitar); debajo, un `<select>` con los que faltan por asignar + botón "+ Agregar". Mismo patrón que `AgregarLineaForm` (Conteo). No se tocó `adminRepo`/RLS — mismas llamadas `addMember`/`removeMember` de antes, solo cambió cómo se arma la lista en pantalla.
  - **No se tocó `AdminScreen.tsx`** ("Equipo por proyecto"): tiene el mismo patrón de checklist y el mismo problema de escala, pero no fue parte de lo pedido — si se quiere,se puede aplicar el mismo cambio ahí.
  - **Verificado en el navegador contra la BD real** (OTT `_verify_repo2_ott`): agregar "Angelo Campos" vía el desplegable lo movió a la lista de asignados y lo hizo aparecer de inmediato en el selector de "Técnico" de la tabla de Material (confirma que el fix de sincronización del PASO 14 sigue funcionando); se sacó de nuevo para dejar el proyecto de prueba como estaba.
  - **Mismo cambio aplicado también en `AdminScreen.tsx`** ("Equipo por proyecto", v0.96, a pedido explícito) — mismo patrón exacto, sin tocar `adminRepo`. **No se pudo verificar visualmente**: no hay cuenta admin de prueba en este entorno (solo invitados jp/técnico); tsc queda limpio y la lógica es la misma ya probada en `EquipoSection`.

- **PASO 29 — Consumibles (sin flag nuevo) + columna Área en movimientos + fix de bodega por defecto (22-07-2026, v0.97)**: el usuario quería una categoría "Consumibles" (huinchas, amarras, aire comprimido, paños de fibra) entregada a técnicos sin el ciclo completo instalado/devuelto/rebajado SAP. Tras varias rondas de precisión con el usuario:
  - **Los consumibles no necesitaron ningún cambio de esquema en `materiales`**: `registrar_movimiento` (vigente desde `0007_salida_preventiva.sql`) ya permite `entrega` sin proyecto — mueve stock físico igual, simplemente no toca `proyecto_materiales` (nada que instalar/devolver/rebajar porque no hay fila de proyecto). Un consumible es literalmente eso: una `entrega` con proyecto vacío, bodega de origen = una nueva bodega "Consumibles" (la crea el usuario con el "+Nueva bodega…" que ya existe), técnico + **Área** (ver abajo) en vez de proyecto.
  - **`supabase/migrations/0020_movimientos_area.sql`** (nueva, **pendiente de correr**): agrega `movimientos.area` (`'ATT'|'OyM'`, nullable) + backfill (con proyecto → área del proyecto; sin proyecto, todo lo existente hoy es "salida preventiva" → OyM) + `registrar_movimiento` con un `p_area` nuevo (`default null`). Regla confirmada explícitamente por el usuario tras dos rondas de precisión: **el área NO se deriva de la bodega de origen/destino** (puede salir material de cualquier bodega para un trabajo preventivo o consumible) — se elige a mano en el formulario cuando no hay proyecto; con proyecto, se deriva sola de `projects.area` como siempre.
  - **`RegistrarMovimientoForm.tsx`**: nuevo selector "Área (consumibles / salida preventiva)" (ATT/OyM, default OyM — preserva el comportamiento histórico de "salida preventiva"), visible solo cuando `!esEntrada && !requiereProyecto && proyectoIdEfectivo === null`. Mismo formulario sirve para registrar un consumible: Entrega, sin proyecto, Área elegida, bodega=Consumibles.
  - **Columna "Área" en Inventario → Movimientos** (`MOV_COLUMNS`), mismo patrón `ColumnHeader` — base directa para el futuro KPI de consumo mensual por área.
  - **Fix de bodega por defecto en `ResumenProyectoTable`**: al elegir material en una fila nueva de "+ Nuevo material", si la bodega actual de la fila (la del área por defecto, ej. C088) no tiene stock de ese material pero otra sí, la fila salta sola a esa bodega (`getStock` + comparación, ya existía la función, sin endpoint nuevo). Evita registrar una salida de una bodega donde el material nunca estuvo (negativos falsos).
  - **⚠️ IMPORTANTE — orden de despliegue**: como `registrarMovimiento()` ahora manda `p_area` en **todas** las llamadas (no solo consumibles/sin-proyecto), **cualquier registro de movimiento (Entrada, Entrega, Instalado, Devuelto, Rebajado — todos) va a fallar con "Could not find the function..." hasta que se corra `0020_movimientos_area.sql`**. Confirmado en vivo: se intentó una Entrada de prueba contra la BD real antes de correr la migración y tiró exactamente ese error. No es un bug, es la firma vieja de la función en el servidor sin el parámetro nuevo — se resuelve corriendo la migración.
  - **Verificado en el navegador contra la BD real**: selector de Área aparece/desaparece correctamente según haya proyecto elegido o no. El fix de bodega se probó con SKU `84303` (stock real solo en C132, ninguna fila en C088) desde una OTT ATT — la bodega de la fila saltó de C088 a C132 sola. La columna Área en Movimientos ya renderiza (como "—" hasta que se corra la migración y haya movimientos nuevos).
  - **Pendiente**: usuario debe (1) correr `0020_movimientos_area.sql`, (2) crear la bodega "Consumibles" cuando quiera empezar a usarla, (3) bajar el stock de los 3 SKUs acumulados vía Conteo (no vía código, decisión ya tomada).

- **PASO 30 — Módulo Incidencias (22-07-2026, v0.98)**: penúltimo módulo del roadmap acordado (Panel KPI → Estado de pago por OTT → **Incidencias** → carga de materiales reales, y después producción). Objetivo explícito del usuario: solo anotar materiales usados en un incidente OyM — **no genera informes**. 4 pestañas: Información (Ingeniero, Código *, Dirección), Materiales, Fotos, Comentarios.
  - **Casi todo reusado sin código nuevo**: Materiales → `LogisticaTab` (`src/ui/LogisticaTab.tsx`), Comentarios → `ObservacionesSection`, ciclo activo/cerrado → `EstadoProyectoBadge`/`closeProject`/`reopenProject` — los tres ya eran 100% genéricos por `project_id`, sin ninguna dependencia de ATT/Preventivos.
  - **Hallazgo real que cambió el diseño**: `projects.area` solo admite `'ATT'|'OyM'` y `preventivoRepo.list()` filtraba solo por `area='OyM'` — si Incidencias hubiera usado esa misma área sin más, sus filas se habrían mezclado con Preventivos ahí (constructor incorrecto de un Preventivo vacío por cada Incidencia). El usuario, al revisar el plan, pidió explícitamente dejar **Incidencias y Preventivos como subáreas de OyM**: se agregó `projects.subarea` (nullable, `'preventivo'|'incidencia'`, con backfill de todo lo existente en OyM a `'preventivo'`) — `area` sigue siendo el área real (la que usa `movimientos.area` del PASO 29, sin tocar). `preventivoRepo.list()` ahora filtra también `subarea='preventivo'`.
  - **`supabase/migrations/0021_incidencias.sql`** (nueva, **pendiente de correr**): `projects.direccion` + `projects.subarea` (+ backfill) + tabla `incidencia_fotos` (project_id directo, sin tabla "informe" intermedia — Información entera vive en columnas de `projects`: `ott`→código, `ingeniero_proyecto`→ingeniero, `direccion`→dirección) con RLS igual a `informes`.
  - **Nuevo módulo `src/modules/incidencias/`**, estructura mínima espejo de `att/` sin tramos/hitos/infraestructura: `types.ts`, `data/incidenciaRepo.ts` (sin tabla "informe"), `data/photoStorage.ts` (prefijo `incidencias/`), `store.ts` (online-first, mismo patrón), hooks de autoguardado/fotos (con el fix del PASO 25 aplicado desde el día 1 — no solo mount-skip), `components/Home.tsx`, `components/Editor.tsx` (4 tabs), `components/SeccionInformacion.tsx`, `components/SeccionFotos.tsx` (galería simple, sin categorías), `index.tsx`.
  - **Dos correcciones del usuario durante la revisión del plan, ya incorporadas**:
    1. **Técnico no debe tocar Información** — `SeccionInformacion.tsx` la muestra de solo lectura (texto plano) para `rol==='tecnico'`, editable para jp/admin/log (mismo espíritu que `EstadoProyectoBadge`). Quien crea la incidencia (Oficina/Admin — la RLS genérica `projects_write`/`is_jp_or_admin()` ya cubre esto, sin cambios) llena el código; el técnico solo aporta materiales/fotos/comentarios una vez asignado.
    2. **"Descargar cerradas"** (`utils/exportClosedIncidencias.ts` + `components/DescargarCerradosPanel.tsx`): mismo patrón del PASO 21 (Descargar / Descargar y borrar fotos con candado "BORRAR"), sin generador de informe (el ZIP lleva fotos + `datos.json` nada más) — el usuario lo pidió pensando en que las fotos de incidentes también se acumulan en Storage con el tiempo.
  - **Wiring**: `App.tsx` (`import '@/modules/incidencias'`), `ProjectHome.tsx` (se quitó el placeholder "EN PROCESO" hardcodeado), `AsignacionesScreen.tsx` (se sumó `incidenciaRepo.list()` al merge de asignaciones del técnico).
  - **tsc limpio en el primer intento** pese al tamaño del módulo. **Verificado en el navegador contra la BD real** (invitado jp): "Incidencias" ya aparece como módulo activo (sin overlay EN PROCESO) en el Home; el Editor abre con las 4 pestañas y los 3 campos de Información editables; al escribir un código y esperar el autoguardado, salió el error esperado — `Could not find the 'direccion' column of 'projects'` — confirmando que todo el flujo llega correctamente a Supabase y que **falta correr la migración 0021** para que funcione de verdad (mismo patrón de "cambio de código antes que la migración" del PASO 29). El borrador de prueba nunca llegó a persistir en el servidor (todos los inserts fallaron pre-migración) — no quedó nada que limpiar en la BD real, se limpió el borrador local.
  - **0021_incidencias.sql corrida con éxito** (con un reintento — la primera vez dio `40P01 deadlock detected`, un choque de timing transitorio sin relación con el SQL en sí; nada quedó a medias, el reintento completo funcionó).
  - **Bug real encontrado al verificar y corregido**: `LogisticaTab` (`src/ui/LogisticaTab.tsx`) ya incluye `ObservacionesSection` por dentro (así lo usan ATT/Preventivos, que no tienen pestaña "Comentarios" separada) — al reusarlo tal cual para "Materiales", Incidencias mostraba los comentarios duplicados (una vez dentro de Materiales, otra en su propia pestaña Comentarios). Se agregó `incluirComentarios?: boolean` (default `true`, ATT/Preventivos sin cambios) y se pasa `false` desde el Editor de Incidencias.
  - **Verificado de punta a punta en el navegador contra la BD real** (invitado jp): Preventivos ("Independencia — 1") sigue intacto tras el backfill de `subarea`. Se creó `INC-VERIFY-001` con autoguardado exitoso (sin error esta vez); se asignó un técnico (Ignacio Pérez, rol Logística) y se registró una Entrega real de 3 unidades del material de prueba 999905 en C132 — quedó reflejado correctamente en la tabla (`Tránsito: 3 → preventivo`); se agregó y confirmó un comentario, sin duplicado en Materiales; se abrió/cerró/reabrió con el badge de estado; la pestaña Fotos renderiza sin errores. Se dejó el registro de prueba (`INC-VERIFY-001`) sin borrar, mismo criterio que el resto de datos `_verify_*`/`TEST-*` de esta sesión.
  - **No se pudo verificar en vivo que el técnico ve "Información" de solo lectura**: la cuenta invitada técnico (`iperez@sinterk.cl`) cambió de rol tras la carga masiva de personal (PASO 27) — ese correo pertenece a un empleado real con cargo "Técnico Logístico" → rol Logística ahora, no Técnico, así que el guest ya no demuestra la experiencia técnico. Queda verificado a nivel de código (mismo patrón ya probado de `EstadoProyectoBadge`), pendiente de confirmar con una cuenta técnico real cuando se pruebe con el equipo.

- **PASO 31 — Merma + Panel de KPIs (23-07-2026, v0.99)**: penúltimo hito antes de producción (queda "Estado de pago por OTT" y carga de materiales reales). Spec del usuario, resuelta con dos rondas de `AskUserQuestion` antes de planificar: periodo (mes de los últimos 12 + desde/hasta siempre visibles y editables), área (ATT/OyM/Inventario), ATT medido por OTT abiertas/cerradas/pendientes, OyM por Preventivos e Incidencias como mini-ventanas separadas, tabla de material con 7 columnas (Solicitado/Entregado/Instalado/Devuelto/**Rebajado**/**Merma**/Tránsito, las dos en negrita confirmadas explícitamente por el usuario), Consumibles al final solo con Entregado, vista "solo inventario" con filtro de bodega ("todas" incluida) y técnicos (multi-select, mismo patrón que asignación de técnicos del PASO 28).
  - **Merma es un hallazgo del usuario a mitad de las preguntas, no estaba en el esquema**: material físico desechado (ej. despunte de cable sobrante) — distinto de "Rebajado" (baja contable SAP, nunca toca stock físico). Se agregó como tipo real de movimiento, no solo como cálculo del KPI, con el mismo comportamiento de stock que "Instalado" (sale de la ubicación personal del técnico, sin bodega que elegir).
  - **`supabase/migrations/0022_merma.sql`**: `proyecto_materiales.cant_merma`, nuevo valor `'merma'` en el check `movimientos_tipo_check`, whitelist de `adjust_proyecto_material`/`corregir_proyecto_material` ampliada, y rama `elsif p_tipo_ui = 'merma'` en `registrar_movimiento` (copia+extensión de la versión de `0020_movimientos_area.sql`, no una edición in-place — los migrations siempre reemplazan la función completa). En el cliente, Merma se agregó en los mismos puntos donde ya vivían Instalado/Devuelto/Rebajado como lista fija: `types.ts`, `inventarioRepo.ts` (`getResumenProyecto` y `getTecnicoLedger`, cada uno con su propia fórmula de tránsito preexistente e inconsistente entre sí — se les sumó `- cantMerma` a ambas tal cual, sin unificar la inconsistencia ya existente, fuera de alcance), `ResumenProyectoTable.tsx` (arquitectura ya genérica por `CAMPOS.map()`, solo hubo que sumar `'cantMerma'` a los arrays/records y un `<th>` nuevo), `RegistrarMovimientoForm.tsx`, y las pestañas Técnico/Movimientos de `src/modules/inventario/components/Home.tsx` (también genéricas por `.map()` sobre columnas).
  - **`supabase/migrations/0023_kpi_rpcs.sql`**: dos funciones `security definer` (`kpi_proyectos`, `kpi_materiales`) que autorizan ellas mismas con `is_jp_or_admin()` — necesario porque, a diferencia de la RLS normal, agregan sobre TODO el histórico sin las restricciones de fila de un técnico. `kpi_proyectos` cuenta `projects` por `created_at`/`fecha_cierre`/`estado` dentro de área+subarea. `kpi_materiales` agrega `movimientos` por tipo: las 6 columnas de flujo suman cantidad con `fecha` dentro del rango elegido; **Tránsito es una foto, no un flujo** — se calcula con `fecha <= hasta` sin piso en `desde` (saldo acumulado real a esa fecha, incluye arrastre de periodos previos). Un material es "consumible" (va en su propia sección al final, solo con Entregado) si alguno de sus movimientos salió de la bodega literal "Consumibles" — mismo criterio ya usado en el resto de la app, sin columna/flag nueva en `materiales`. `p_area` acepta `null` para la vista "solo inventario" (todas las áreas combinadas, sin proyecto de por medio) — las llamadas por área (ATT/OyM) sí lo fijan.
  - **Nueva carpeta `src/ui/kpi/`** (`KpiScreen.tsx`, `KpiProyectosPanel.tsx`, `KpiMaterialesTable.tsx`) + `src/lib/kpi/kpiRepo.ts` (wrappers tipados de las dos RPC). Ruta `/kpi` nueva en `App.tsx`, link "📊 KPI" en `UserMenu.tsx` (gateado a admin/jp/log, a diferencia de "⚙️ Administración" que es solo admin), y `/kpi` sumado a `homeScreens` en `Layout.tsx` para que el nav inferior no desaparezca ahí (mismo patrón ya usado para `/admin`).
  - **Diseño de la "ventana Inventario"**: el mismo componente `KpiMaterialesTable` se reusa tres veces con distinto scope — sin filtro de bodega/técnico para "Material por proyecto" (cubre toda la actividad del área, incluidos Consumibles sin proyecto), con `ubicacionId` fijo a C088/C132 para la mini-ventana "Inventario" dentro de las vistas ATT/OyM, y con `ubicacionId`/`tecnicoIds` elegibles por el usuario (incluida la opción "Todas" bodegas y `area=null`, todas las áreas) cuando se selecciona el área "Inventario" standalone.
  - **`npx tsc --noEmit` limpio en los tres intentos** (Fase 1 Merma, Fase 2 RPCs no aplica a TS, Fase 3 pantalla). **Verificado en el navegador contra la BD real** (invitado jp), primero con las funciones RPC sin correr (error esperado `Could not find the function public.kpi_proyectos...` — igual patrón "código antes que migración" del PASO 29/30, sin crashear la UI), después de que el usuario corrió `0022`/`0023`: vista ATT muestra 9 OTT abiertas/2 cerradas/7 pendientes y tabla de material con datos reales; vista OyM muestra Preventivos e Incidencias como mini-ventanas separadas (4/3/1 y 1/0/1); se registró una Merma real de 1 unidad sobre el material de prueba 999905 en `INC-VERIFY-001` (Tránsito bajó de 3→2 en la tabla de la incidencia) y el Panel KPI reflejó exactamente ese cambio (Merma: 1, Tránsito: 2 en "Material por proyecto"); la mini-ventana "Inventario (C132)" correctamente mostró Merma: 0 para ese material (la Merma sale de la ubicación propia del técnico, no de la bodega — mismo comportamiento ya esperado de "Instalado"); la vista "Inventario" standalone mostró el catálogo combinado ATT+OyM y el filtro de bodega (C132) redujo correctamente la tabla a un solo material.

- **PASO 32 — Material por punto en Preventivos + prioridad de origen al instalar (23-07-2026, v1.00)**: el usuario pidió volver a un diseño discutido antes (material y comentarios embebidos en la tarjeta de cada punto, no solo vía el selector de punto de la tabla general) y generalizar la fuente del material al marcar "Instalado": 1) el propio proyecto/técnico, 2) si no alcanza, el equipo asignado, 3) si nadie alcanza, forzar en negativo y marcarlo para revisión — confirmado explícitamente que aplica a **toda** la app, no solo a Preventivos.
  - **Bug crítico encontrado antes de poder construir nada de esto**: `preventivoRepo.save()` llamaba SIEMPRE a `replacePuntos()`, que hacía `delete` + `insert` de **todos** los puntos del cuadrante en cada guardado (incluido cada autoguardado) — el `insert` no mandaba el `id` local, así que Postgres asignaba un `gen_random_uuid()` nuevo cada vez. Como `movimientos.punto_id` ya existía desde PASO 6 con `on delete set null`, cualquier movimiento ya asociado a un punto perdía esa asociación en el siguiente autoguardado, silenciosamente. **Fase 0 (prerrequisito)**: `addPunto` (`store.ts`) pasa a generar el id con `crypto.randomUUID()` en vez de `nanoid()`; `replacePuntos` pasa a upsert real por id (solo borra los puntos que el usuario quitó de verdad).
    - **Bug propio introducido y corregido en el camino**: al implementar el upsert, dejé la llamada final como `supabase.from('puntos').insert(rows)` en vez de `.upsert(rows)` — silenciosamente seguía siendo un insert puro, así que el primer guardado real tiró `duplicate key value violates unique constraint "puntos_pkey"` (el punto ya existía con ese id, por venir del fix mismo). Se detectó en la verificación en vivo (no en `tsc`, que no lo puede pescar) y se corrigió a `.upsert(rows)`. Re-verificado: guardado real sin error, confirmado con lectura directa por REST a `puntos`/`projects` que el id del punto no cambia entre dos guardados seguidos.
  - **Fase 1 — Comentarios por punto**: `observaciones.punto_id` nuevo (nullable, `on delete set null`) — reusa la misma tabla/RLS de siempre. `listObservaciones`/`agregarObservacion` ganan un segundo parámetro `puntoId` (default `null` = comentarios generales, comportamiento intacto). `ObservacionesSection.tsx` gana props `puntoId`/`titulo`/`compact`, se embebe dentro de `PuntoCard.tsx` como "Comentarios del punto".
  - **Fase 2 — Material por punto**: nuevo `PuntoMaterialSection.tsx`, embebido en `PuntoCard.tsx` (guardado tras `isUuid(preventivoId)`, mismo guard que Incidencias). Lista lo ya instalado en ESE punto reusando `getResumenProyecto` (ya traía `puntoId` por fila, solo se filtra client-side) y agrega con `registrarMovimiento({ tipoUI: 'instalado', puntoId, ... })` — mismo camino de siempre, sin RPC nueva. El usuario pidió que el técnico se elija con un `<select>` (no fijo al usuario logueado), por defecto el propio si es miembro del proyecto.
  - **Fase 3 — Tabla general de Logística**: `ResumenProyectoTable.tsx` gana un filtro "Punto: Todos ▾" (solo Preventivos). Por defecto agrega todas las filas de un material+lote sin importar el punto (`agregarPorMaterial`) — Instalado ahí pasa a ser un total **no editable/no corregible** (no hay a qué punto atribuirlo). Al elegir un punto específico, filtra a solo sus filas y ahí Instalado vuelve a ser editable/corregible (mismo resultado que el "+" de la tarjeta del punto). Se sacó el modo "🔧 Corregir errores de tipeo" de la vista agregada (correguir un total no tiene un único registro al que aplicarle el valor absoluto) y se apagaba solo si estaba activo al volver a "Todos".
  - **Fase 4 — Movimientos**: confirmado sin cambios, la pestaña ya solo mostraba el proyecto/cuadrante (`m.projectOtt`), nunca el punto.
  - **Fase 5 — Prioridad de origen al instalar (`0025_prioridad_instalado.sql`)**: `adjust_stock` gana `p_permitir_negativo` (se salta el chequeo de stock insuficiente, solo lo usa el paso 3 de abajo). La rama `instalado` de `registrar_movimiento` ahora prueba, en orden: 1) stock propio del técnico atribuido, 2) el compañero de equipo (`project_members`) con más stock que alcance completo, 3) si nadie alcanza, fuerza en el atribuido permitiendo negativo y marca `movimientos.requiere_revision = true` + abre una fila en `eventos_inventario` (mismo flujo/UI de resolución que las discrepancias de conteo, en Conteo → Eventos). Aplica a **todo** `tipoUI='instalado'` en la app (el "+" del punto, la tabla general, y ATT), porque todos pasan por la misma función.
  - **Verificado de punta a punta en el navegador Y por REST directo contra la BD real** (invitado jp), sobre el cuadrante real "Independencia — 1": confirmado con dos guardados reales seguidos (editar comuna, esperar autoguardado, releer por REST) que el id del punto P01 no cambia; se agregó material 999903 (sin stock en ningún técnico del proyecto, a propósito) desde la tarjeta del Punto 1 — apareció el aviso "⚠️ Sin stock suficiente..." y se confirmó por REST: `eventos_inventario` con `diferencia: -1, estado: 'abierto'` y `movimientos.requiere_revision: true` en la fila correcta (`punto_id` = P01); la tabla general en "Todos" mostró Instalado=1 no editable y Tránsito=-1 (0 entregado − 1 instalado); filtrando a "Punto: P01" el mismo Instalado volvió a ser editable; el evento apareció correctamente en Conteo → Eventos de la ventana Inventario con el material/técnico/diferencia exactos. Comentarios por punto verificados sin error tras la migración ("Sin observaciones todavía." en vez del error de columna faltante).

- **PASO 33 — Bodega "Consumibles" renombrada a "Insumos" (23-07-2026, v1.01)**: pedido simple del usuario tras revisar el Panel KPI. `supabase/migrations/0026_insumos.sql`: `update ubicaciones set nombre='Insumos' where nombre='Consumibles'` + `kpi_materiales` reescrita con el nuevo nombre (el único lugar del código que comparaba por el nombre literal de la bodega, ver PASO 31). Se barrieron también los comentarios/etiquetas de UI que decían "consumibles" (`ResumenProyectoTable.tsx`, `RegistrarMovimientoForm.tsx`, `types.ts`, `KpiScreen.tsx`, `KpiMaterialesTable.tsx`) — no se tocaron los nombres internos de campo (`esConsumible`/`es_consumible`, ya retirados de todas formas en el PASO 34). Verificado en el navegador y por la BD real: la bodega aparece como "Insumos" en todos los `<select>` de Inventario/Logística, y el KPI la sigue detectando bien.

- **PASO 34 — Panel de KPIs: rediseño de proyectos y materiales (24-07-2026, v1.02)**: cuatro pedidos de ajuste tras usar el panel: (1) tabla de detalle de proyectos, no solo 3 números; (2) bug real — un mes sin actividad mostraba los mismos SKU con ceros; (3) fusión de "Material por proyecto"/"Inventario (bodega)" (mostraban lo mismo) con columnas nuevas Origen/Físico/Digital, e Insumos como tabla aparte; (4) Inventario "por bodega" (ahora multi-selección) o "por técnico" como modos excluyentes, no ambos filtros a la vez.
  - **Hallazgo técnico importante, confirmado con el usuario antes de diseñar**: `stock` es un saldo VIVO (no hay historial por fecha), y los movimientos de traslado/entrega solo graban una punta de la operación — reconstruir el saldo exacto de un mes pasado no es posible sin un cambio de esquema mayor. Se resolvió: Físico/Digital solo se calculan y muestran cuando `hasta` es exactamente hoy (el cliente decide si pide esas columnas; si no, ni se calculan).
  - **`supabase/migrations/0027_kpi_v2.sql`**: `kpi_proyectos` (solo contaba) se reemplaza por `kpi_proyectos_detalle` (una fila por proyecto con `estado` calculado — `cerrado` si se cerró dentro del rango aunque se haya abierto antes, si no `abierto` si se creó dentro del rango, si no `pendiente` si se creó antes y sigue sin cerrar — mutuamente excluyente, en ese orden de prioridad); los 3 números del resumen se cuentan en el cliente desde esas mismas filas, una sola definición. `kpi_materiales` reescrita: `p_ubicacion_ids uuid[]` (antes un solo id), más `p_excluir_ubicacion_ids` (para sacar Insumos de las tablas "por proyecto"), `p_stock_ubicacion_ids` (Físico/Digital), `p_bodega_defecto` (nombre a excluir de `origen_bodega`, así esa columna solo se llena cuando el material vino de una bodega distinta a la esperada). El bug del mes vacío se arregló armando la lista de materiales relevantes como la UNIÓN de "tuvo movimiento" y "tiene stock pedido" (nunca todo el catálogo), y descartando al final las filas donde absolutamente todo da 0/null.
  - **Bug propio encontrado en la verificación en vivo (no lo pesca `tsc` ni una revisión de sintaxis)**: en una función plpgsql con `returns table(...)`, cualquier referencia SIN calificar a un nombre igual al de una columna de salida (ej. `ott`, `material_id`) es ambigua para Postgres — apenas until se corrió contra la BD real salieron `column reference "ott" is ambiguous` y `"material_id" is ambiguous`. Se corrigió en `supabase/migrations/0028_kpi_v2_fix.sql` calificando cada CTE con alias explícitos (`c_ott`, `mr_material_id`, etc.) en vez de nombres bare.
  - **`KpiProyectosPanel.tsx`**: ahora pide `kpi_proyectos_detalle`, cuenta los 3 números por `estado`, y agrega una tabla (Proyecto/Estado/Fecha de inicio) ordenada Abierto→Pendiente→Cerrado. **`KpiMaterialesTable.tsx`**: `ubicacionId`→`ubicacionIds[]`, nuevos props `excluirUbicacionIds`/`stockUbicacionIds`/`bodegaDefecto`/`mostrarOrigenTecnico`/`columnas` (`'completas'|'soloEntregado'`, esta última la usa la tabla de Insumos); se retira el flag `esConsumible`/la sub-sección "Insumos" (ya no hace falta, Insumos es su propia tabla ahora). **`KpiScreen.tsx`**: ATT pasa a 1 tabla fusionada ("Material usado en proyectos", con Físico/Digital de C088) + 1 tabla Insumos (se quita el viejo panel "Inventario (C088)" duplicado); OyM pasa a 4 tablas (Todo OyM con Físico/Digital de C132, Preventivos, Incidencias, Insumos); Inventario gana un selector "Por bodega | Por técnico" excluyente — bodega ahora multi-selección (mismo patrón de chips que técnicos).
  - **Verificado de punta a punta en el navegador contra la BD real** (invitado jp): un mes sin actividad (agosto 2025) muestra las 3 tablas completamente vacías (antes repetía los mismos SKU con ceros); con `hasta`=hoy aparecen Físico/Digital (incluido un material con stock real pero cero actividad del mes, antes invisible); con un mes pasado desaparecen; la tabla de proyectos mostró 🟢Abierto/⚪Cerrado en el orden correcto con proyectos reales; Origen se rellenó correctamente con el nombre de la bodega cuando el material salió de una distinta a la esperada (`C103` en vez de `—`), y con nombres de técnico real en modo "Por técnico" (incluido un caso con dos técnicos separados por coma); los modos Por bodega/Por técnico se comprobaron excluyentes.

- **PASO 35 — Traspaso entre bodegas (29-07-2026, v1.05)**: pedido explícito de Andrés tras aclarar que Entradas/Salidas siempre exigía un técnico ("útil cuando se quiere cuadrar el material que se sacó de una bodega a otra"). Hasta ahora no existía ningún camino bodega→bodega directo: Entrada es externo→bodega, Entrega es bodega→técnico, Devuelto es técnico→bodega — ninguno mueve stock entre dos bodegas sin pasar por una persona.
  - **`supabase/migrations/0037_traslado_bodega.sql`** (nueva, **pendiente de correr en Supabase**): agrega `movimientos.ubicacion_destino_id` (nullable, FK a `ubicaciones`) — necesaria porque, a diferencia de Entrega/Devuelto (que registran el otro extremo vía `usuario_id`, el técnico), un traspaso no tiene técnico y ambos extremos son bodegas; ninguna columna existente podía guardar la segunda. Nuevo valor de `movimientos.tipo`: `'traslado_bodega'` — deliberadamente **distinto** del valor `'traslado'` ya existente (que significa "Devuelto", técnico→bodega, y que el Panel de KPI ya usa para la columna Devuelto filtrando `tipo='traslado'`); reusarlo habría inflado esa columna con movimientos que no son devoluciones de técnico. `registrar_movimiento` gana un parámetro nuevo al final (`p_ubicacion_bodega_destino_id`, compatible con todos los llamadores existentes que no lo mandan) y una rama nueva: exige bodega origen y destino distintas, sin proyecto ni técnico, mueve stock físico con dos `adjust_stock` (resta en origen, suma en destino).
  - **Cliente**: `MovimientoTipoUI`/`RegistrarMovimientoInput`/`Movimiento` (`src/lib/inventario/types.ts`) y `inventarioRepo.ts` ganan el campo nuevo; `listMovimientos` tuvo que pasar de `ubicaciones(nombre)` a un embed con hint por columna (`origen:ubicaciones!ubicacion_id(nombre)`, `destino:ubicaciones!ubicacion_destino_id(nombre)`) porque `movimientos` ahora tiene DOS FK hacia `ubicaciones` — PostgREST necesita el hint para no ambigüar cuál usar. `RegistrarMovimientoForm.tsx`: nuevo tipo "Traspaso entre bodegas" en el selector de Tipo, oculta Proyecto/Área/Técnico (no aplican) y agrega un segundo selector de bodega ("Bodega de destino…") por línea de material, junto al ya existente de origen. `Home.tsx` (pestaña Movimientos): la columna Bodega muestra "origen → destino" cuando corresponde.
  - **Verificado**: `tsc --noEmit` limpio. En el navegador contra la BD real (invitado jp): al elegir "Traspaso entre bodegas" en el formulario, Proyecto/Área/Técnico desaparecen y aparecen ambos selectores de bodega (origen y destino) — confirmado visualmente. Se completó un traspaso real (material 6, C088→C132, cantidad 1) y al enviar salió el error esperado `Could not find the function public.registrar_movimiento(...p_ubicacion_bodega_destino_id...)` — confirma que el cliente arma y manda el llamado RPC completo y correcto (mismo patrón de "código antes que migración" ya visto en PASOs anteriores); no quedó nada que limpiar en la BD (el insert nunca llegó a ejecutarse).
  - **29-07-2026, tras correr 0037 — dos bugs reales de sobrecarga de funciones encontrados al verificar de nuevo**, ninguno introducido por 0037 (ambos preexistían, 0037 solo forzó un recompilado fresco que los hizo visibles): (1) `registrar_movimiento` tenía 3 versiones sobrecargadas conviviendo en la BD (0005/0007 sin `p_area`, 0020/0022/0025 con `p_area`, y la de 0037) porque ninguna migración anterior dropeaba la firma vieja al agregar un parámetro — confirmado con un script de verificación que llamaba con un subconjunto de parámetros y recibió `PGRST203 Could not choose the best candidate function`. (2) Un nivel más adentro: `adjust_stock` tiene el mismo problema desde `0025_prioridad_instalado.sql` (agregó `p_permitir_negativo`, de 5 a 6 parámetros, sin dropear la de 5) — **esto afecta a TODOS los tipos de movimiento, no solo al traspaso nuevo**, porque toda rama de `registrar_movimiento` llama a `adjust_stock` con 5 argumentos posicionales; confirmado en vivo con un intento real de Entrada que falló con `42725 function public.adjust_stock(uuid, uuid, text, numeric, integer) is not unique`. Se revisaron todas las demás funciones con múltiples `create or replace` (`adjust_proyecto_material`, `corregir_proyecto_material`, `agregar_linea_conteo`, `cerrar_conteo`, `importar_lineas_conteo`, `reasignar_transito_a_preventivo`) y ninguna cambió de firma entre versiones — el problema es exclusivo de `registrar_movimiento`/`adjust_stock`.
  - **`supabase/migrations/0038_registrar_movimiento_overloads_fix.sql`**: dropea las 2 firmas viejas de `registrar_movimiento` y la firma vieja de 5 parámetros de `adjust_stock`, dejando una sola versión de cada una. Ambos intentos de prueba previos (Entrada y el llamado ambiguo) habían fallado limpio dentro de la transacción de Postgres — se confirmó por REST que no quedó ninguna fila de `stock` ni `movimientos` a medias en la bodega de prueba usada. **Corrida por Andrés el mismo día.**
  - **Verificación final, de punta a punta contra la BD real, tras correr 0037 y 0038**: Entrada de 5 unidades del material de prueba 999901 a la bodega de prueba `TEST-IMPORT-VELOCIDAD` (creada para esta verificación, junto con `TEST-TRASPASO-DESTINO`) → OK. Traspaso de 2 unidades `TEST-IMPORT-VELOCIDAD → TEST-TRASPASO-DESTINO` → OK, fila de `movimientos` con `tipo='traslado_bodega'` y `ubicacion_destino_id` correcto. Stock verificado por REST: origen quedó en 3 (5-2), destino en 2 (0+2) — exacto. El embed disambiguado de `listMovimientos` (`origen:ubicaciones!ubicacion_id`, `destino:ubicaciones!ubicacion_destino_id`) resuelve bien (probado por REST con el mismo `select` que usa el cliente). **Confirmado visualmente en la pestaña Movimientos**: la fila muestra "TEST-IMPORT-VELOCIDAD → TEST-TRASPASO-DESTINO" en la columna Bodega, tal como se diseñó. Sin errores de consola. Quedan la bodega `TEST-TRASPASO-DESTINO` y el material 999901 con stock de prueba en ambas bodegas de prueba — mismo criterio que el resto de datos `TEST-*`/`_verify_*` de esta rama, a limpiar junto con el resto antes del cutover (ver punto 16 de "Próximos pasos").

- **PASO 36 — Rediseño de resolución de eventos de Conteo (29-07-2026, v1.06)**: pedido explícito de Andrés tras revisar cómo funcionaba antes (ver PASO 35 más arriba para el contexto de "Traspaso entre bodegas", que motivó la pregunta). El modelo viejo (`resolver_evento_inventario`, 3 etiquetas fijas: devolucion_pendiente/reubicacion/perdida) solo guardaba "por qué" — sin efecto real, y sin forma de ver eventos ya resueltos en ningún lado de la app.
  - **Diseño acordado con 3 rondas de `AskUserQuestion` antes de tocar código** (dado el riesgo de descuadrar stock): (1) un evento se puede resolver en **varias partes** — de una diferencia de 10, resolver 6 ahora y dejar 4 pendientes; (2) cuando "Consumo" se atribuye a un proyecto, debe sumar de verdad a `proyecto_materiales.cant_instalada` de ese proyecto (aparece en su Logística) y generar un `movimientos` con tipo='instalado' (aparece en el Panel de KPI) — no solo quedar como dato del evento; (3) el técnico es opcional en ese caso (no siempre se sabe quién lo usó).
  - **3 formas de resolver, cada una con efecto real** (antes ninguna tenía): **Consumo** (para faltantes) — Área (OTT/INC/Preventivo/Pérdida) + Proyecto si no es Pérdida; **Devolución** (para sobrantes) — origen (técnico o bodega) al que se le resta la cantidad; **Traspaso** (para faltantes, alternativa a Consumo) — destino (técnico o bodega) al que se le suma la cantidad, sin volver a tocar la bodega del conteo (ya quedó correcta al cerrarlo).
  - **`supabase/migrations/0039_conteo_resolucion_rediseno.sql`** (nueva, **pendiente de correr**): `eventos_inventario.cantidad_resuelta` (acumula lo resuelto; el evento pasa a `resuelto` cuando iguala `abs(diferencia)`); tabla nueva `eventos_inventario_resoluciones` (una fila por resolución parcial: tipo, cantidad, area, project_id, tecnico_user_id, ubicacion_id, nota); `movimientos.evento_resolucion_id` (nullable, traza cada movimiento generado por una resolución). Función nueva `resolver_evento_parcial` (reemplaza `resolver_evento_inventario`, que se dropea) — valida que la cantidad no supere lo restante, que Consumo/Traspaso solo apliquen a faltantes y Devolución solo a sobrantes, y que el origen/destino sea exactamente uno (técnico o bodega, nunca ambos ni ninguno). Reutiliza los helpers ya existentes (`adjust_stock` con `p_permitir_negativo=true` para restar de un origen que puede no alcanzar, `adjust_proyecto_material`, `ensure_ubicacion_tecnico`) — sin duplicar lógica de negocio.
  - **Bug propio encontrado y corregido antes de correr nada**: al escribir la función de un tirón, 3 de los 5 `insert into movimientos` quedaron con `material_id` faltante en la lista de valores (columnas y valores desalineados) — detectado en una relectura línea por línea antes de pedir que se corriera la migración, no en producción.
  - **UI**: `src/modules/inventario/components/Home.tsx` — el banner de eventos pendientes del Home de Conteo (`EventosAbiertosSection`) pasa a ser de solo lectura (ya no resuelve inline) y ahora muestra la fecha de cada evento. Al entrar a un conteo específico (`ConteoDetail`), arriba aparecen **todos** los eventos de ESE conteo, abiertos y resueltos (`listEventosPorConteo`, nuevo — filtra por `conteo_lineas.conteo_id` con un embed `!inner`), cada uno con su estado (Pendiente/Parcial/Resuelto), el historial de resoluciones ya aplicadas, y el formulario para resolver (parcialmente) lo que falta. Debajo, la tabla de líneas del conteo — **la lista de tarjetas se reemplazó por una tabla con `ColumnHeader`** (mismo patrón que Bodega/KPI), pero **solo en este detalle de un conteo particular**, tal como se pidió; "Contado" sigue editable con el mismo botón ✓ de antes.
  - **`adminRepo.ProjectSummary` ganó `subarea`** (nullable) — hacía falta para poder filtrar los proyectos de "Consumo" por Incidencia vs. Preventivo dentro de OyM (ambos comparten `area='OyM'`); cambio aditivo, no afecta a los llamadores existentes (`AdminScreen`, `RegistrarMovimientoForm`) que no lo usan.
  - **Verificado**: `tsc --noEmit` limpio a la primera. En el navegador, con la migración aún sin correr, la pantalla de Conteo intenta la consulta nueva (con el embed a `eventos_inventario_resoluciones`) y falla limpio con `Could not find a relationship between 'eventos_inventario' and 'eventos_inventario_resoluciones' in the schema cache` — confirma que el cliente llega bien a Supabase con la forma correcta, mismo patrón de siempre.
  - **29-07-2026, tras correr 0039 — verificación completa de punta a punta contra la BD real** (bodegas de prueba `TEST-IMPORT-VELOCIDAD`/`TEST-TRASPASO-DESTINO`, material de prueba 999901): (1) un evento faltante (diferencia −2) se resolvió en **dos partes** — 1 unidad vía Consumo→proyecto ATT real (`TEST-1660705120`) y 1 unidad vía Traspaso→bodega — confirmado que `proyecto_materiales.cant_instalada` subió a 1 y se insertó un `movimientos` con `tipo='instalado'`, `area='ATT'` (derivada del proyecto real, no del selector), y que el stock de la bodega destino del traspaso subió +1 **sin volver a tocar** la bodega del conteo; el evento pasó a `resuelto` recién al completar las 2 unidades. (2) Un evento sobrante (diferencia +3) se resolvió vía Devolución→técnico — confirmado que el stock del técnico bajó exactamente 3 y se insertó un `movimientos` con `tipo='traslado'` a nombre suyo. (3) **En el navegador**: el Home de Conteo ya muestra fecha en cada evento pendiente (3 eventos viejos reales, sin relación con esta prueba, ya la mostraban); al entrar a cada conteo de prueba, "Eventos de este conteo" aparece primero con el historial completo de resoluciones (tipo/cantidad/detalle/nota, más reciente primero) y "Resuelto" cuando corresponde, y la tabla de líneas debajo ya en formato `ColumnHeader` (SKU/Material/Lote/Sistema/Contado/Diferencia). Sin errores de consola. Quedan los datos de prueba (`TEST-IMPORT-VELOCIDAD`, `TEST-TRASPASO-DESTINO`, los 2 conteos y sus eventos/resoluciones) para la limpieza general antes del cutover (punto 16 de "Próximos pasos").
  - **Ajuste pedido después**: cada evento pendiente en el Home de Conteo gana un botón "Ver conteo →" directo a su conteo (antes solo decía "entra al conteo correspondiente" en texto). Requirió agregar `conteoId` a `EventoInventario` — sale de un embed `conteo_lineas(conteo_id)` (forward, sin `!inner`, distinto del `!inner` que ya usaba `listEventosPorConteo` para filtrar). Los eventos que **no** vienen de un conteo (los de "instalación forzada" del PASO 32, que insertan directo en `eventos_inventario` sin `conteo_linea_id`) correctamente no muestran el botón — confirmado en el navegador con los 3 eventos reales pendientes: solo el que sí viene de un conteo (SKU 55636) tiene "Ver conteo →", y al tocarlo navega directo a "C088 · Físico" con el evento arriba y la tabla debajo.
  - **Hallazgo real señalado por el usuario**: `resolver_evento_parcial` nunca dependió de que el evento viniera de un conteo (solo usa material/ubicación/diferencia) — pero el Home de Conteo, al mostrar los eventos de "instalación forzada" (sin conteo) en modo solo-lectura, no les daba NINGÚN camino para resolverse (el formulario de resolución solo vivía dentro de `ConteoDetail`). Se unificó: `EventoCard` (ya usado dentro de un conteo) se generaliza con dos props opcionales — `onVerConteo` (solo se muestra si el evento tiene `conteoId`) y `mostrarUbicacion` (necesario en la lista general, que mezcla varias bodegas/técnicos) — y `EventosAbiertosSection` pasa a reusarlo directo en vez de tener su propia tarjeta de solo lectura. Los eventos sin conteo muestran una nota aclaratoria ("Instalación forzada sin stock suficiente — a nombre de X") en vez del link a conteo, pero el mismo botón "Resolver →"/formulario ya funciona igual para ambos orígenes.
  - **Verificado en el navegador contra la BD real**: los 3 eventos pendientes ahora muestran "Resolver →" (antes solo 1 de los 3 tenía cómo resolverse); se resolvió el evento de prueba conocido (SKU 999903, "instalación forzada", sin conteo) vía Consumo→Pérdida — confirmado por REST que quedó `estado='resuelto'`, `cantidad_resuelta=1`, con su fila en `eventos_inventario_resoluciones` (`tipo='consumo'`, `area='perdida'`). De paso, esto resuelve uno de los eventos de prueba pendientes de limpieza mencionados en el punto 16 de "Próximos pasos" (el material 999903 de PASO 32) — ya no hace falta limpiarlo aparte, quedó resuelto como cualquier evento real.
  - **29-07-2026 — refinamiento pedido tras revisar el flujo con Andrés**: al pensar en cómo resolver de verdad un evento de "instalación forzada" (técnico), surgió un problema real — el stock de un técnico forzado en negativo NO está "ya correcto" (a diferencia de una bodega recién contada), así que Devolución/Traspaso tal como se diseñaron (de un solo lado) no alcanzan, y Consumo→proyecto duplicaría el instalado (el proyecto ya se acreditó una vez, en el momento de forzar la instalación).
  - **`supabase/migrations/0040_reasignacion_eventos_tecnico.sql`** (nueva, **pendiente de correr**):
    - `eventos_inventario.movimiento_id` (nullable, FK a `movimientos`) — se llena en `registrar_movimiento` con el `id` del movimiento que ya se insertó justo antes (mismo `v_row`), en vez de inferir por fecha/hora. null en eventos de Conteo (no nacen de un solo movimiento). Esto de paso resuelve, sin columna aparte, la necesidad de saber a qué proyecto pertenece la instalación forzada (se llega ahí vía `movimientos.project_id`).
    - `resolver_evento_parcial` reescrita: detecta el origen (`ubicaciones.tipo` del evento) y bifurca: **Devolución** pasa a ser de **dos lados** cuando el origen es un técnico (además de restarle al origen real, le devuelve al técnico del evento lo que le falta — su negativo era el error a corregir, no algo ya asentado); **Traspaso** queda bloqueado para eventos de técnico (el material ya se instaló, no está "por encontrar"); **Consumo→proyecto** queda bloqueado si el evento ya tiene `movimiento_id` (evita duplicar el instalado — solo Consumo→Pérdida sigue disponible ahí). Tipo nuevo **`reasignacion`**: mueve el consumo a otro técnico que sea miembro del mismo proyecto (validado contra `project_members` del proyecto del movimiento original — sin adivinar nada) y que tenga stock real (nunca fuerza negativo, a diferencia de Devolución); no toca `proyecto_materiales` (el total instalado del proyecto ya es correcto, esto solo corrige de quién salió físicamente el material). Candados nuevos: no se puede elegir como origen/destino/reasignación al mismo técnico que ya tiene el evento.
    - **Cliente**: `EventoInventario` gana `ubicacionTipo`, `movimientoId` y `origenMovimiento` (proyecto/técnico/cantidad/fecha del movimiento original, resuelto vía un embed nuevo `movimientos(...)` con `projects(ott)`/`profiles(nombre,email)` anidados — sin ambigüedad porque `movimientos` solo tiene una FK a cada uno). La tarjeta de evento ahora muestra el consumo original exacto ("Consumo simultáneo: N unidad(es) el [fecha] · proyecto X · técnico Y") en vez de solo el texto genérico. `ResolverEventoForm`: los tipos disponibles ahora dependen del origen (técnico → Consumo/Devolución/Reasignación; bodega → los de siempre), Consumo queda fijo en "Pérdida" (sin selector) cuando el evento ya tiene `movimiento_id`, y Reasignación trae un selector de técnico ya filtrado a los miembros del proyecto (`adminRepo.listMembers`), excluyendo siempre al técnico que ya tiene el evento.
    - **Verificado**: `tsc --noEmit` limpio. Se releyeron los 6 `insert into movimientos` de la función nueva uno por uno para evitar el mismo bug de columnas/valores desalineados de 0039. En el navegador, con la migración aún sin correr, la pantalla de Conteo falla limpio con `Could not find a relationship between 'eventos_inventario' and 'movimientos' in the schema cache` — mismo patrón de siempre.
    - **29-07-2026, tras correr 0040 — verificación completa contra la BD real** (proyecto real `TEST-1660705120`, técnicos reales IGNACIO/tecnico2 ya asignados a él): se forzó una instalación real de 5 unidades sin stock en nadie del equipo (confirmado `movimiento_id` poblado en el evento) y se resolvió vía **Reasignación** a tecnico2 (a quien se le dio stock real aparte) — el técnico original volvió a su valor previo exacto, tecnico2 bajó exactamente lo reasignado, y `proyecto_materiales.cant_instalada` **no se movió** más que lo que ya reflejaba la instalación original (sin duplicado). Se forzó una segunda instalación (6 unidades) y se resolvió vía **Devolución→bodega** (dos lados): el técnico volvió a su valor correcto y la bodega bajó exactamente 6. Se probó también el candado de Reasignación intentando reasignar a un técnico con 0 de stock — rechazado con "Stock físico insuficiente" en vez de forzar negativo, tal como se diseñó. **En el navegador**: la tarjeta del evento nuevo mostró "Consumo simultáneo: 6 unidad(es) el [fecha y hora exacta] · proyecto TEST-1660705120 · IGNACIO ANDRES PEREZ LOPEZ"; el formulario ofreció solo Consumo/Devolución/Reasignar a técnico (sin Traspaso); Consumo quedó fijo en "Solo Pérdida"; el selector de Reasignar solo mostró a tecnico2 (excluyó a IGNACIO, el técnico del propio evento, aunque también es miembro del proyecto). Evento de prueba cerrado con Consumo→Pérdida al final. Sin errores de consola.
  - **Ajuste pedido después**: para un evento de técnico, se agrega un botón **"Ver proyecto →"** (junto al ya existente "Ver conteo →" de eventos de bodega) que navega directo a `/att/:id`, `/preventivos/:id` o `/incidencias/:id` según corresponda — para poder revisar el proyecto completo (asignaciones, tabla de material) antes de decidir cómo resolver. Requirió sumar `area`/`subarea` del proyecto al embed `movimientos(...)→projects(...)` ya existente (mismo viaje, sin consulta nueva) y un helper `rutaProyecto()` con `useNavigate` de `react-router-dom` (mismo patrón ya usado en `AsignacionesScreen`/ATT `Home.tsx`). Verificado en el navegador: el botón aparece solo en eventos de técnico con proyecto conocido, y al tocarlo navega correctamente a `#/att/{id}` mostrando el editor del proyecto real (`TEST-1660705120`).

## Gotchas importantes
- **Publishable key `sb_publishable_` NO es JWT**: para probar la REST API anónima con curl/PowerShell, mandar SOLO el header `apikey` (NO `Authorization: Bearer`, da 401).
- La migración usa `set check_function_bodies = off` para permitir que las funciones referencien tablas creadas más abajo.
- Los roles anon/authenticated necesitan `GRANT` (ya aplicados; incluidos en la migración para instalaciones nuevas).
- **`.env` es gitignored** — recrear en cada PC (ver setup abajo).
- **Primer admin**: Supabase → Authentication → Add user (marcar *Auto Confirm*), luego `update public.profiles set rol='admin', nombre='...' where email='...';`
- ~~`attRepo.remove()` no-op silencioso para jp/invitado~~ **RESUELTO en paso 3**: ver "Store online-first" arriba (`close()` + detección de 0 filas afectadas).
- **`projects.jefe_proyecto` (texto) nunca se creó en la BD real**, pese a que el commit `a03672a` y este doc decían que sí: ese commit solo tocó el `CREATE TABLE` de `0001_init.sql` (referencia para instalaciones nuevas), no alteró la tabla ya existente en el proyecto Supabase compartido. Síntoma: `PGRST204 Could not find the 'jefe_proyecto' column...` en cualquier `insert`/`update` de `projects` (bloqueaba **todo** `attRepo.save()`, no solo cosas nuevas). `NOTIFY pgrst, 'reload schema'` NO lo arregla (no es problema de caché; la columna de verdad no existía — confirmado con `SELECT jefe_proyecto` dando `42703 column does not exist`, con hint apuntando a `jefe_proyecto_id`, la FK que sí existe). Fix aplicado ya en la BD: `alter table public.projects add column if not exists jefe_proyecto text;`. **Lección**: cuando se edite `0001_init.sql` para reflejar un cambio de esquema en una tabla que ya existe en producción, hay que además correr el `ALTER TABLE` real contra la BD — editar el archivo de referencia no basta.

## Setup en un PC nuevo
Prerrequisitos: **Node 22** (el que usa el deploy) y **git**. No hace falta copiar nada a mano salvo el `.env`.
```bash
git clone https://github.com/Sinterk/TelecomCatalog.git
cd TelecomCatalog
git checkout backend-supabase
npm install
# crear .env (gitignored) con:
#   VITE_SUPABASE_URL=https://xwawplezarrfonuyaaxu.supabase.co
#   VITE_SUPABASE_ANON_KEY=sb_publishable_x01p7hsYvNKlWklAQ11H-Q_KPenq-XW
#   VITE_GUEST_EMAIL=abarahona.sinterk@gmail.com          # modo invitado (jp)
#   VITE_GUEST_PASSWORD=...                                # pegar tal cual, sin comillas (tiene caracteres especiales)
#   VITE_GUEST_TECNICO_EMAIL=iperez@sinterk.cl             # invitado rol técnico — TEMPORAL, ver "Estado actual"
#   VITE_GUEST_TECNICO_PASSWORD=...
npm run dev   # sin certs mkcert corre en http://localhost:5173
```
> **NO re-ejecutar** `supabase/migrations/0001_init.sql` ni `0002_preventivos.sql`: ambos ya están aplicados en el proyecto Supabase compartido (`xwawplezarrfonuyaaxu`). Son solo para instalaciones nuevas o de referencia del esquema.

## Convenciones y reglas
- **Commits en español**, terminando con el trailer `Co-Authored-By: Claude <noreply@anthropic.com>`. No commitear `.claude/launch.json` ni `.env`.
- **Subir la versión** (`APP_VERSION` en `vite.config.ts`) en cada cambio visible.
- **NO hacer push/merge a `main` todavía**: push a `main` dispara el deploy y publica el gate de login, dejando fuera a los usuarios actuales (que aún no tienen cuenta). Trabajar en `backend-supabase`; recién fusionar a `main` cuando datos + usuarios estén listos, como cutover coordinado.
- **Gotcha PWA/caché**: la app instalada suele servir el bundle viejo hasta cerrarla del todo (no solo minimizar) o borrar datos del sitio; por eso se sube `APP_VERSION` para confirmar el bundle nuevo.
- **La cuenta invitado es una cuenta real y compartida — cuidado al probar la pantalla de admin con ella**: en una sesión de prueba su `rol` quedó en `log` en vez de `jp` (probablemente al probar el selector de rol en Usuarios), lo que rompió silenciosamente el modo invitado (un `log` no puede crear/editar proyectos vía RLS) hasta notarlo y corregirlo. Si el modo invitado deja de poder guardar, lo primero a chequear es `select rol from profiles where email='abarahona.sinterk@gmail.com'` — debe decir `jp`.

## Próximos pasos (en orden)
1. ~~**Capa de datos** `src/modules/att/data/attRepo.ts`~~ ✅ **HECHO**. Mapeo clave: `jefeProyecto`→`projects.jefe_proyecto` (texto), `fotoAerea`→`informes.foto_general_path`, `ingresoRed`/`infraestructura`→jsonb.
2. ~~**Fotos → Storage**~~ ✅ **HECHO** (ver "Estado actual").
3. ~~**Refactor del store online-first**~~ ✅ **HECHO** (ver "Estado actual"). Falta cablear un botón/indicador de "informe cerrado" en la UI si algún día se quiere una vista de cerrados (hoy hay filtro Activos/Cerrados/Todos en Home, pero no hay acción de "reabrir" un cerrado).
4. ~~**Migración de borradores locales**~~ ✅ **HECHO** (ver "Estado actual"). Es un aviso manual en Home, no una migración automática silenciosa — el técnico ve el botón y decide cuándo sincronizar.
5. ~~**Pantalla admin**~~ ✅ **HECHO** (ver "Estado actual"): edición de perfiles + equipo por proyecto (ahora incluye proyectos de ambos módulos). Verificado por el usuario con la cuenta admin real, incluido el fix de auto-democión.
6. ~~**Migración de Preventivos a Supabase**~~ ✅ **HECHO** (ver "Estado actual", paso 6): mismo patrón de ATT replicado — esquema nuevo, capa de datos, fotos a Storage, store online-first, filtro/buscador. Verificado en el navegador de punta a punta.
7. ~~**Módulo de inventario**~~ ✅ **HECHO** (ver "Estado actual", paso 7): función de BD `registrar_movimiento` + `reasignar_transito_a_preventivo`, capa de datos, formulario compartido, pestañas Logística en ATT/Preventivos, ventana Inventario con 5 sub-tabs. Verificado en el navegador y contra la BD real.
8. ~~**Conteo + stock negativo + umbral de renovación**~~ ✅ **HECHO** (ver "Estado actual", paso 7b). Verificado contra la BD real y en el navegador.
9. ~~**Crear bodega inline + importar conteo digital desde Excel SAP**~~ ✅ **HECHO** (probado desde otro PC en red local, primera vez con datos reales). Ver detalle abajo.
10. ~~**Primer conteo de un material no genera diferencia**~~ ✅ **HECHO**. Ver detalle abajo.
11. ~~**Drag-and-drop en toda subida de archivos**~~ ✅ **HECHO**. Ver detalle abajo.
12. ~~**Alias de columnas SAP estándar (Material/Texto breve de material/Lote/Libre utilización)**~~ ✅ **HECHO**. Ver detalle abajo.
13. ~~**Tabla en pestaña Bodega**~~ ✅ **HECHO**: la lista de tarjetas se cambió por una tabla (Material/Bodega/Lote/Físico/Digital/Umbral), con fila resaltada en rojo/ámbar para descuadre/renovar (mismo criterio que antes, ahora como color de fila en vez de borde de tarjeta) y el editor de umbral inline funcionando igual dentro de la celda. Verificado en el navegador contra datos reales (bodega C132).
14. ~~**Pulido de Bodega (SKU numérico, filtro tipo Google Sheets en todas las columnas, fix de menú recortado) + Excel más rápido + Logística/ATT (material buscable, lote con disponibilidad, defaults, tabla editable + modo corrección)**~~ ✅ **HECHO** (13-07-2026, ver "PASO 8" arriba). Verificado en el navegador y contra la BD real.
15. ~~**Vista "Material asignado" para el rol técnico en su propia Home**~~ ✅ **HECHO** (14-07-2026, v0.77, ver "PASO 9" arriba): menú de dos ventanas (Asignaciones / Inventario propio), verificado en el navegador con el invitado técnico.
16. **← SIGUIENTE** — pendientes:
    - **Limpieza de datos de prueba antes de producción**: el usuario dijo que borrará todo lo generado hasta ahora antes del cutover (proyectos `_verify_*`/`TEST-*`, bodega `TEST-IMPORT-VELOCIDAD`, materiales `999901-999905`, movimientos de prueba de esta sesión y la anterior). Los stock digitales "son simples de subir de nuevo" según el usuario, así que no hay que preservarlos. **Incluye también el invitado técnico temporal** (`iperez@sinterk.cl`, ver "Estado actual"): quitar `GUEST_TECNICO_*`/botón/`GuestKind` de `auth.ts`/`LoginScreen.tsx`/`UserMenu.tsx`, las vars del `.env`, y borrar la cuenta en el dashboard de Supabase.
    - Sub-opción Consumibles y tabla de proveedores (pospuestos, alcance sin definir).
    - Probar el flujo completo de Logística/Conteo con un usuario técnico/log real de cara al cutover a `main` (hoy solo hay admin+jp reales; el invitado técnico temporal sirve para probar la UI/RLS del rol pero no reemplaza cuentas reales de técnicos).
    - El catálogo de materiales real ya se está cargando (import SAP) y ya hay bodegas C088/C103/C132 con stock físico real cargado — la app ya no está "vacía".
17. ~~**Panel de KPIs operacionales**~~ ✅ **HECHO** (23-07-2026, v0.99, ver "PASO 31" arriba): proyectos (OTT/Preventivos/Incidencias abiertas/cerradas/pendientes) + consumo de materiales por periodo/área/bodega/técnico, con Merma agregada como tipo real de movimiento. Verificado en el navegador y contra la BD real.
18. ~~**Material por punto en Preventivos + prioridad de origen al instalar**~~ ✅ **HECHO** (23-07-2026, v1.00, ver "PASO 32" arriba): material/comentarios embebidos en la tarjeta de cada punto, tabla general con total agregado + filtro por punto, y prioridad de 3 pasos (proyecto → equipo → forzar negativo con aviso) para todo "Instalado" en la app. Incluyó un fix de un bug crítico preexistente (id de los puntos no estable entre guardados). Verificado en el navegador y por REST directo contra la BD real.
19. **← SIGUIENTE** — pendientes, en el orden que dio el usuario:
    - **Modelo de estado de pago por OTT (EP)**. Objetivo: al cerrar un OTT, autogenerar el borrador de líneas del EP (código LPU + cantidad + tipo de tendido + observaciones) en vez de que el JP llene el Excel mensual a mano; salida = vista editable en la PWA + botón para copiar/exportar valores listos para pegar en la pestaña real del Excel (el `.xlsm` real de facturación a Entel NO se reescribe por código — es un workbook no modular, con hojas fijas "1".."30", fórmulas VLOOKUP/INDIRECT cruzadas y protección de hoja en zigzag; abrirlo con openpyxl ya avisa que descarta validaciones/WMF al guardar).
      - Esquema planeado (5 tablas): `lpu_codigos`/`lpu_precios_zona`/`lpu_ito_servicios` (catálogo LPU importado tal cual, ~1130 filas, precio editable por zona — 13 zonas de ATT) → `lpu_material_map` (material→código LPU, sugiere fusión al usar mufa/CMIC-ODF) → `lpu_tendido_map` ((tipo_cable,tipo_tendido)→código LPU) → `ep_informes` (uno por OTT) → `ep_lineas` (descripción/precio CONGELADOS al guardar, para que un reajuste de precio futuro no altere un EP ya enviado). El "avance" nunca se guarda tal cual — se recalcula en vivo desde tramos+materiales+mapeos+precios actuales cada vez que se pide (botón "regenerar avance"); solo lo que el JP confirma se escribe en `ep_lineas`.
      - Códigos LPU de fusiones ya confirmados: `NPE4020` (confección/instalación mufa), `NPE4070` (fusión en mufa 1 a 4), `NPE3900` (confección cabecera), `NPE3950` (fusión en cabecera 1 a 4) — tipo LPU `FUSIONES`. `NPE6500` "HH DISPONIBILIDAD DE EQUIPO" (tipo `Otros`) es el comodín acordado con Entel para trabajo que no encaja en otro código (detalle siempre en Observaciones). Tipo de tendido: selector manual en el sitio, no se deriva automáticamente.
      - **28-07-2026**: creada `supabase/migrations/0031_lpu_codigos.sql` (`lpu_codigos`, `lpu_precios_zona`, `lpu_ito_servicios`, `lpu_ito_precios_zona` — lectura jp/admin, escritura solo admin), `0032_estados_de_pago.sql` (`lpu_material_map`, `lpu_tendido_map`, `ep_informes`, `ep_lineas` — jp/admin, sin acceso técnico; `ep_lineas` congela codigo_att/descripcion/precio_unitario al guardar) y `scripts/importar-lpu.mjs`. Ambas migraciones corridas por Andrés en el SQL Editor de Supabase el mismo día.
      - **28-07-2026 (mismo día, tras recibir el .xlsm real)**: la estructura real de la hoja "LPU Obras por Zonas" NO calzaba del todo con lo asumido en 0031 — los nombres de zona están en la fila INMEDIATAMENTE ANTERIOR a la de "Corr"/"Código ATT"/etc (no en la misma fila), y hay dos columnas sin encabezado de texto justo antes de "Corr" (una sin significado claro, preservada igual como `grupo`; la otra es "Estado" — VIGENTE/NO DISPONIBLE/ELIMINADO — inferida solo por posición). Y la hoja "LPU Sup Itos obras e Itos serv" resultó tener una forma TOTALMENTE distinta a la asumida: no tiene zonas ni código LPU, es una tabla plana de 18 prestaciones (Prestación/Valor Unit base/Reajuste $/Valor Unit final) agrupadas en 3 secciones (Supervisores EOATT, ITO OBRAS, ITO Servicio Telco) marcadas por filas de solo texto. Corregido en `supabase/migrations/0033_lpu_ito_fix.sql` (agrega `lpu_codigos.grupo`, recrea `lpu_ito_servicios` sin zonas y elimina `lpu_ito_precios_zona`) + reescritura completa de `scripts/importar-lpu.mjs` con parsers separados para cada hoja. Validado con un dry-run de la lógica de parseo contra el `.xlsm` real (sin tocar la BD): 305 códigos únicos en "LPU Obras por Zonas" (NPE4020/NPE6500 confirmados con sus 13/12 precios de zona reales), 18 prestaciones en la hoja ITO — 3 códigos de la hoja LPU vienen legítimamente sin precio de zona fijo (ej. "de acuerdo al monto neto indicado en la factura"), no es un bug del parser.
      - El `.xlsm` real quedó en la raíz del repo (`eneroEP03 SINTERK ENERO2026_A.xlsm`) — es dato comercial real de Entel/ATT, se agregó `*.xlsm` a `.gitignore` para que nunca se suba.
      - **Mismo gotcha de 0017 (`profiles`), esta vez en las tablas LPU**: al correr `importar-lpu.mjs` con la service role key real, falló con `permission denied for table lpu_codigos` — igual que en PASO de `profiles`/alta masiva, `service_role` bypasea RLS en este proyecto pero de todos modos necesita el `GRANT` de tabla explícito, y 0031-0033 no lo tenían. Corregido en `supabase/migrations/0034_lpu_grant_service_role.sql` (grant a `service_role` en las 7 tablas nuevas). **Lección para toda migración futura que cree tablas nuevas**: agregar SIEMPRE `grant ... to service_role` junto con el de `authenticated`, no solo cuando un script falle.
      - **28-07-2026**: con `0034` corrida, el import funcionó pero fue muy lento (~900 llamadas HTTP secuenciales, un find+insert/update por cada una de las 305 filas) y con eso más ventana para `TypeError: fetch failed` transitorios (2-3 códigos por corrida, distintos cada vez — no es un problema de datos, el script es idempotente así que una recorrida los completa). También apareció `ON CONFLICT DO UPDATE command cannot affect row a second time` en la hoja ITO: el Excel real trae una prestación duplicada literal ("Validación de procedimientos de trabajos por técnicos de campo" x2 bajo "ITO Servicio Telco") y un upsert no puede afectar la misma fila dos veces en el mismo lote.
      - Corregido: `supabase/migrations/0035_lpu_codigo_att_unique.sql` agrega `unique(codigo_att)` a `lpu_codigos`, y `scripts/importar-lpu.mjs` se reescribió para hacer upsert por lote (1 llamada para los 305 códigos + unas pocas de 500 precios de zona cada una, en vez de ~900 llamadas) y deduplicar por `categoria+prestacion` antes de escribir en `lpu_ito_servicios`.
      - **28-07-2026 — import completo**: con `0035` corrida, `importar-lpu.mjs` terminó limpio: 305/305 códigos en `lpu_codigos`, 3125/3125 precios en `lpu_precios_zona`, 17/17 prestaciones en `lpu_ito_servicios`. El catálogo LPU real (enero 2026) ya está en la BD.
      - **28-07-2026 — diseño de las 3 fuentes de líneas del EP (conversación, aún sin código)**:
        - **Zona**: no se deriva de `región`/`comuna` del formulario ATT (son texto libre, sin estandarizar) — va un selector "Zona LPU" independiente en la pantalla de EP, con las 13 zonas del catálogo y `RM-CENTRO` preseleccionado (confirmado con Andrés: no se ha atendido OTT fuera de Santiago aún).
        - **(a) Materiales instalados** → vía `lpu_material_map` (pendiente de crear): por cada material puede haber 1+ reglas (material_id, lpu_codigo_id, cantidad por unidad instalada/factor). Cubre tanto 1:1 (mufa → NPE4020 + NPE4070) como fracciones de HH compartidas (ej. varios tipos de ferretería sumando cantidad hacia el mismo código HH). Necesita una pantalla de admin para editarlo — es la respuesta a "¿se puede editar cómo se refleja un material en el EP?": sí, es exactamente lo que hace esta tabla.
        - **(b) Metros tendidos** → vía `lpu_tendido_map` (pendiente de crear): `(tipo_tendido, capacidad_min, capacidad_max) → lpu_codigo_id`. Requiere 2 columnas nuevas en `materiales` (`tipo_tendido`, `capacidad`), llenadas solo para SKUs de cable. Al regenerar: por cada tramo, se busca entre los materiales instalados en ESE proyecto (mismo id que ya usa `getResumenProyecto`/`ResumenProyectoTable` — confirmado que `AttRecord.id` = `projectId`) los de categoría cable; si hay exactamente uno con cantidad instalada, se usa su tipo/capacidad como default. Si hay cero o más de uno (cable reutilizado retirado de otro sitio, no es material "nuevo" de bodega — caso real mencionado por Andrés), queda en blanco y el JP lo completa a mano; el metraje del tramo siempre se pre-llena igual.
        - **(c) Eventos (Hitos)** → **deferido explícitamente, "indagar un poco más" antes de definir**. Lo único acordado: salen de la sección Hitos ya existente (`AttRecord.hitos[]` — hoy solo `{id, fecha, descripcion}` en texto libre), representan cobros (ej. visita técnica, pasada de muro). Como la descripción es texto libre sin taxonomía cerrada, NO hay mapeo automático confiable a un código LPU todavía — probablemente cada Hito aparezca como línea sugerida con observación pre-llenada desde `descripcion`, pero código LPU + cantidad elegidos a mano por el JP (candidatos en el catálogo: tipos VISITA SIMPLE/VISTA COMPLEJA/RETIRO/SUSPENSION, o el comodín NPE6500). **No implementar todavía — retomar este punto al final, después de (a) y (b).**
        - Todo lo anterior son solo sugerencias (origen='auto'); el JP edita/confirma cada línea antes de guardar (ya cubierto por el diseño de `ep_lineas`).
      - **29-07-2026 — (1) migración creada y corrida**: `supabase/migrations/0036_ep_mapeos_completar.sql` agrega `materiales.tipo_tendido`/`materiales.capacidad` (nullable, solo se llenan para SKUs de cable); `lpu_material_map.factor_cantidad` (numeric, default 1 — cantidad_sugerida_lpu = cant_instalada × factor_cantidad, sumando entre materiales que mapeen al mismo código, para el caso de fracciones de HH compartidas); y reemplaza la llave de `lpu_tendido_map` de `(tipo_cable, tipo_tendido)` (supuesto en 0032, antes de la conversación de diseño detallada) por la real `(tipo_tendido, capacidad_min, capacidad_max)` — sin migrar datos porque la tabla seguía vacía. Andrés confirmó haberla corrido en Supabase.
      - **29-07-2026 — (2) pantalla de admin creada**: capa de datos nueva `src/lib/lpu/{types,lpuRepo}.ts` (mismo patrón que `src/lib/kpi/kpiRepo.ts` — wrappers tipados sobre `lpu_codigos`/`lpu_material_map`/`lpu_tendido_map`, embeds `*, lpu_codigos(*)` para traer descripción/código en un solo viaje) + `materiales.tipoTendido`/`materiales.capacidad` agregados a `Material`/`inventarioRepo.ts` (`updateMaterialTendido`, mismo patrón que `updateMaterialComentario`). Componente nuevo `src/ui/LpuCodigoSelect.tsx` (selector con buscador sobre los ~305 códigos, mismo patrón portal-a-`document.body` que `MaterialSelect.tsx`, oculta los `estado='ELIMINADO'`). Sección nueva `src/ui/admin/LpuMapeoSection.tsx`, montada en `AdminScreen.tsx` tras Usuarios/Equipo: (a) **Material → Código LPU** — `MaterialSelect` para elegir un material, editor de `tipo_tendido`/`capacidad`, y lista de códigos LPU asociados (agregar/activar-desactivar/quitar, con `factor_cantidad` por fila); (b) **Tendido → Código LPU** — tabla de filas `(tipo_tendido, capacidad_min, capacidad_max) → código` con alta/baja/activar. A propósito sin `ColumnHeader` (orden/checklist) esta primera versión — el admin busca UN material o agrega UNA fila a la vez, no necesita analizar la tabla completa todavía; se puede sumar después si hace falta.
        - **Verificado**: `tsc --noEmit` limpio. **No se pudo probar la UI visualmente** — `AdminScreen` está gateada a `rol==='admin'` (doble cerrojo: RLS + candado de UI) y este entorno solo tiene invitados `jp`/`tecnico` configurados (mismo límite ya documentado en el PASO 26 para esta misma pantalla). En su lugar se verificó el camino de datos real contra Supabase con la sesión invitada `jp`: `lpu_codigos` (con datos reales), `lpu_material_map`/`lpu_tendido_map` con el embed `*, lpu_codigos(*)` (vacíos, esperado — sin filas todavía, sin error de sintaxis), y `materiales.tipo_tendido`/`materiales.capacidad` (columnas existen, `null` en los 3 SKUs de muestra, esperado). Se confirmó también que un intento de `insert` en `lpu_material_map` como `jp` devuelve `403 / "new row violates row-level security policy"` — la RLS admin-only de escritura funciona como se diseñó. **Falta que Andrés pruebe la UI en sí con su cuenta admin real** (elegir un material real de cable, asignarle tipo_tendido/capacidad, mapear una mufa a sus códigos de fusión, y una fila de tendido) antes de dar este punto por cerrado del todo.
      - **Falta, en orden**: (1) ✅ hecho; (2) ✅ hecho, pendiente de prueba visual con cuenta admin real; (3) lógica de "regenerar avance" (lee tramos+resumen de materiales instalados+mapeos+precios actuales, en vivo, sin guardar); (4) UI de EP (vista editable + botón "regenerar avance" + exportar/copiar para pegar en el Excel real); (5) definir e implementar (c) Eventos/Hitos, al final.
    - **Carga de materiales reales** (el catálogo real ya se está cargando vía import SAP, ver punto 16 arriba — falta terminarlo).
    - **Limpieza de datos de prueba antes de producción** (ver punto 16 arriba, sigue pendiente — ahora también incluye el material de prueba 999903 instalado con `requiere_revision=true` en "Independencia — 1" durante la verificación de PASO 32, y el evento abierto en `eventos_inventario` asociado). Script listo en `scripts/limpieza-datos-prueba.sql` (pendiente de correr).
    - Después de eso: cutover a `main`.

- **Rename a "SinterkProyectos" (30-07-2026, v1.11)**: "TelecomCatalog" era un placeholder — nombre oficial pedido por Andrés. Repo de GitHub renombrado (`Sinterk/TelecomCatalog` → `Sinterk/SinterkProyectos`, hecho por Andrés) + remoto local actualizado. Cambiado en código: `<title>`/`apple-mobile-web-app-title` (`index.html`), `manifest.name`/`short_name` (`vite.config.ts`), branding en `LoginScreen.tsx`/`Layout.tsx`, `package.json.name`, y el campo `app:` dentro del JSON/ZIP que exportan Preventivos/Incidencias/ATT (solo metadata descriptiva, el import no lo valida — no rompe compatibilidad con ZIPs viejos). `supabase/migrations/0001_init.sql` NO se toca (migración ya aplicada, el nombre ahí es solo un comentario de cabecera inerte). Verificado en el navegador: título de pestaña y login muestran "SinterkProyectos".

- **Sugerencias de mejora (30-07-2026, v1.11)**: pedido explícito de Andrés de cara al mes de capacitación — botón en el menú de usuario para reportar un problema o proponer una mejora (ejemplo dado: "problema encontrado en Preventivos"), con fecha/hora, asunto y cuerpo. Sugerencia agregada por Claude y aceptada: campo `estado` (`pendiente`/`revisado`/`resuelto`) para que admin/jp/log puedan hacer triage — mismo patrón abierto/resuelto que `eventos_inventario`, sin el cual esto sería solo una bandeja sin forma de dar seguimiento durante el mes de capacitación.
  - **`supabase/migrations/0041_sugerencias.sql`** (nueva, **pendiente de correr**): tabla `sugerencias` (`usuario_id` default `auth.uid()`, `asunto`, `cuerpo`, `estado` default `'pendiente'`, `created_at` — fecha+hora salen de esta sola columna, igual que `movimientos.fecha` en el resto de la app). RLS: cualquier autenticado inserta a su propio nombre; `is_jp_or_admin()` (admin/jp/log) lee todas y es la única que puede hacer `update` de `estado`; el resto solo lee lo propio. Sin política de `delete` (queda como registro). `grant` a `authenticated`/`service_role` desde el principio (lección de 0017/0034).
  - **Capa de datos**: `src/lib/sugerencias/{types,sugerenciasRepo}.ts`, mismo patrón que `src/lib/lpu/lpuRepo.ts` (`listSugerencias` con embed `*, profiles(nombre,email)`, `crearSugerencia`, `actualizarEstadoSugerencia`).
  - **UI**: `src/ui/SugerenciasScreen.tsx` (formulario asunto/cuerpo arriba, lista abajo — admin/jp/log ven todas con quién la envió y un selector de estado; el resto solo ve las propias, estado como badge de solo lectura), ruta `/sugerencias` en `App.tsx`, link "💡 Sugerencias de mejora" en `UserMenu.tsx` (visible para todos los roles, arriba del cambio de contraseña).
  - **Verificado**: `tsc --noEmit` limpio; HMR aplicó los cambios de `App.tsx`/`UserMenu.tsx` sin errores en consola. **No se pudo probar el flujo completo (enviar → ver → cambiar estado) en el navegador de este entorno** — el login de invitado no respondió en la sesión de verificación (posible límite del sandbox de este entorno, no relacionado con el código nuevo) y de todos modos la migración 0041 sigue sin correr. Falta que Andrés corra 0041 en Supabase y pruebe el flujo real (enviar una sugerencia como técnico/invitado, confirmar que aparece en la vista de admin/jp/log con el selector de estado, confirmar que un usuario sin ese rol no puede cambiar el estado ni ver las de otros).
  - **31-07-2026**: migración 0041 corrida por Andrés. Verificado de punta a punta con la cuenta invitado técnico (rol real `log` en ese momento, ver PASO de "Pruebas de preventivos" abajo): enviar → aparece con fecha/hora/nombre real (embed a `profiles`) → cambio de estado a "Revisado" persistido tras recargar. Fila de prueba dejada a propósito (marcada en el cuerpo como borrable).
- **Fix navegación: `/sugerencias` sin forma de volver (30-07-2026, v1.11)**: `/sugerencias` faltaba en el whitelist `homeScreens` de `Layout.tsx` (el mismo que ya incluye `/admin`/`/kpi`) — sin eso la barra inferior con "Inicio" no se renderizaba ahí. Agregado a ambas ramas (técnico/no-técnico). Verificado en el navegador.

- **Pruebas de preventivos (31-07-2026, v1.12)**: Andrés probó el módulo Preventivos como técnico y reportó dos cosas.
  - **(1) El cuadrante se podía editar desde terreno**: el código ya tenía la restricción (`soloFotos={isTecnico}` en `Editor.tsx`) — la causa real era que la cuenta invitado técnico (`iperez@sinterk.cl`) tenía el rol real `log` ("Logística") en Supabase, no `tecnico` ("Terreno"), confirmado al loguearse con ella durante esta sesión. No es un bug de código — Andrés lo corrigió desde Administración → Usuarios.
  - **(2) Aviso de material negativo visible en terreno**: sí era un vacío real. `PuntoMaterialSection.tsx` mostraba el aviso "⚠️ Sin stock suficiente…" sin importar el rol. Corregido: ahora solo se muestra a admin/jp/log (`!isTecnico`) — el técnico registra lo instalado sin ver ese manejo, que queda para que oficina lo resuelva vía el panel de eventos de Inventario. Único lugar en toda la UI donde se mostraba este aviso (`grep` de `requiereRevision` no encontró otros).
  - **Verificado**: `tsc --noEmit` limpio. No se pudo probar visualmente con un técnico real en este entorno (mismo límite del punto 1, ya corregido por Andrés después).

- **Cuarta forma de resolver eventos de técnico: "Agregar" (31-07-2026, v1.12)**: pedido explícito de Andrés tras mostrarle las opciones existentes (Consumo/Devolución/Reasignar a técnico) para un evento real de técnico en negativo (material 428, Rafael Navarrete, proyecto "1"). Racional: si el técnico instaló material que su stock registrado no tenía, es porque ya lo traía consigo sin contabilizar — no salió de ninguna bodega ni de otro técnico ahora, así que se le suma directo sin restarle a nadie (a diferencia de Devolución/Reasignación, que sí exigen un origen real).
  - **`supabase/migrations/0042_agregar_stock_tecnico.sql`** (nueva, **pendiente de correr**): agrega `'agregar'` al check de `eventos_inventario_resoluciones.tipo`; `resolver_evento_parcial` (misma firma, true replace) agrega la rama — `adjust_stock` de una sola punta sobre la ubicación del propio técnico + `movimientos` con `tipo='ajuste'` (ya permitido desde 0037). Bloqueada para eventos de bodega/Conteo (mismo dominio que Reasignación — solo `ubicacionTipo='tecnico'` y diferencia negativa).
  - **UI**: `TIPO_RESOLUCION_LABELS`/`tiposDisponibles` en `Home.tsx` agregan `agregar` a los eventos de técnico; nota explicativa en el formulario; `ResolucionRow` muestra "Sumado directo (sin origen)…" en el historial. Ojo con el payload de `submit()`: el ternario de `tecnicoUserId` caía por defecto a `modo === 'tecnico' ? tecnicoId : undefined` para cualquier tipo no listado explícitamente — sin una rama `tipo === 'agregar' ? undefined` de por medio, se habría mandado `tecnicoId` como `''` (string vacío, no uuid válido) al RPC. Corregido antes de probar.
  - **Verificado en el navegador contra el evento real de Rafael Navarrete**: la pestaña "Agregar" aparece junto a las otras tres, muestra solo Cantidad + nota explicativa + Nota (sin campos de origen), sin errores de consola. **No se probó el submit real** — la migración 0042 no se ha corrido todavía; hacerlo generaría "Tipo de resolución no reconocido: agregar" contra el RPC viejo. Falta que Andrés corra 0042 y pruebe el flujo completo.

- **Separar eventos de Conteo y de Técnico por pestaña (31-07-2026, v1.12)**: pedido explícito de Andrés tras ver las 5 diferencias mezcladas (bodega + técnico) en la misma sección arriba de la pestaña Conteo — quería cada tipo resolviéndose en su propia pestaña. Solo cliente, sin migración: `EventosAbiertosSection` ya era el mismo componente en ambos lados, solo hacía falta filtrar qué le llega y dónde se monta.
  - `ConteoLista` ahora filtra `evs.filter(e => e.ubicacionTipo !== 'tecnico')` antes de guardarlos en el `eventos` de estado — solo quedan los de origen bodega (Conteo).
  - `TecnicoTab` gana su propio fetch (`listEventosInventario({estado:'abierto'})` filtrado a `ubicacionTipo === 'tecnico'`) y renderiza `EventosAbiertosSection` arriba del selector de técnico — global (no depende de cuál técnico esté elegido en el desplegable de abajo), igual que el banner de Conteo no depende de cuál conteo esté abierto.
  - `EventosAbiertosSection`/`EventoCard` ya tenían `onVerConteo` opcional (los eventos de técnico nunca tienen `conteoId`) — solo se ajustó el tipo de `EventosAbiertosSection` para reflejarlo (antes lo pedía obligatorio aunque `EventoCard` ya lo aceptaba opcional).
  - **Verificado en el navegador contra los eventos reales**: pestaña Conteo pasó de mostrar 5 diferencias a mostrar solo 1 (la de bodega C088); pestaña Técnico muestra las 4 restantes (Rafael Navarrete ×2, iperez ×2). `tsc --noEmit` limpio.

- **Primera versión de la pantalla de Estado de Pago (EP) en ATT (31-07-2026, v1.14)**: pedido explícito de Andrés — "crea la ventana de EP en OTT con los datos que hay hasta ahora, para probar que esté en orden". Retoma el punto 19/EP que estaba pendiente ("(3) lógica de regenerar avance" y "(4) UI de EP") — cubre (a) materiales instalados y (b) metros tendidos; (c) Eventos/Hitos sigue deliberadamente fuera (aún no definido, ver diseño del 29-07-2026 más arriba).
  - **`supabase/migrations/0043_ep_zona.sql`** (nueva, **pendiente de correr**): agrega `ep_informes.zona` (nullable) — la Zona LPU elegida por el JP se guarda por OTT en vez de reelegirse cada vez. No existía ninguna columna para esto (confirmado revisando 0032/0036 — el diseño original la daba por hecha como "selector independiente" sin decir dónde se guardaba).
  - **`src/lib/lpu/lpuRepo.ts`** ganó 3 funciones que faltaban: `listZonasLpu()` (nombres de zona reales desde `lpu_precios_zona`, sin lista fija en código), `listPreciosPorZona(zona)` (mapa lpu_codigo_id→precio), `listLpuMaterialMapPorMateriales(ids[])` (bulk, evita N consultas al armar el avance).
  - **`src/lib/ep/{types,epRepo}.ts`** (nuevo módulo, mismo patrón que `src/lib/lpu/`): `getOrCrearEpInforme` (uno por proyecto, crea con zona 'RM-CENTRO' si no existe), `actualizarZonaEpInforme`, `listEpLineas`/`guardarEpLineas` (reemplaza TODO el set de líneas al guardar — sin historial parcial, se regenera completo), y `calcularAvanceEp(projectId, zona, tramos)` — el cálculo en vivo:
    - (a) reusa `getResumenProyecto` (ya probado, `punto_id` siempre null en ATT) × `lpu_material_map` activo, sumando `cant_instalada × factor_cantidad` por código.
    - (b) el tendido: identifica materiales "de cable" como los que tienen `tipo_tendido` configurado (no hay ninguna constante de `categoria='cable'` en el código — se usa ese campo, que por diseño "solo se llena para SKUs de cable", como proxy correcto). Si hay **exactamente uno** instalado en el proyecto, busca su fila de `lpu_tendido_map` por `(tipo_tendido, capacidad dentro de min/max)` y suma el metraje de TODOS los tramos del informe a ese único código. Si hay cero o más de uno, se omite (mismo criterio ya acordado: cable reutilizado retirado de otro sitio no es material "nuevo", ambiguo sin intervención del JP).
  - **UI**: `src/modules/att/components/EstadoPagoTab.tsx`, montado como 3ª pestaña "Estado de Pago" en `Editor.tsx` (junto a "Info de proyecto"/"Logística", mismo patrón `isUuid(id)` que ya usaba Logística — oculta la pestaña completa para rol técnico, ya que la RLS de `ep_informes`/`ep_lineas` es admin/jp/log únicamente). Selector de Zona LPU, botón "Regenerar avance" (reemplaza las líneas `origen='auto'` con el cálculo fresco, conserva las `'manual'` ya agregadas), tabla editable (cantidad/precio/observaciones editables por fila, quitar fila), selector `LpuCodigoSelect` + botón para agregar línea manual, total general, botón "Guardar" (`guardarEpLineas`) y botón "Copiar para Excel" (arma un TSV de las filas visibles y lo copia al portapapeles con `navigator.clipboard.writeText`, para pegar directo en las columnas del Excel real — cubre el punto "(4) UI de EP... exportar/copiar" sin exportar un .xlsx real).
  - **Verificado en el navegador contra un proyecto ATT real de esta sesión** (`TEST-1660705120`, 1 tramo): la pestaña aparece, `tsc --noEmit` limpio. Al abrir falla con el error esperado y correcto — `ep_informes.crear: Could not find the 'zona' column of 'ep_informes' in the schema cache` — porque **falta correr 0043**. Confirma que el resto del camino (fetch de zonas/códigos LPU, intento de crear el `ep_informe`) llega bien a Supabase antes de fallar justo en la columna que falta, mismo patrón de verificación usado en features anteriores sin la migración corrida.
  - **Falta, en orden**: (1) que Andrés corra `0043_ep_zona.sql`; (2) probar "Regenerar avance" contra un proyecto real con materiales instalados Y mapeados en `lpu_material_map`/`lpu_tendido_map` (el mapeo en sí — `LpuMapeoSection.tsx` — seguía "pendiente de prueba visual con cuenta admin real" desde el 29-07-2026, así que puede dar 0 líneas sugeridas si todavía no hay ninguna fila mapeada — **no sería un bug de esta pantalla**, sino falta de datos de mapeo); (3) probar Guardar/Copiar para Excel con datos reales; (4) definir e implementar (c) Eventos/Hitos, al final, como estaba planeado.

- **31-07-2026 — 0042 y 0043 corridas, verificación real completa**:
  - **"Agregar" (0042)**: probado contra el evento real de Rafael Navarrete (material 428, -1, proyecto "1", el mismo usado para mostrarle a Andrés las opciones existentes). Cantidad 1, confirmar → el evento desapareció de "3 diferencia(s) por resolver" en la pestaña Técnico (bajó de 4 a 3), confirmando que `resolver_evento_parcial` completó su transacción (si `adjust_stock` hubiera fallado, la función entera habría hecho rollback y el evento seguiría abierto). No hizo falta inspeccionar `stock` aparte — la atomicidad de la función ya lo garantiza.
  - **EP (0043)**: al abrir la pestaña Estado de Pago tras correr la migración, salió un error nuevo: `ep_informes.crear: duplicate key value violates unique constraint "ep_informes_project_id_key"`. Causa: `getOrCrearEpInforme` hacía select-then-insert sin ser atómico — con doble invocación del efecto de React (StrictMode en desarrollo, u otra causa de re-render rápido), las dos llamadas no encontraron fila, ambas intentaron crearla, y la segunda chocó contra `unique(project_id)`. **Corregido** en `src/lib/ep/epRepo.ts`: si el insert falla con código Postgres `23505` (duplicado), se hace un segundo `select` y se devuelve esa fila en vez de fallar. Verificado: recargada la pantalla, sin error; zonas reales cargadas (RM-CENTRO/AFTA/ARICA/CALAMA/CENTRO/CENTRO SUR — no hay lista fija en código, viene de `lpu_precios_zona`); "Regenerar avance" corrió sin error (0 líneas — esperado, este proyecto de prueba no tiene materiales mapeados todavía en `lpu_material_map`/`lpu_tendido_map`, no es un bug); "Guardar" con 0 líneas funcionó limpio.
  - **Pendiente real para dar la pantalla de EP por probada del todo**: correr el flujo contra un proyecto con materiales instalados Y mapeados (sigue pendiente la prueba visual de `LpuMapeoSection.tsx` con cuenta admin real, ver 29-07-2026 más arriba).
  - **Diagnóstico del caso real "CMIC"**: Andrés reportó que un CMIC instalado en `_verify_repo2_ott` no aparecía al regenerar avance. Investigado por REST directo contra la BD real (sesión invitada jp): "CMIC" no es ningún material del catálogo — es el nombre alternativo (aún sin ingresar) de "ODF 12 FIBRAS" (SKU 51024, `cant_instalada=1` en ese proyecto). Encontrado además que `lpu_material_map` tiene **0 filas en toda la base** — ningún material de ningún proyecto puede aparecer todavía por esa vía, no es un bug del cálculo. De paso confirmé que `apodo` no tenía ninguna forma de editarse desde la app (solo se mostraba) — esto motivó el punto siguiente.

- **Catálogo de materiales (31-07-2026, v1.16)**: pedido explícito de Andrés — faltaba una vista "base de datos" con todos los SKU (formato tabla) para gestionar nombre alternativo (el `apodo` sin editor, ver caso CMIC arriba), tipo de material y proveedores.
  - **`supabase/migrations/0044_catalogo_materiales.sql`** (nueva, **pendiente de correr**): tabla `material_tipos` (lista ABIERTA — el admin agrega nuevos desde la UI con "+ Nuevo tipo", por eso es tabla y no un `check` fijo), seedeada con 4 valores (Ferretería, Cable ADSS, Cable ducto, Cable autosoportado); `materiales.tipo_id` (FK nullable = "vacío"); `materiales.proveedores` (`text[]`, lista FIJA por ahora — Entel/Everything/CLEH, sin "+" para agregar más, validada con `check (proveedores <@ array[...])` en vez de tabla de unión ya que no hace falta extenderla). RLS de `material_tipos`: lectura para cualquier autenticado, inserción solo `can_move_inventory()` (admin/jp/log) — sin política de update/delete, los tipos no se editan ni se borran, solo se agregan. `materiales` ya tenía `mat_write` (`can_move_inventory()`) cubriendo las columnas nuevas, no hizo falta política aparte.
  - **Consolidación con el Estado de Pago**: `calcularAvanceEp` (`src/lib/ep/epRepo.ts`) usaba `tipoTendido !== null` como proxy de "es material de cable" — reemplazado por el Tipo del catálogo: cualquier tipo cuyo nombre empiece con `"Cable "` (case-insensitive) cuenta como cable. `tipo_tendido`/`capacidad` siguen siendo la clave real de búsqueda en `lpu_tendido_map` (sin cambios, se configuran aparte en Mapeo LPU) — el Tipo nuevo solo mejora la detección de "es candidato a tendido", no reemplaza esos dos campos.
  - **`src/lib/inventario/inventarioRepo.ts`**: `listMateriales()` ahora trae el embed `material_tipos(id,nombre)`; funciones nuevas `updateMaterialApodo` (no existía ninguna — apodo era de solo lectura desde que se creó el campo), `updateMaterialTipo`, `updateMaterialProveedores`, `listMaterialTipos`, `crearMaterialTipo`. `Material` (types.ts) ganó `tipoId`/`tipo`/`proveedores`; nuevo tipo `MaterialTipo` y constante `PROVEEDORES_MATERIAL`.
  - **UI**: originalmente `src/ui/admin/CatalogoMaterialesSection.tsx` (admin-only) — **movida el mismo día** a una pestaña más ("Catálogo") dentro de `src/modules/inventario/components/Home.tsx` (pedido explícito de Andrés: "que lo vea todo el equipo de oficina, no solo admin"). No hizo falta gateo nuevo — el módulo Inventario ya es inalcanzable para el rol técnico (su única route está etiquetada `'Inicio'` y `App.tsx` filtra esas para técnico), así que "todos los de oficina" = admin/jp/log ya queda cubierto tal cual. El archivo de Admin se borró; `AdminScreen.tsx` quedó sin esa sección. Tabla de las 86 SKU activas (una sola carga, sin paginar — filtro de texto simple por SKU/descripción/apodo, mismo criterio de "primera versión simple" que `LpuMapeoSection`), con nombre alternativo editable inline (guarda al perder foco), Tipo como `<select>` nativo con "+ Nuevo tipo…" al final (crea y asigna en el mismo paso) y Proveedores como popover con checkboxes (portal a `document.body`, mismo patrón que `LpuCodigoSelect`).
  - **Verificado en el navegador con la cuenta invitada jp (rol de oficina real) tras correr 0044** — encontrados y corregidos DOS bugs reales en el camino:
    1. **Nombre alternativo**: al principio pareció no guardar — resultó ser que mi primer intento de prueba disparaba un evento `blur` sintético, que React no escucha (usa `focusout` internamente para `onBlur`). Con una interacción real (o `focusout` disparado a mano) sí guarda y persiste tras recargar — no era un bug de la app.
    2. **Proveedores — bug real**: marcar dos checkboxes seguido (ej. Entel y luego CLEH) perdía uno de los dos al recargar, en orden distinto cada vez. Causa: cada clic disparaba su propio `UPDATE` a Supabase con el estado que tenía en ESE momento; con dos escrituras en carrera sobre la misma fila, la que el servidor responde último pisa a la otra sin importar cuál se envió primero — no alcanzaba con corregir el closure de React (se probó y seguía perdiéndose una). **Corregido de raíz**: los checkboxes ahora solo tocan estado local mientras el popover está abierto, y se guarda con un único `UPDATE` recién al cerrarlo (clic afuera) — sin escrituras en carrera posibles. Verificado con la misma prueba de estrés (marcar/desmarcar 2 rápido): quedó exactamente "Everything, CLEH" tras recargar, como se esperaba.
    Tipo "Cable ADSS" y nombre alternativo "CMIC" en SKU 51024 (ODF 12 fibras) verificados persistidos tras recargar.

- **Catálogo: columna Mínimo + Proveedores abiertos (31-07-2026, v1.17)**: dos pedidos explícitos de Andrés sobre el Catálogo recién movido.
  - **Columna Mínimo**: no es un campo nuevo — reutiliza `stockMinimo`/`UmbralEditor` que ya existían para la alerta roja/ámbar de la pestaña Bodega. Solo se agregó la columna en `CatalogoTab`/`FilaCatalogoMaterial` para poder editarlo también desde ahí; `onSaved` apunta al mismo `reload()` de la pestaña (mismo patrón que Bodega, no se intentó un parche local más fino).
  - **Proveedores abierto** ("que se pueda agregar nuevos proveedores como en tipo"): un `check` no puede validar contra una tabla, así que no bastaba con quitarle la lista fija al array — se rediseñó con el mismo patrón de `material_tipos`, pero N a N (un material puede tener varios proveedores).
    - **`supabase/migrations/0045_proveedores_abiertos.sql`** (nueva, **pendiente de correr**): tabla `proveedores` (id, nombre único) seedeada con Entel/Everything/CLEH — mismo RLS que `material_tipos` (lectura cualquier autenticado, alta `can_move_inventory()`, sin update/delete). Tabla de unión `material_proveedores` (material_id, proveedor_id, PK compuesta, ambos `on delete cascade`) — RLS `can_move_inventory()` para todo (select/insert/update/delete, ya que acá sí hace falta borrar filas al reemplazar la selección). Migra los datos que ya existían en el array viejo (`unnest` + join por nombre — la prueba real hecha en SKU 51024, "Everything"+"CLEH", queda preservada) antes de `drop column materiales.proveedores` y su `check` viejo.
    - **`inventarioRepo.ts`**: `listMateriales()` cambia el embed de `proveedores` (columna) a `material_proveedores(proveedores(id,nombre))` (join a través de la tabla de unión, aplanado en `materialFromRow`); `updateMaterialProveedores(materialId, proveedorIds: string[])` pasa de un simple `update` de array a un reemplazo real (`delete` + `insert` en la tabla de unión — mismo patrón "reemplazar todo" que `guardarEpLineas`); nuevas `listProveedores()`/`crearProveedor(nombre)`, mismo patrón que `listMaterialTipos`/`crearMaterialTipo`.
    - **UI**: `ProveedoresSelect` deja de usar la constante fija `PROVEEDORES_MATERIAL` (eliminada) y ahora recibe el catálogo completo de proveedores como prop (`catalogo: Proveedor[]`, cargado una vez en `CatalogoTab` vía `listProveedores()`); el popover suma un "+ Nuevo proveedor…" al final (mismo patrón que el "+ Nuevo tipo…" del `<select>` de Tipo, pero como input+botón dentro del popover en vez de una opción de `<select>`, porque acá es multi-selección) — crea el proveedor, lo agrega al catálogo compartido (visible de inmediato para las demás filas) y lo deja marcado para el material que se estaba editando.
  - **Verificado en el navegador**: `tsc --noEmit` limpio. Al abrir la pestaña Catálogo con la migración aún sin correr, falla limpio con `materiales.list: Could not find a relationship between 'materiales' and 'material_proveedores' in the schema cache` — confirma que el cliente ya apunta al esquema nuevo. Falta que Andrés corra `0045` y pruebe: columna Mínimo (que siga marcando fila roja/ámbar en Bodega), y "+ Nuevo proveedor…" en Catálogo.
  - **01-08-2026**: `0045` corrida y verificada por Andrés mismo (editó Mínimo y agregó un proveedor de prueba real antes de que yo llegara a probarlo) — confirmado en el navegador que ambas cosas persisten y coexisten sin pisarse.

- **Bug real: preventivo duplicado por corte de conexión durante el guardado (01-08-2026, v1.18)**: reportado por Andrés ("se duplicó desde mi pc un preventivo, creo que es por que lo moví offline") — pidió confirmar la causa. Investigado a fondo (agente Explore) y confirmado: **sí, esa es la causa**, y el mismo bug existe idéntico en ATT e Incidencias (los 3 comparten el patrón contra `projects`).
  - **Causa raíz**: el primer guardado de un borrador nuevo (id local = nanoid, no uuid) hacía `projects.insert(...)` dejando que Postgres generara el uuid — sin ninguna clave estable mandada por el cliente. Si la conexión se cortaba DESPUÉS de que ese insert ya se aplicó en el servidor pero ANTES de que el cliente recibiera la respuesta (exactamente lo que pasa al quedar offline a mitad de un guardado, o si el equipo se queda dormido un instante en ese punto), el registro local seguía con el nanoid viejo — nunca se enteraba del uuid nuevo. El siguiente reintento (autoguardado a los 800ms, o el botón "Guardar cambios") repetía el mismo insert con el mismo contenido, creando una segunda fila: el duplicado que vio Andrés. El resto de la cadena de guardado (informe/tramos/hitos/fotos/puntos) ya era find-or-create o upsert — por eso solo se duplicaba el `project` de nivel superior, no sus hijos.
  - **`supabase/migrations/0046_projects_client_local_id.sql`** (nueva, **pendiente de correr**): agrega `projects.client_local_id` (text, `unique`) — guarda el nanoid de creación del borrador.
  - **Fix idéntico en los 3 repos** (`src/modules/{att,preventivos,incidencias}/data/*Repo.ts` — no hay un repo compartido entre módulos, cada uno ya duplicaba `isUuid`/`currentUserId` a propósito, se siguió el mismo patrón): nueva función `insertProjectIdempotente(clientLocalId, patch)` — antes de insertar, busca si ya existe una fila con ese `client_local_id` (reintento tras respuesta perdida → la usa en vez de duplicar); si no existe, inserta mandando `client_local_id`; si el insert choca con `23505` (unique_violation — dos pestañas con el mismo borrador insertando casi a la vez), vuelve a buscar y usa la que ganó la carrera en vez de fallar (mismo patrón ya usado en `getOrCrearEpInforme`, PASO del 31-07-2026).
  - **Deliberadamente NO se cambió** el esquema de ids (nanoid→uuid al primer guardado, `isUuid(record.id)` como señal de "ya sincronizado") — ese criterio se usa en varios lugares más (`hasPendingSync`, gateo de la pestaña Logística en los 3 editores, `DescargarCerradosPanel`). Cambiarlo habría sido un refactor mucho más grande; `client_local_id` cierra la ventana de duplicado sin tocar esa convención.
  - **01-08-2026 — resuelto**: corrida `0046`. El duplicado era "Independencia" — Andrés confirmó cuál era el bueno y borró el otro directamente. No quedó pendiente ninguna limpieza de datos de este bug.

- **Registrar Movimiento — 3 mejoras pedidas por Andrés (01-08-2026, v1.19)**: desde Inventario → Entradas/Salidas → Registro.
  1. **Proyecto con buscador + alta rápida**: nuevo `src/ui/ProyectoSelect.tsx` (mismo patrón portal que `MaterialSelect`/`LpuCodigoSelect`) reemplaza el `<select>` nativo en `RegistrarMovimientoForm.tsx`. Si el texto escrito no coincide con ningún `ott` existente, ofrece "+ ATT" / "+ Preventivo/Incidencia" para crear un proyecto **base** (solo `ott`+`area`, sin informe/tramos — el JP lo completa después abriendo el editor real, que hace `update` sobre ese mismo id sin problema). Nueva `adminRepo.crearProyectoBase(ott, area)`. **Gateado a admin/jp** (`puedeCrear`, chequea `profile.rol`) — la RLS de insert en `projects` (`is_jp_or_admin`, 0001_init.sql) ya era admin/jp-only; un usuario `log` puede buscar y elegir pero no ve la opción de crear (no se tocó esa RLS para no ampliar quién puede dar de alta proyectos).
  2. **Bodega de origen por defecto según área**: el mecanismo YA existía (`BODEGA_DEFECTO_POR_AREA` en `src/lib/inventario/defaults.ts`, ATT→C088/OyM→C132) pero solo se aplicaba con `fixedProject` (pestaña Logística de ATT/Preventivo) — en esta pantalla standalone `fixedProject` siempre es `undefined`, así que nunca disparaba. Ahora se calcula un "área efectiva" (`fixedProject?.area ?? proyecto elegido.area ?? areaSel`) que se recalcula en cada render, no una sola vez.
     - **Bug propio encontrado y corregido en el momento**: la primera versión de este fix seguía rellenando solo líneas "vacías", pero el selector de Área (sin proyecto) ya rellena la bodega con el default de OyM apenas se abre el formulario — al elegir después un proyecto ATT, la línea ya "no estaba vacía" y el default de C088 nunca entraba (confirmado en el navegador: creé un proyecto ATT y la bodega se quedó en C132). Corregido guardando cuál fue el último default aplicado (`lastDefaultApplied` ref) y pisando también las líneas que sigan teniendo exactamente ese valor viejo, no solo las vacías.
  3. **Lote con disponibilidad**: ya estaba implementado — `LoteSelect` (compartido con `ResumenProyectoTable`, la vista "tipo ATT") ya lista `{lote} ({disponible})` en cuanto conoce material+bodega. Solo hacía falta el fix de (2) para que la bodega quedara elegida a tiempo; no se tocó `LoteSelect`.
  - **Verificado en el navegador con la cuenta jp**: buscador de proyecto filtra correctamente; escribir un ott exacto existente NO ofrece crear, uno nuevo sí; creado "TEST-PROYECTO-RAPIDO" como ATT y confirmado que la bodega pasó de C132 (default inicial sin proyecto) a C088 al elegirlo; Lote mostró opciones reales con cantidad (ej. "940158 (9)", "944309 (14)") para SKU 6 en C088. Quedó un proyecto base de prueba ("TEST-PROYECTO-RAPIDO", solo `projects`, sin informe) sin borrar — no se pudo eliminar desde esta sesión (no aparece en la lista de ATT al no tener informe asociado, y un borrado directo por REST está bloqueado a propósito para esta herramienta); agregar a la limpieza de datos de prueba pendiente.

- **Cutover a producción — paso previo (01-08-2026, v1.20)**: Andrés pidió pasar a producción sin esperar a correr la limpieza de datos de prueba. Antes de tocar `main`, dos decisiones tomadas explícitamente con él:
  1. **Sacar el invitado técnico del código ahora** (no dejarlo activo) — el propio comentario en `auth.ts` ya advertía "NO desplegar a producción con esta cuenta activa" (las vars `VITE_*` quedan embebidas en cualquier bundle construido con ese `.env`). Sacado de `src/lib/auth.ts` (`GUEST_TECNICO_EMAIL`/`PASSWORD`, `guestTecnicoEnabled`, `guestTecnicoPassword`, `signInAsGuestTecnico`, `GuestKind` vuelve a ser solo `'jp' | null`), `src/ui/LoginScreen.tsx` (botón "Continuar como invitado (técnico)" y su handler) y `src/ui/UserMenu.tsx` (ya no distingue tipo de invitado, `guestPassword` a secas). Vars sacadas de `.env` local (gitignored, no aparece en el diff). **Pendiente, no bloqueaba el cutover**: borrar la cuenta `iperez@sinterk.cl` en el dashboard de Supabase (Authentication → Users) — el código ya no ofrece ninguna forma de usarla, así que no hay apuro.
  2. **Mecánica del cutover**: `main` y `backend-supabase` están totalmente divergidos (`main` sigue siendo la app cliente-only pre-Supabase, commits "v0.52" etc.) — no es un merge normal. Andrés eligió reemplazar `main` directamente (`git push origin backend-supabase:main`), no abrir PR — dispara el deploy automático a GitHub Pages en cuanto se sube.
  - **Verificado**: `tsc --noEmit` limpio; grep de `GUEST_TECNICO`/`guestTecnico`/`signInAsGuestTecnico` en todo `src/` sin resultados tras el cambio.

- **Mapeo LPU movido a Catálogo (01-08-2026, v1.21)**: pedido de Andrés a mitad del cutover — "mueve material lpu y tendido lpu de administración a catálogo". `src/ui/admin/LpuMapeoSection.tsx` (las dos secciones "Material → Código LPU" y "Tendido → Código LPU") se borró; su contenido pasó a `src/modules/inventario/components/Home.tsx` como `LpuMaterialMapEditor`/`LpuTendidoMapEditor`, montadas dentro de `CatalogoTab` debajo de la tabla de SKU. Mismo motivo que el resto del Catálogo (Home.tsx ya es inalcanzable para técnico, no hace falta gateo aparte) y mismo patrón de migración que se usó para `CatalogoMaterialesSection` el 31-07-2026. `AdminScreen.tsx` quedó solo con Usuarios/Equipo.
  - **Verificado en el navegador con la cuenta jp**: ambas secciones ("Material → Código LPU"/"Tendido → Código LPU") aparecen dentro de Inventario → Catálogo, con el buscador de material funcionando; `/admin` sigue bloqueada para jp (sin cambios, ya lo estaba). `tsc --noEmit` limpio.

## Verificar conexión (PowerShell)
```powershell
$h = @{ apikey = "sb_publishable_x01p7hsYvNKlWklAQ11H-Q_KPenq-XW" }
Invoke-WebRequest -Uri "https://xwawplezarrfonuyaaxu.supabase.co/rest/v1/materiales?select=*&limit=1" -Headers $h -UseBasicParsing
# esperado: HTTP 200, body [] (RLS bloquea filas sin login)
```
